shadow$provide.module$node_modules$prosemirror_gapcursor$dist$index_cjs = function(global, require, module, exports) {
  function _typeof(obj) {
    "@babel/helpers - typeof";
    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(obj) {
      return typeof obj;
    } : function(obj) {
      return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
    }, _typeof(obj);
  }
  function _classCallCheck(instance, Constructor) {
    if (!(instance instanceof Constructor)) {
      throw new TypeError("Cannot call a class as a function");
    }
  }
  function _defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || !1;
      descriptor.configurable = !0;
      "value" in descriptor && (descriptor.writable = !0);
      Object.defineProperty(target, descriptor.key, descriptor);
    }
  }
  function _createClass(Constructor, protoProps, staticProps) {
    protoProps && _defineProperties(Constructor.prototype, protoProps);
    staticProps && _defineProperties(Constructor, staticProps);
    Object.defineProperty(Constructor, "prototype", {writable:!1});
    return Constructor;
  }
  function _inherits(subClass, superClass) {
    if ("function" !== typeof superClass && null !== superClass) {
      throw new TypeError("Super expression must either be null or a function");
    }
    subClass.prototype = Object.create(superClass && superClass.prototype, {constructor:{value:subClass, writable:!0, configurable:!0}});
    Object.defineProperty(subClass, "prototype", {writable:!1});
    superClass && _setPrototypeOf(subClass, superClass);
  }
  function _setPrototypeOf(o, p) {
    _setPrototypeOf = Object.setPrototypeOf || function(o, p) {
      o.__proto__ = p;
      return o;
    };
    return _setPrototypeOf(o, p);
  }
  function _createSuper(Derived) {
    var hasNativeReflectConstruct = _isNativeReflectConstruct();
    return function() {
      var Super = _getPrototypeOf(Derived);
      if (hasNativeReflectConstruct) {
        var NewTarget = _getPrototypeOf(this).constructor;
        Super = Reflect.construct(Super, arguments, NewTarget);
      } else {
        Super = Super.apply(this, arguments);
      }
      if (!Super || "object" !== _typeof(Super) && "function" !== typeof Super) {
        if (void 0 !== Super) {
          throw new TypeError("Derived constructors may only return object or undefined");
        }
        if (void 0 === this) {
          throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
        }
        Super = this;
      }
      return Super;
    };
  }
  function _isNativeReflectConstruct() {
    if ("undefined" === typeof Reflect || !Reflect.construct || Reflect.construct.sham) {
      return !1;
    }
    if ("function" === typeof Proxy) {
      return !0;
    }
    try {
      return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
      })), !0;
    } catch (e) {
      return !1;
    }
  }
  function _getPrototypeOf(o) {
    _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf : function(o) {
      return o.__proto__ || Object.getPrototypeOf(o);
    };
    return _getPrototypeOf(o);
  }
  function arrow(axis, dir) {
    var dirStr = "vert" == axis ? 0 < dir ? "down" : "up" : 0 < dir ? "right" : "left";
    return function(state, dispatch, view) {
      var sel = state.selection, $start = 0 < dir ? sel.$to : sel.$from, mustMove = sel.empty;
      if (sel instanceof prosemirrorState.TextSelection) {
        if (!view.endOfTextblock(dirStr) || 0 == $start.depth) {
          return !1;
        }
        mustMove = !1;
        $start = state.doc.resolve(0 < dir ? $start.after() : $start.before());
      }
      view = GapCursor.findGapCursorFrom($start, dir, mustMove);
      if (!view) {
        return !1;
      }
      dispatch && dispatch(state.tr.setSelection(new GapCursor(view)));
      return !0;
    };
  }
  function handleClick(view, pos, event) {
    if (!view || !view.editable) {
      return !1;
    }
    pos = view.state.doc.resolve(pos);
    if (!GapCursor.valid(pos) || (event = view.posAtCoords({left:event.clientX, top:event.clientY})) && -1 < event.inside && prosemirrorState.NodeSelection.isSelectable(view.state.doc.nodeAt(event.inside))) {
      return !1;
    }
    view.dispatch(view.state.tr.setSelection(new GapCursor(pos)));
    return !0;
  }
  function beforeinput(view, event) {
    if ("insertCompositionText" != event.inputType || !(view.state.selection instanceof GapCursor)) {
      return !1;
    }
    event = view.state.selection.$from;
    var insert = event.parent.contentMatchAt(event.index()).findWrapping(view.state.schema.nodes.text);
    if (!insert) {
      return !1;
    }
    for (var frag = prosemirrorModel.Fragment.empty, i = insert.length - 1; 0 <= i; i--) {
      frag = prosemirrorModel.Fragment.from(insert[i].createAndFill(null, frag));
    }
    insert = view.state.tr.replace(event.pos, event.pos, new prosemirrorModel.Slice(frag, 0, 0));
    insert.setSelection(prosemirrorState.TextSelection.near(insert.doc.resolve(event.pos + 1)));
    view.dispatch(insert);
    return !1;
  }
  function drawGapCursor(state) {
    if (!(state.selection instanceof GapCursor)) {
      return null;
    }
    var node = document.createElement("div");
    node.className = "ProseMirror-gapcursor";
    return prosemirrorView.DecorationSet.create(state.doc, [prosemirrorView.Decoration.widget(state.selection.head, node, {key:"gapcursor"})]);
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  global = require("module$node_modules$prosemirror_keymap$dist$index_cjs");
  var prosemirrorState = require("module$node_modules$prosemirror_state$dist$index_cjs"), prosemirrorModel = require("module$node_modules$prosemirror_model$dist$index_cjs"), prosemirrorView = require("module$node_modules$prosemirror_view$dist$index_cjs"), GapCursor = function(_prosemirrorState$Sel) {
    function GapCursor($pos) {
      _classCallCheck(this, GapCursor);
      return _super.call(this, $pos, $pos);
    }
    _inherits(GapCursor, _prosemirrorState$Sel);
    var _super = _createSuper(GapCursor);
    _createClass(GapCursor, [{key:"map", value:function(doc, mapping) {
      doc = doc.resolve(mapping.map(this.head));
      return GapCursor.valid(doc) ? new GapCursor(doc) : prosemirrorState.Selection.near(doc);
    }}, {key:"content", value:function() {
      return prosemirrorModel.Slice.empty;
    }}, {key:"eq", value:function(other) {
      return other instanceof GapCursor && other.head == this.head;
    }}, {key:"toJSON", value:function() {
      return {type:"gapcursor", pos:this.head};
    }}, {key:"getBookmark", value:function() {
      return new GapBookmark(this.anchor);
    }}], [{key:"fromJSON", value:function(doc, json) {
      if ("number" != typeof json.pos) {
        throw new RangeError("Invalid input for GapCursor.fromJSON");
      }
      return new GapCursor(doc.resolve(json.pos));
    }}, {key:"valid", value:function($pos) {
      var parent = $pos.parent, JSCompiler_temp;
      if (!(JSCompiler_temp = parent.isTextblock)) {
        a: {
          for (JSCompiler_temp = $pos.depth; 0 <= JSCompiler_temp; JSCompiler_temp--) {
            var index = $pos.index(JSCompiler_temp), parent$jscomp$0 = $pos.node(JSCompiler_temp);
            if (0 == index) {
              if (parent$jscomp$0.type.spec.isolating) {
                break;
              }
            } else {
              for (JSCompiler_temp = parent$jscomp$0.child(index - 1);; JSCompiler_temp = JSCompiler_temp.lastChild) {
                if (0 == JSCompiler_temp.childCount && !JSCompiler_temp.inlineContent || JSCompiler_temp.isAtom || JSCompiler_temp.type.spec.isolating) {
                  JSCompiler_temp = !0;
                  break a;
                }
                if (JSCompiler_temp.inlineContent) {
                  JSCompiler_temp = !1;
                  break a;
                }
              }
            }
          }
          JSCompiler_temp = !0;
        }
        JSCompiler_temp = !JSCompiler_temp;
      }
      if (!JSCompiler_temp) {
        a: {
          for (JSCompiler_temp = $pos.depth; 0 <= JSCompiler_temp; JSCompiler_temp--) {
            if (index = $pos.indexAfter(JSCompiler_temp), parent$jscomp$0 = $pos.node(JSCompiler_temp), index == parent$jscomp$0.childCount) {
              if (parent$jscomp$0.type.spec.isolating) {
                break;
              }
            } else {
              for (JSCompiler_temp = parent$jscomp$0.child(index);; JSCompiler_temp = JSCompiler_temp.firstChild) {
                if (0 == JSCompiler_temp.childCount && !JSCompiler_temp.inlineContent || JSCompiler_temp.isAtom || JSCompiler_temp.type.spec.isolating) {
                  JSCompiler_temp = !0;
                  break a;
                }
                if (JSCompiler_temp.inlineContent) {
                  JSCompiler_temp = !1;
                  break a;
                }
              }
            }
          }
          JSCompiler_temp = !0;
        }
        JSCompiler_temp = !JSCompiler_temp;
      }
      if (JSCompiler_temp) {
        return !1;
      }
      JSCompiler_temp = parent.type.spec.allowGapCursor;
      return null != JSCompiler_temp ? JSCompiler_temp : ($pos = parent.contentMatchAt($pos.index()).defaultType) && $pos.isTextblock;
    }}, {key:"findGapCursorFrom", value:function($pos, dir) {
      var mustMove = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : !1;
      a: for (;;) {
        if (!mustMove && GapCursor.valid($pos)) {
          return $pos;
        }
        mustMove = $pos.pos;
        var next;
        for (next = $pos.depth;; next--) {
          var parent = $pos.node(next);
          if (0 < dir ? $pos.indexAfter(next) < parent.childCount : 0 < $pos.index(next)) {
            next = parent.child(0 < dir ? $pos.indexAfter(next) : $pos.index(next) - 1);
            break;
          } else if (0 == next) {
            return null;
          }
          mustMove += dir;
          parent = $pos.doc.resolve(mustMove);
          if (GapCursor.valid(parent)) {
            return parent;
          }
        }
        for (;;) {
          parent = 0 < dir ? next.firstChild : next.lastChild;
          if (!parent) {
            if (next.isAtom && !next.isText && !prosemirrorState.NodeSelection.isSelectable(next)) {
              $pos = $pos.doc.resolve(mustMove + next.nodeSize * dir);
              mustMove = !1;
              continue a;
            }
            break;
          }
          next = parent;
          mustMove += dir;
          parent = $pos.doc.resolve(mustMove);
          if (GapCursor.valid(parent)) {
            return parent;
          }
        }
        return null;
      }
    }}]);
    return GapCursor;
  }(prosemirrorState.Selection);
  GapCursor.prototype.visible = !1;
  GapCursor.findFrom = GapCursor.findGapCursorFrom;
  prosemirrorState.Selection.jsonID("gapcursor", GapCursor);
  var GapBookmark = function() {
    function GapBookmark(pos) {
      _classCallCheck(this, GapBookmark);
      this.pos = pos;
    }
    _createClass(GapBookmark, [{key:"map", value:function(mapping) {
      return new GapBookmark(mapping.map(this.pos));
    }}, {key:"resolve", value:function(doc) {
      doc = doc.resolve(this.pos);
      return GapCursor.valid(doc) ? new GapCursor(doc) : prosemirrorState.Selection.near(doc);
    }}]);
    return GapBookmark;
  }(), handleKeyDown = global.keydownHandler({ArrowLeft:arrow("horiz", -1), ArrowRight:arrow("horiz", 1), ArrowUp:arrow("vert", -1), ArrowDown:arrow("vert", 1)});
  exports.GapCursor = GapCursor;
  exports.gapCursor = function() {
    return new prosemirrorState.Plugin({props:{decorations:drawGapCursor, createSelectionBetween:function(_view, $anchor, $head) {
      return $anchor.pos == $head.pos && GapCursor.valid($head) ? new GapCursor($head) : null;
    }, handleClick, handleKeyDown, handleDOMEvents:{beforeinput}}});
  };
};

//# sourceMappingURL=module$node_modules$prosemirror_gapcursor$dist$index_cjs.js.map
