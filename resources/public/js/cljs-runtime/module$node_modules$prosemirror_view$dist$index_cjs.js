shadow$provide.module$node_modules$prosemirror_view$dist$index_cjs = function(global, require, module, exports) {
  function _slicedToArray(arr, i$jscomp$0) {
    var JSCompiler_temp = Array.isArray(arr) ? arr : void 0;
    if (!JSCompiler_temp) {
      a: {
        var t = null == arr ? null : "undefined" != typeof Symbol && arr[Symbol.iterator] || arr["@@iterator"];
        if (null != t) {
          var e, i, u, a = [], f = !0, o = !1;
          try {
            if (i = (t = t.call(arr)).next, 0 === i$jscomp$0) {
              if (Object(t) !== t) {
                JSCompiler_temp = void 0;
                break a;
              }
              f = !1;
            } else {
              for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== i$jscomp$0); f = !0) {
              }
            }
          } catch (r) {
            o = !0;
            var n = r;
          } finally {
            try {
              if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) {
                JSCompiler_temp = void 0;
                break a;
              }
            } finally {
              if (o) {
                throw n;
              }
            }
          }
          JSCompiler_temp = a;
        } else {
          JSCompiler_temp = void 0;
        }
      }
    }
    if (!(arr = JSCompiler_temp || _unsupportedIterableToArray(arr, i$jscomp$0))) {
      throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    return arr;
  }
  function _createForOfIteratorHelper(o, allowArrayLike) {
    var it = "undefined" !== typeof Symbol && o[Symbol.iterator] || o["@@iterator"];
    if (!it) {
      if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && "number" === typeof o.length) {
        it && (o = it);
        var i = 0;
        allowArrayLike = function() {
        };
        return {s:allowArrayLike, n:function() {
          return i >= o.length ? {done:!0} : {done:!1, value:o[i++]};
        }, e:function(_e) {
          throw _e;
        }, f:allowArrayLike};
      }
      throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    var normalCompletion = !0, didErr = !1, err;
    return {s:function() {
      it = it.call(o);
    }, n:function() {
      var step = it.next();
      normalCompletion = step.done;
      return step;
    }, e:function(_e2) {
      didErr = !0;
      err = _e2;
    }, f:function() {
      try {
        if (!normalCompletion && null != it["return"]) {
          it["return"]();
        }
      } finally {
        if (didErr) {
          throw err;
        }
      }
    }};
  }
  function _unsupportedIterableToArray(o, minLen) {
    if (o) {
      if ("string" === typeof o) {
        return _arrayLikeToArray(o, minLen);
      }
      var n = Object.prototype.toString.call(o).slice(8, -1);
      "Object" === n && o.constructor && (n = o.constructor.name);
      if ("Map" === n || "Set" === n) {
        return Array.from(o);
      }
      if ("Arguments" === n || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) {
        return _arrayLikeToArray(o, minLen);
      }
    }
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  function _get() {
    _get = "undefined" !== typeof Reflect && Reflect.get ? Reflect.get.bind() : function(target, property, receiver) {
      var base;
      for (base = target; !Object.prototype.hasOwnProperty.call(base, property) && (base = _getPrototypeOf(base), null !== base);) {
      }
      if (base) {
        return base = Object.getOwnPropertyDescriptor(base, property), base.get ? base.get.call(3 > arguments.length ? target : receiver) : base.value;
      }
    };
    return _get.apply(this, arguments);
  }
  function _inherits(subClass, superClass) {
    if ("function" !== typeof superClass && null !== superClass) {
      throw new TypeError("Super expression must either be null or a function");
    }
    subClass.prototype = Object.create(superClass && superClass.prototype, {constructor:{value:subClass, writable:!0, configurable:!0}});
    Object.defineProperty(subClass, "prototype", {writable:!1});
    superClass && _setPrototypeOf(subClass, superClass);
  }
  function _setPrototypeOf(o, p) {
    _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(o, p) {
      o.__proto__ = p;
      return o;
    };
    return _setPrototypeOf(o, p);
  }
  function _createSuper(Derived) {
    var hasNativeReflectConstruct = _isNativeReflectConstruct();
    return function() {
      var Super = _getPrototypeOf(Derived);
      if (hasNativeReflectConstruct) {
        var NewTarget = _getPrototypeOf(this).constructor;
        Super = Reflect.construct(Super, arguments, NewTarget);
      } else {
        Super = Super.apply(this, arguments);
      }
      if (!Super || "object" !== _typeof(Super) && "function" !== typeof Super) {
        if (void 0 !== Super) {
          throw new TypeError("Derived constructors may only return object or undefined");
        }
        Super = _assertThisInitialized(this);
      }
      return Super;
    };
  }
  function _assertThisInitialized(self) {
    if (void 0 === self) {
      throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
    }
    return self;
  }
  function _isNativeReflectConstruct() {
    if ("undefined" === typeof Reflect || !Reflect.construct || Reflect.construct.sham) {
      return !1;
    }
    if ("function" === typeof Proxy) {
      return !0;
    }
    try {
      return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
      })), !0;
    } catch (e) {
      return !1;
    }
  }
  function _getPrototypeOf(o) {
    _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(o) {
      return o.__proto__ || Object.getPrototypeOf(o);
    };
    return _getPrototypeOf(o);
  }
  function _typeof(o) {
    "@babel/helpers - typeof";
    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
      return typeof o;
    } : function(o) {
      return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
    }, _typeof(o);
  }
  function _classCallCheck(instance, Constructor) {
    if (!(instance instanceof Constructor)) {
      throw new TypeError("Cannot call a class as a function");
    }
  }
  function _defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || !1;
      descriptor.configurable = !0;
      "value" in descriptor && (descriptor.writable = !0);
      var JSCompiler_temp_const = Object, JSCompiler_temp_const$jscomp$0 = JSCompiler_temp_const.defineProperty;
      a: {
        var JSCompiler_inline_result = descriptor.key;
        if ("object" === _typeof(JSCompiler_inline_result) && null !== JSCompiler_inline_result) {
          var prim = JSCompiler_inline_result[Symbol.toPrimitive];
          if (void 0 !== prim) {
            JSCompiler_inline_result = prim.call(JSCompiler_inline_result, "string");
            if ("object" !== _typeof(JSCompiler_inline_result)) {
              break a;
            }
            throw new TypeError("@@toPrimitive must return a primitive value.");
          }
          JSCompiler_inline_result = String(JSCompiler_inline_result);
        }
      }
      JSCompiler_inline_result = "symbol" === _typeof(JSCompiler_inline_result) ? JSCompiler_inline_result : String(JSCompiler_inline_result);
      JSCompiler_temp_const$jscomp$0.call(JSCompiler_temp_const, target, JSCompiler_inline_result, descriptor);
    }
  }
  function _createClass(Constructor, protoProps, staticProps) {
    protoProps && _defineProperties(Constructor.prototype, protoProps);
    staticProps && _defineProperties(Constructor, staticProps);
    Object.defineProperty(Constructor, "prototype", {writable:!1});
    return Constructor;
  }
  function scanFor(node, off, targetNode, targetOff, dir) {
    for (;;) {
      if (node == targetNode && off == targetOff) {
        return !0;
      }
      if (off == (0 > dir ? 0 : nodeSize(node))) {
        var parent = node.parentNode;
        if (!parent || 1 != parent.nodeType || hasBlockDesc(node) || atomElements.test(node.nodeName) || "false" == node.contentEditable) {
          return !1;
        }
        off = domIndex(node) + (0 > dir ? 0 : 1);
        node = parent;
      } else if (1 == node.nodeType) {
        node = node.childNodes[off + (0 > dir ? -1 : 0)];
        if ("false" == node.contentEditable) {
          return !1;
        }
        off = 0 > dir ? nodeSize(node) : 0;
      } else {
        return !1;
      }
    }
  }
  function nodeSize(node) {
    return 3 == node.nodeType ? node.nodeValue.length : node.childNodes.length;
  }
  function textNodeBefore$1(node, offset) {
    for (;;) {
      if (3 == node.nodeType && offset) {
        return node;
      }
      if (1 == node.nodeType && 0 < offset) {
        if ("false" == node.contentEditable) {
          return null;
        }
        node = node.childNodes[offset - 1];
        offset = nodeSize(node);
      } else if (node.parentNode && !hasBlockDesc(node)) {
        offset = domIndex(node), node = node.parentNode;
      } else {
        return null;
      }
    }
  }
  function textNodeAfter$1(node, offset) {
    for (;;) {
      if (3 == node.nodeType && offset < node.nodeValue.length) {
        return node;
      }
      if (1 == node.nodeType && offset < node.childNodes.length) {
        if ("false" == node.contentEditable) {
          return null;
        }
        node = node.childNodes[offset];
        offset = 0;
      } else if (node.parentNode && !hasBlockDesc(node)) {
        offset = domIndex(node) + 1, node = node.parentNode;
      } else {
        return null;
      }
    }
  }
  function hasBlockDesc(dom) {
    for (var desc, cur = dom; cur && !(desc = cur.pmViewDesc); cur = cur.parentNode) {
    }
    return desc && desc.node && desc.node.isBlock && (desc.dom == dom || desc.contentDOM == dom);
  }
  function keyEvent(keyCode, key) {
    var event = document.createEvent("Event");
    event.initEvent("keydown", !0, !0);
    event.keyCode = keyCode;
    event.key = event.code = key;
    return event;
  }
  function getSide(value, side) {
    return "number" == typeof value ? value : value[side];
  }
  function scrollRectIntoView(view, rect$jscomp$0, startDOM) {
    var scrollThreshold = view.someProp("scrollThreshold") || 0, scrollMargin = view.someProp("scrollMargin") || 5, doc = view.dom.ownerDocument;
    for (view = startDOM || view.dom; view; view = parentNode(view)) {
      if (1 == view.nodeType) {
        var elt = view;
        startDOM = elt == doc.body;
        if (startDOM) {
          var JSCompiler_temp = (JSCompiler_temp = doc.defaultView && doc.defaultView.visualViewport) ? {left:0, right:JSCompiler_temp.width, top:0, bottom:JSCompiler_temp.height} : {left:0, right:doc.documentElement.clientWidth, top:0, bottom:doc.documentElement.clientHeight};
        } else {
          JSCompiler_temp = elt;
          var rect = JSCompiler_temp.getBoundingClientRect();
          JSCompiler_temp = {left:rect.left, right:rect.left + JSCompiler_temp.clientWidth * (rect.width / JSCompiler_temp.offsetWidth || 1), top:rect.top, bottom:rect.top + JSCompiler_temp.clientHeight * (rect.height / JSCompiler_temp.offsetHeight || 1)};
        }
        var bounding = JSCompiler_temp;
        rect = JSCompiler_temp = 0;
        rect$jscomp$0.top < bounding.top + getSide(scrollThreshold, "top") ? rect = -(bounding.top - rect$jscomp$0.top + getSide(scrollMargin, "top")) : rect$jscomp$0.bottom > bounding.bottom - getSide(scrollThreshold, "bottom") && (rect = rect$jscomp$0.bottom - rect$jscomp$0.top > bounding.bottom - bounding.top ? rect$jscomp$0.top + getSide(scrollMargin, "top") - bounding.top : rect$jscomp$0.bottom - bounding.bottom + getSide(scrollMargin, "bottom"));
        rect$jscomp$0.left < bounding.left + getSide(scrollThreshold, "left") ? JSCompiler_temp = -(bounding.left - rect$jscomp$0.left + getSide(scrollMargin, "left")) : rect$jscomp$0.right > bounding.right - getSide(scrollThreshold, "right") && (JSCompiler_temp = rect$jscomp$0.right - bounding.right + getSide(scrollMargin, "right"));
        if (JSCompiler_temp || rect) {
          if (startDOM) {
            doc.defaultView.scrollBy(JSCompiler_temp, rect);
          } else {
            var startX = elt.scrollLeft;
            bounding = elt.scrollTop;
            rect && (elt.scrollTop += rect);
            JSCompiler_temp && (elt.scrollLeft += JSCompiler_temp);
            JSCompiler_temp = elt.scrollLeft - startX;
            elt = elt.scrollTop - bounding;
            rect$jscomp$0 = {left:rect$jscomp$0.left - JSCompiler_temp, top:rect$jscomp$0.top - elt, right:rect$jscomp$0.right - JSCompiler_temp, bottom:rect$jscomp$0.bottom - elt};
          }
        }
        if (startDOM || /^(fixed|sticky)$/.test(getComputedStyle(view).position)) {
          break;
        }
      }
    }
  }
  function scrollStack(dom) {
    for (var stack = [], doc = dom.ownerDocument, cur = dom; cur && (stack.push({dom:cur, top:cur.scrollTop, left:cur.scrollLeft}), dom != doc); cur = parentNode(cur)) {
    }
    return stack;
  }
  function restoreScrollStack(stack, dTop) {
    for (var i = 0; i < stack.length; i++) {
      var _stack$i = stack[i], dom = _stack$i.dom, top = _stack$i.top;
      _stack$i = _stack$i.left;
      dom.scrollTop != top + dTop && (dom.scrollTop = top + dTop);
      dom.scrollLeft != _stack$i && (dom.scrollLeft = _stack$i);
    }
  }
  function focusPreventScroll(dom) {
    if (dom.setActive) {
      return dom.setActive();
    }
    if (preventScrollSupported) {
      return dom.focus(preventScrollSupported);
    }
    var stored = scrollStack(dom);
    dom.focus(null == preventScrollSupported ? {get preventScroll() {
      preventScrollSupported = {preventScroll:!0};
      return !0;
    }} : void 0);
    preventScrollSupported || (preventScrollSupported = !1, restoreScrollStack(stored, 0));
  }
  function findOffsetInNode(node, coords) {
    for (var closest, dxClosest = 2e8, coordsClosest, offset = 0, rowBot = coords.top, rowTop = coords.top, firstBelow, coordsBelow, child = node.firstChild, childIndex = 0; child; child = child.nextSibling, childIndex++) {
      if (1 == child.nodeType) {
        var rects = child.getClientRects();
      } else if (3 == child.nodeType) {
        rects = textRange(child).getClientRects();
      } else {
        continue;
      }
      for (var i = 0; i < rects.length; i++) {
        var rect = rects[i];
        if (rect.top <= rowBot && rect.bottom >= rowTop) {
          rowBot = Math.max(rect.bottom, rowBot);
          rowTop = Math.min(rect.top, rowTop);
          var dx = rect.left > coords.left ? rect.left - coords.left : rect.right < coords.left ? coords.left - rect.right : 0;
          if (dx < dxClosest) {
            closest = child;
            coordsClosest = (dxClosest = dx) && 3 == closest.nodeType ? {left:rect.right < coords.left ? rect.right : rect.left, top:coords.top} : coords;
            1 == child.nodeType && dx && (offset = childIndex + (coords.left >= (rect.left + rect.right) / 2 ? 1 : 0));
            continue;
          }
        } else {
          rect.top > coords.top && !firstBelow && rect.left <= coords.left && rect.right >= coords.left && (firstBelow = child, coordsBelow = {left:Math.max(rect.left, Math.min(rect.right, coords.left)), top:rect.top});
        }
        !closest && (coords.left >= rect.right && coords.top >= rect.top || coords.left >= rect.left && coords.top >= rect.bottom) && (offset = childIndex + 1);
      }
    }
    !closest && firstBelow && (closest = firstBelow, coordsClosest = coordsBelow, dxClosest = 0);
    if (closest && 3 == closest.nodeType) {
      a: {
        node = closest;
        coords = node.nodeValue.length;
        closest = document.createRange();
        for (dxClosest = 0; dxClosest < coords; dxClosest++) {
          if (closest.setEnd(node, dxClosest + 1), closest.setStart(node, dxClosest), offset = singleRect(closest, 1), offset.top != offset.bottom && inRect(coordsClosest, offset)) {
            coordsClosest = {node, offset:dxClosest + (coordsClosest.left >= (offset.left + offset.right) / 2 ? 1 : 0)};
            break a;
          }
        }
        coordsClosest = {node, offset:0};
      }
      return coordsClosest;
    }
    return !closest || dxClosest && 1 == closest.nodeType ? {node, offset} : findOffsetInNode(closest, coordsClosest);
  }
  function inRect(coords, rect) {
    return coords.left >= rect.left - 1 && coords.left <= rect.right + 1 && coords.top >= rect.top - 1 && coords.top <= rect.bottom + 1;
  }
  function elementFromPoint(element, coords, box) {
    var len = element.childNodes.length;
    if (len && box.top < box.bottom) {
      for (var i = box = Math.max(0, Math.min(len - 1, Math.floor(len * (coords.top - box.top) / (box.bottom - box.top)) - 2));;) {
        var child = element.childNodes[i];
        if (1 == child.nodeType) {
          for (var rects = child.getClientRects(), j = 0; j < rects.length; j++) {
            var rect = rects[j];
            if (inRect(coords, rect)) {
              return elementFromPoint(child, coords, rect);
            }
          }
        }
        if ((i = (i + 1) % len) == box) {
          break;
        }
      }
    }
    return element;
  }
  function nonZero(rect) {
    return rect.top < rect.bottom || rect.left < rect.right;
  }
  function singleRect(target, bias) {
    var rects = target.getClientRects();
    return rects.length && (bias = rects[0 > bias ? 0 : rects.length - 1], nonZero(bias)) ? bias : Array.prototype.find.call(rects, nonZero) || target.getBoundingClientRect();
  }
  function _coordsAtPos(view, pos, side) {
    var _view$docView$domFrom = view.docView.domFromPos(pos, 0 > side ? -1 : 1), node = _view$docView$domFrom.node, offset = _view$docView$domFrom.offset;
    _view$docView$domFrom = _view$docView$domFrom.atom;
    var supportEmptyRange = webkit || gecko;
    if (3 == node.nodeType) {
      if (!supportEmptyRange || !BIDI.test(node.nodeValue) && (0 > side ? offset : offset != node.nodeValue.length)) {
        return supportEmptyRange = _view$docView$domFrom = offset, view = 0 > side ? 1 : -1, 0 > side && !offset ? (supportEmptyRange++, view = -1) : 0 <= side && offset == node.nodeValue.length ? (_view$docView$domFrom--, view = 1) : 0 > side ? _view$docView$domFrom-- : supportEmptyRange++, flattenV(singleRect(textRange(node, _view$docView$domFrom, supportEmptyRange), view), 0 > view);
      }
      side = singleRect(textRange(node, offset, offset), side);
      return gecko && offset && /\s/.test(node.nodeValue[offset - 1]) && offset < node.nodeValue.length && (_view$docView$domFrom = singleRect(textRange(node, offset - 1, offset - 1), -1), _view$docView$domFrom.top == side.top && (node = singleRect(textRange(node, offset, offset + 1), -1), node.top != side.top)) ? flattenV(node, node.left < _view$docView$domFrom.left) : side;
    }
    if (!view.state.doc.resolve(pos - (_view$docView$domFrom || 0)).parent.inlineContent) {
      return null == _view$docView$domFrom && offset && (0 > side || offset == nodeSize(node)) && (supportEmptyRange = node.childNodes[offset - 1], 1 == supportEmptyRange.nodeType) ? flattenH(supportEmptyRange.getBoundingClientRect(), !1) : null == _view$docView$domFrom && offset < nodeSize(node) && (offset = node.childNodes[offset], 1 == offset.nodeType) ? flattenH(offset.getBoundingClientRect(), !0) : flattenH(node.getBoundingClientRect(), 0 <= side);
    }
    if (null == _view$docView$domFrom && offset && (0 > side || offset == nodeSize(node)) && (view = node.childNodes[offset - 1], view = 3 == view.nodeType ? textRange(view, nodeSize(view) - (supportEmptyRange ? 0 : 1)) : 1 != view.nodeType || "BR" == view.nodeName && view.nextSibling ? null : view)) {
      return flattenV(singleRect(view, 1), !1);
    }
    if (null == _view$docView$domFrom && offset < nodeSize(node)) {
      for (offset = node.childNodes[offset]; offset.pmViewDesc && offset.pmViewDesc.ignoreForCoords;) {
        offset = offset.nextSibling;
      }
      if (offset = offset ? 3 == offset.nodeType ? textRange(offset, 0, supportEmptyRange ? 0 : 1) : 1 == offset.nodeType ? offset : null : null) {
        return flattenV(singleRect(offset, -1), !0);
      }
    }
    return flattenV(singleRect(3 == node.nodeType ? textRange(node) : node, -side), 0 <= side);
  }
  function flattenV(rect, left) {
    if (0 == rect.width) {
      return rect;
    }
    left = left ? rect.left : rect.right;
    return {top:rect.top, bottom:rect.bottom, left, right:left};
  }
  function flattenH(rect, top) {
    if (0 == rect.height) {
      return rect;
    }
    top = top ? rect.top : rect.bottom;
    return {top, bottom:top, left:rect.left, right:rect.right};
  }
  function withFlushedState(view, state, f) {
    var viewState = view.state, active = view.root.activeElement;
    viewState != state && view.updateState(state);
    active != view.dom && view.focus();
    try {
      return f();
    } finally {
      viewState != state && view.updateState(viewState), active != view.dom && active && active.focus();
    }
  }
  function endOfTextblockVertical(view, state, dir) {
    var sel = state.selection, $pos = "up" == dir ? sel.$from : sel.$to;
    return withFlushedState(view, state, function() {
      for (var dom = view.docView.domFromPos($pos.pos, "up" == dir ? -1 : 1).node;;) {
        var nearest = view.docView.nearestDesc(dom, !0);
        if (!nearest) {
          break;
        }
        if (nearest.node.isBlock) {
          dom = nearest.contentDOM || nearest.dom;
          break;
        }
        dom = nearest.dom.parentNode;
      }
      nearest = _coordsAtPos(view, $pos.pos, 1);
      for (dom = dom.firstChild; dom; dom = dom.nextSibling) {
        if (1 == dom.nodeType) {
          var boxes = dom.getClientRects();
        } else if (3 == dom.nodeType) {
          boxes = textRange(dom, 0, dom.nodeValue.length).getClientRects();
        } else {
          continue;
        }
        for (var i = 0; i < boxes.length; i++) {
          var box = boxes[i];
          if (box.bottom > box.top + 1 && ("up" == dir ? nearest.top - box.top > 2 * (box.bottom - nearest.top) : box.bottom - nearest.bottom > 2 * (nearest.bottom - box.top))) {
            return !1;
          }
        }
      }
      return !0;
    });
  }
  function endOfTextblockHorizontal(view, state, dir) {
    var $head = state.selection.$head;
    if (!$head.parent.isTextblock) {
      return !1;
    }
    var offset = $head.parentOffset, atStart = !offset;
    offset = offset == $head.parent.content.size;
    var sel = view.domSelection();
    return sel ? maybeRTL.test($head.parent.textContent) && sel.modify ? withFlushedState(view, state, function() {
      var _view$domSelectionRan = view.domSelectionRange(), oldNode = _view$domSelectionRan.focusNode, oldOff = _view$domSelectionRan.focusOffset, anchorNode = _view$domSelectionRan.anchorNode;
      _view$domSelectionRan = _view$domSelectionRan.anchorOffset;
      var oldBidiLevel = sel.caretBidiLevel;
      sel.modify("move", dir, "character");
      var parentDOM = $head.depth ? view.docView.domAfterPos($head.before()) : view.dom, _view$domSelectionRan2 = view.domSelectionRange(), newNode = _view$domSelectionRan2.focusNode;
      _view$domSelectionRan2 = _view$domSelectionRan2.focusOffset;
      parentDOM = newNode && !parentDOM.contains(1 == newNode.nodeType ? newNode : newNode.parentNode) || oldNode == newNode && oldOff == _view$domSelectionRan2;
      try {
        sel.collapse(anchorNode, _view$domSelectionRan), oldNode && (oldNode != anchorNode || oldOff != _view$domSelectionRan) && sel.extend && sel.extend(oldNode, oldOff);
      } catch (_) {
      }
      null != oldBidiLevel && (sel.caretBidiLevel = oldBidiLevel);
      return parentDOM;
    }) : "left" == dir || "backward" == dir ? atStart : offset : $head.pos == $head.start() || $head.pos == $head.end();
  }
  function docViewDesc(doc, outerDeco, innerDeco, dom, view) {
    patchOuterDeco(dom, dom, noDeco, computeOuterDeco(outerDeco, doc, 1 != dom.nodeType));
    doc = new NodeViewDesc(void 0, doc, outerDeco, innerDeco, dom, dom, dom, view, 0);
    doc.contentDOM && doc.updateChildren(view, 0);
    return doc;
  }
  function renderDescs(parentDOM, descs, view) {
    for (var dom = parentDOM.firstChild, written = !1, i = 0; i < descs.length; i++) {
      var desc = descs[i], childDOM = desc.dom;
      if (childDOM.parentNode == parentDOM) {
        for (; childDOM != dom;) {
          dom = rm(dom), written = !0;
        }
        dom = dom.nextSibling;
      } else {
        written = !0, parentDOM.insertBefore(childDOM, dom);
      }
      desc instanceof MarkViewDesc && (dom = dom ? dom.previousSibling : parentDOM.lastChild, renderDescs(desc.contentDOM, desc.children, view), dom = dom ? dom.nextSibling : parentDOM.firstChild);
    }
    for (; dom;) {
      dom = rm(dom), written = !0;
    }
    written && view.trackWrites == parentDOM && (view.trackWrites = null);
  }
  function computeOuterDeco(outerDeco, node, needsWrap) {
    if (0 == outerDeco.length) {
      return noDeco;
    }
    for (var top = needsWrap ? noDeco[0] : new OuterDecoLevel(), result = [top], i = 0; i < outerDeco.length; i++) {
      var attrs = outerDeco[i].type.attrs;
      if (attrs) {
        attrs.nodeName && result.push(top = new OuterDecoLevel(attrs.nodeName));
        for (var name in attrs) {
          var val = attrs[name];
          null != val && (needsWrap && 1 == result.length && result.push(top = new OuterDecoLevel(node.isInline ? "span" : "div")), "class" == name ? top["class"] = (top["class"] ? top["class"] + " " : "") + val : "style" == name ? top.style = (top.style ? top.style + ";" : "") + val : "nodeName" != name && (top[name] = val));
        }
      }
    }
    return result;
  }
  function patchOuterDeco(outerDOM, nodeDOM, prevComputed, curComputed) {
    if (prevComputed == noDeco && curComputed == noDeco) {
      return nodeDOM;
    }
    for (var i = 0; i < curComputed.length; i++) {
      var deco = curComputed[i], prev = prevComputed[i];
      if (i) {
        var parent = void 0;
        prev && prev.nodeName == deco.nodeName && nodeDOM != outerDOM && (parent = nodeDOM.parentNode) && parent.nodeName.toLowerCase() == deco.nodeName || (parent = document.createElement(deco.nodeName), parent.pmIsDeco = !0, parent.appendChild(nodeDOM), prev = noDeco[0]);
        nodeDOM = parent;
      }
      var m, name = m = void 0;
      parent = nodeDOM;
      prev = prev || noDeco[0];
      for (name in prev) {
        "class" == name || "style" == name || "nodeName" == name || name in deco || parent.removeAttribute(name);
      }
      for (m in deco) {
        "class" != m && "style" != m && "nodeName" != m && deco[m] != prev[m] && parent.setAttribute(m, deco[m]);
      }
      if (prev["class"] != deco["class"]) {
        m = prev["class"] ? prev["class"].split(" ").filter(Boolean) : [];
        name = deco["class"] ? deco["class"].split(" ").filter(Boolean) : [];
        for (var i$jscomp$0 = 0; i$jscomp$0 < m.length; i$jscomp$0++) {
          -1 == name.indexOf(m[i$jscomp$0]) && parent.classList.remove(m[i$jscomp$0]);
        }
        for (i$jscomp$0 = 0; i$jscomp$0 < name.length; i$jscomp$0++) {
          -1 == m.indexOf(name[i$jscomp$0]) && parent.classList.add(name[i$jscomp$0]);
        }
        0 == parent.classList.length && parent.removeAttribute("class");
      }
      if (prev.style != deco.style) {
        if (prev.style) {
          for (name = /\s*([\w\-\xa1-\uffff]+)\s*:(?:"(?:\\.|[^"])*"|'(?:\\.|[^'])*'|\(.*?\)|[^;])*/g; m = name.exec(prev.style);) {
            parent.style.removeProperty(m[1]);
          }
        }
        deco.style && (parent.style.cssText += deco.style);
      }
    }
    return nodeDOM;
  }
  function sameOuterDeco(a, b) {
    if (a.length != b.length) {
      return !1;
    }
    for (var i = 0; i < a.length; i++) {
      if (!a[i].type.eq(b[i].type)) {
        return !1;
      }
    }
    return !0;
  }
  function rm(dom) {
    var next = dom.nextSibling;
    dom.parentNode.removeChild(dom);
    return next;
  }
  function compareSide(a, b) {
    return a.type.side - b.type.side;
  }
  function iterDeco(parent, deco, onWidget, onNode) {
    var locals = deco.locals(parent), offset = 0;
    if (0 == locals.length) {
      for (onWidget = 0; onWidget < parent.childCount; onWidget++) {
        var child = parent.child(onWidget);
        onNode(child, locals, deco.forChild(offset, child), onWidget);
        offset += child.nodeSize;
      }
    } else {
      child = 0;
      for (var active = [], restNode = null, parentIndex = 0;;) {
        for (var widget = void 0, widgets = void 0; child < locals.length && locals[child].to == offset;) {
          var next = locals[child++];
          next.widget && (widget ? (widgets || (widgets = [widget])).push(next) : widget = next);
        }
        if (widget) {
          if (widgets) {
            for (widgets.sort(compareSide), widget = 0; widget < widgets.length; widget++) {
              onWidget(widgets[widget], parentIndex, !!restNode);
            }
          } else {
            onWidget(widget, parentIndex, !!restNode);
          }
        }
        widget = widgets = void 0;
        if (restNode) {
          widget = -1, widgets = restNode, restNode = null;
        } else if (parentIndex < parent.childCount) {
          widget = parentIndex, widgets = parent.child(parentIndex++);
        } else {
          break;
        }
        for (next = 0; next < active.length; next++) {
          active[next].to <= offset && active.splice(next--, 1);
        }
        for (; child < locals.length && locals[child].from <= offset && locals[child].to > offset;) {
          active.push(locals[child++]);
        }
        next = offset + widgets.nodeSize;
        if (widgets.isText) {
          var cutAt = next;
          child < locals.length && locals[child].from < cutAt && (cutAt = locals[child].from);
          for (var _i4 = 0; _i4 < active.length; _i4++) {
            active[_i4].to < cutAt && (cutAt = active[_i4].to);
          }
          cutAt < next && (restNode = widgets.cut(cutAt - offset), widgets = widgets.cut(0, cutAt - offset), next = cutAt, widget = -1);
        } else {
          for (; child < locals.length && locals[child].to < next;) {
            child++;
          }
        }
        cutAt = widgets.isInline && !widgets.isLeaf ? active.filter(function(d) {
          return !d.inline;
        }) : active.slice();
        onNode(widgets, cutAt, deco.forChild(offset, widgets), widget);
        offset = next;
      }
    }
  }
  function iosHacks(dom) {
    if ("UL" == dom.nodeName || "OL" == dom.nodeName) {
      var oldCSS = dom.style.cssText;
      dom.style.cssText = oldCSS + "; list-style: square !important";
      window.getComputedStyle(dom).listStyle;
      dom.style.cssText = oldCSS;
    }
  }
  function replaceNodes(nodes, from, to, view, replacement) {
    for (var result = [], i = 0, off = 0; i < nodes.length; i++) {
      var child = nodes[i], start = off, end = off += child.size;
      start >= to || end <= from ? result.push(child) : (start < from && result.push(child.slice(0, from - start, view)), replacement && (result.push(replacement), replacement = void 0), end > to && result.push(child.slice(to - start, child.size, view)));
    }
    return result;
  }
  function selectionFromDOM(view) {
    var origin = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : null, domSel = view.domSelectionRange(), doc = view.state.doc;
    if (!domSel.focusNode) {
      return null;
    }
    var nearestDesc = view.docView.nearestDesc(domSel.focusNode), inWidget = nearestDesc && 0 == nearestDesc.size, head = view.docView.posFromDOM(domSel.focusNode, domSel.focusOffset, 1);
    if (0 > head) {
      return null;
    }
    var $head = doc.resolve(head), anchor;
    if (selectionCollapsed(domSel)) {
      for (anchor = head; nearestDesc && !nearestDesc.node;) {
        nearestDesc = nearestDesc.parent;
      }
      var nearestDescNode = nearestDesc.node, JSCompiler_temp;
      if (JSCompiler_temp = nearestDesc && nearestDescNode.isAtom && prosemirrorState.NodeSelection.isSelectable(nearestDescNode) && nearestDesc.parent) {
        if (nearestDescNode = nearestDescNode.isInline) {
          a: {
            nearestDescNode = domSel.focusNode;
            var offset = domSel.focusOffset;
            domSel = nearestDesc.dom;
            JSCompiler_temp = 0 == offset;
            for (offset = offset == nodeSize(nearestDescNode); JSCompiler_temp || offset;) {
              if (nearestDescNode == domSel) {
                nearestDescNode = !0;
                break a;
              }
              var index = domIndex(nearestDescNode);
              nearestDescNode = nearestDescNode.parentNode;
              if (!nearestDescNode) {
                nearestDescNode = !1;
                break a;
              }
              JSCompiler_temp = JSCompiler_temp && 0 == index;
              offset = offset && index == nodeSize(nearestDescNode);
            }
            nearestDescNode = void 0;
          }
        }
        JSCompiler_temp = !nearestDescNode;
      }
      if (JSCompiler_temp) {
        var selection = nearestDesc.posBefore;
        selection = new prosemirrorState.NodeSelection(head == selection ? $head : doc.resolve(selection));
      }
    } else {
      if (domSel instanceof view.dom.ownerDocument.defaultView.Selection && 1 < domSel.rangeCount) {
        $head = head;
        for (anchor = 0; anchor < domSel.rangeCount; anchor++) {
          nearestDesc = domSel.getRangeAt(anchor), $head = Math.min($head, view.docView.posFromDOM(nearestDesc.startContainer, nearestDesc.startOffset, 1)), head = Math.max(head, view.docView.posFromDOM(nearestDesc.endContainer, nearestDesc.endOffset, -1));
        }
        if (0 > $head) {
          return null;
        }
        head = _slicedToArray(head == view.state.selection.anchor ? [head, $head] : [$head, head], 2);
        anchor = head[0];
        head = head[1];
        $head = doc.resolve(head);
      } else {
        anchor = view.docView.posFromDOM(domSel.anchorNode, domSel.anchorOffset, 1);
      }
      if (0 > anchor) {
        return null;
      }
    }
    doc = doc.resolve(anchor);
    selection || (selection = selectionBetween(view, doc, $head, "pointer" == origin || view.state.selection.head < $head.pos && !inWidget ? 1 : -1));
    return selection;
  }
  function editorOwnsSelection(view) {
    return view.editable ? view.hasFocus() : hasSelection(view) && document.activeElement && document.activeElement.contains(view.dom);
  }
  function selectionToDOM(view) {
    var force = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : !1, sel = view.state.selection;
    syncNodeSelection(view, sel);
    if (editorOwnsSelection(view)) {
      if (!force && view.input.mouseDown && view.input.mouseDown.allowDefault && chrome) {
        var domSel = view.domSelectionRange(), curSel = view.domObserver.currentSelection;
        if (domSel.anchorNode && curSel.anchorNode && isEquivalentPosition(domSel.anchorNode, domSel.anchorOffset, curSel.anchorNode, curSel.anchorOffset)) {
          view.input.mouseDown.delayedSelectionSync = !0;
          view.domObserver.setCurSelection();
          return;
        }
      }
      view.domObserver.disconnectSelection();
      if (view.cursorWrapper) {
        if (force = view.domSelection(), sel = document.createRange(), force) {
          var node = view.cursorWrapper.dom, img = "IMG" == node.nodeName;
          img ? sel.setStart(node.parentNode, domIndex(node) + 1) : sel.setStart(node, 0);
          sel.collapse(!0);
          force.removeAllRanges();
          force.addRange(sel);
          !img && !view.state.selection.visible && ie && 11 >= ie_version && (node.disabled = !0, node.disabled = !1);
        }
      } else {
        domSel = sel.anchor, curSel = sel.head, !brokenSelectBetweenUneditable || sel instanceof prosemirrorState.TextSelection || (sel.$from.parent.inlineContent || (node = temporarilyEditableNear(view, sel.from)), sel.empty || sel.$from.parent.inlineContent || (img = temporarilyEditableNear(view, sel.to))), view.docView.setSelection(domSel, curSel, view.root, force), brokenSelectBetweenUneditable && (node && resetEditable(node), img && resetEditable(img)), sel.visible ? view.dom.classList.remove("ProseMirror-hideselection") : 
        (view.dom.classList.add("ProseMirror-hideselection"), "onselectionchange" in document && removeClassOnSelectionChange(view));
      }
      view.domObserver.setCurSelection();
      view.domObserver.connectSelection();
    }
  }
  function temporarilyEditableNear(view, pos) {
    pos = view.docView.domFromPos(pos, 0);
    view = pos.node;
    var offset = pos.offset;
    pos = offset < view.childNodes.length ? view.childNodes[offset] : null;
    view = offset ? view.childNodes[offset - 1] : null;
    if (safari && pos && "false" == pos.contentEditable) {
      return setEditable(pos);
    }
    if (!(pos && "false" != pos.contentEditable || view && "false" != view.contentEditable)) {
      if (pos) {
        return setEditable(pos);
      }
      if (view) {
        return setEditable(view);
      }
    }
  }
  function setEditable(element) {
    element.contentEditable = "true";
    safari && element.draggable && (element.draggable = !1, element.wasDraggable = !0);
    return element;
  }
  function resetEditable(element) {
    element.contentEditable = "false";
    element.wasDraggable && (element.draggable = !0, element.wasDraggable = null);
  }
  function removeClassOnSelectionChange(view) {
    var doc = view.dom.ownerDocument;
    doc.removeEventListener("selectionchange", view.input.hideSelectionGuard);
    var domSel = view.domSelectionRange(), node = domSel.anchorNode, offset = domSel.anchorOffset;
    doc.addEventListener("selectionchange", view.input.hideSelectionGuard = function() {
      if (domSel.anchorNode != node || domSel.anchorOffset != offset) {
        doc.removeEventListener("selectionchange", view.input.hideSelectionGuard), setTimeout(function() {
          editorOwnsSelection(view) && !view.state.selection.visible || view.dom.classList.remove("ProseMirror-hideselection");
        }, 20);
      }
    });
  }
  function syncNodeSelection(view, sel) {
    sel instanceof prosemirrorState.NodeSelection ? (sel = view.docView.descAt(sel.from), sel != view.lastSelectedViewDesc && (clearNodeSelection(view), sel && sel.selectNode(), view.lastSelectedViewDesc = sel)) : clearNodeSelection(view);
  }
  function clearNodeSelection(view) {
    view.lastSelectedViewDesc && (view.lastSelectedViewDesc.parent && view.lastSelectedViewDesc.deselectNode(), view.lastSelectedViewDesc = void 0);
  }
  function selectionBetween(view, $anchor, $head, bias) {
    return view.someProp("createSelectionBetween", function(f) {
      return f(view, $anchor, $head);
    }) || prosemirrorState.TextSelection.between($anchor, $head, bias);
  }
  function hasFocusAndSelection(view) {
    return view.editable && !view.hasFocus() ? !1 : hasSelection(view);
  }
  function hasSelection(view) {
    var sel = view.domSelectionRange();
    if (!sel.anchorNode) {
      return !1;
    }
    try {
      return view.dom.contains(3 == sel.anchorNode.nodeType ? sel.anchorNode.parentNode : sel.anchorNode) && (view.editable || view.dom.contains(3 == sel.focusNode.nodeType ? sel.focusNode.parentNode : sel.focusNode));
    } catch (_) {
      return !1;
    }
  }
  function moveSelectionBlock(state, dir) {
    var _state$selection = state.selection, $anchor = _state$selection.$anchor;
    _state$selection = _state$selection.$head;
    $anchor = 0 < dir ? $anchor.max(_state$selection) : $anchor.min(_state$selection);
    return (state = $anchor.parent.inlineContent ? $anchor.depth ? state.doc.resolve(0 < dir ? $anchor.after() : $anchor.before()) : null : $anchor) && prosemirrorState.Selection.findFrom(state, dir);
  }
  function apply(view, sel) {
    view.dispatch(view.state.tr.setSelection(sel).scrollIntoView());
    return !0;
  }
  function selectHorizontally(view, dir, mods) {
    var sel = view.state.selection;
    if (sel instanceof prosemirrorState.TextSelection) {
      if (-1 < mods.indexOf("s")) {
        var $head = sel.$head;
        mods = $head.textOffset ? null : 0 > dir ? $head.nodeBefore : $head.nodeAfter;
        if (!mods || mods.isText || !mods.isLeaf) {
          return !1;
        }
        dir = view.state.doc.resolve($head.pos + mods.nodeSize * (0 > dir ? -1 : 1));
        return apply(view, new prosemirrorState.TextSelection(sel.$anchor, dir));
      }
      if (sel.empty) {
        if (view.endOfTextblock(0 < dir ? "forward" : "backward")) {
          return (dir = moveSelectionBlock(view.state, dir)) && dir instanceof prosemirrorState.NodeSelection ? apply(view, dir) : !1;
        }
        if (!(mac && -1 < mods.indexOf("m"))) {
          sel = sel.$head;
          mods = sel.textOffset ? null : 0 > dir ? sel.nodeBefore : sel.nodeAfter;
          if (!mods || mods.isText) {
            return !1;
          }
          var nodePos = 0 > dir ? sel.pos - mods.nodeSize : sel.pos;
          return mods.isAtom || ($head = view.docView.descAt(nodePos)) && !$head.contentDOM ? prosemirrorState.NodeSelection.isSelectable(mods) ? apply(view, new prosemirrorState.NodeSelection(0 > dir ? view.state.doc.resolve(sel.pos - mods.nodeSize) : sel)) : webkit ? apply(view, new prosemirrorState.TextSelection(view.state.doc.resolve(0 > dir ? nodePos : nodePos + mods.nodeSize))) : !1 : !1;
        }
      } else {
        return !1;
      }
    } else {
      return sel instanceof prosemirrorState.NodeSelection && sel.node.isInline ? apply(view, new prosemirrorState.TextSelection(0 < dir ? sel.$to : sel.$from)) : (dir = moveSelectionBlock(view.state, dir)) ? apply(view, dir) : !1;
    }
  }
  function nodeLen(node) {
    return 3 == node.nodeType ? node.nodeValue.length : node.childNodes.length;
  }
  function isIgnorable(dom, dir) {
    var desc = dom.pmViewDesc;
    return desc && 0 == desc.size && (0 > dir || dom.nextSibling || "BR" != dom.nodeName);
  }
  function skipIgnoredNodes(view, dir) {
    if (0 > dir) {
      var sel = view.domSelectionRange(), node = sel.focusNode;
      sel = sel.focusOffset;
      if (node) {
        dir = !1;
        for (gecko && 1 == node.nodeType && sel < nodeLen(node) && isIgnorable(node.childNodes[sel], -1) && (dir = !0);;) {
          if (0 < sel) {
            if (1 != node.nodeType) {
              break;
            } else {
              var before = node.childNodes[sel - 1];
              if (isIgnorable(before, -1)) {
                var moveNode = node;
                var moveOffset = --sel;
              } else if (3 == before.nodeType) {
                node = before, sel = node.nodeValue.length;
              } else {
                break;
              }
            }
          } else if (isBlockNode(node)) {
            break;
          } else {
            for (before = node.previousSibling; before && isIgnorable(before, -1);) {
              moveNode = node.parentNode, moveOffset = domIndex(before), before = before.previousSibling;
            }
            if (before) {
              node = before, sel = nodeLen(node);
            } else {
              node = node.parentNode;
              if (node == view.dom) {
                break;
              }
              sel = 0;
            }
          }
        }
        dir ? setSelFocus(view, node, sel) : moveNode && setSelFocus(view, moveNode, moveOffset);
      }
      view = void 0;
    } else {
      moveOffset = view.domSelectionRange();
      moveNode = moveOffset.focusNode;
      moveOffset = moveOffset.focusOffset;
      if (moveNode) {
        for (dir = nodeLen(moveNode);;) {
          if (moveOffset < dir) {
            if (1 != moveNode.nodeType) {
              break;
            }
            if (isIgnorable(moveNode.childNodes[moveOffset], 1)) {
              node = moveNode, sel = ++moveOffset;
            } else {
              break;
            }
          } else if (isBlockNode(moveNode)) {
            break;
          } else {
            for (moveOffset = moveNode.nextSibling; moveOffset && isIgnorable(moveOffset, 1);) {
              node = moveOffset.parentNode, sel = domIndex(moveOffset) + 1, moveOffset = moveOffset.nextSibling;
            }
            if (moveOffset) {
              moveNode = moveOffset, moveOffset = 0, dir = nodeLen(moveNode);
            } else {
              moveNode = moveNode.parentNode;
              if (moveNode == view.dom) {
                break;
              }
              moveOffset = dir = 0;
            }
          }
        }
        node && setSelFocus(view, node, sel);
      }
      view = void 0;
    }
    return view;
  }
  function isBlockNode(dom) {
    return (dom = dom.pmViewDesc) && dom.node && dom.node.isBlock;
  }
  function textNodeAfter(node, offset) {
    for (; node && offset == node.childNodes.length && !hasBlockDesc(node);) {
      offset = domIndex(node) + 1, node = node.parentNode;
    }
    for (; node && offset < node.childNodes.length;) {
      node = node.childNodes[offset];
      if (3 == node.nodeType) {
        return node;
      }
      if (1 == node.nodeType && "false" == node.contentEditable) {
        break;
      }
      offset = 0;
    }
  }
  function textNodeBefore(node, offset) {
    for (; node && !offset && !hasBlockDesc(node);) {
      offset = domIndex(node), node = node.parentNode;
    }
    for (; node && offset;) {
      node = node.childNodes[offset - 1];
      if (3 == node.nodeType) {
        return node;
      }
      if (1 == node.nodeType && "false" == node.contentEditable) {
        break;
      }
      offset = node.childNodes.length;
    }
  }
  function setSelFocus(view, node, offset) {
    if (3 != node.nodeType) {
      var before;
      if (before = textNodeAfter(node, offset)) {
        node = before, offset = 0;
      } else if (before = textNodeBefore(node, offset)) {
        node = before, offset = before.nodeValue.length;
      }
    }
    if (before = view.domSelection()) {
      if (selectionCollapsed(before)) {
        var range = document.createRange();
        range.setEnd(node, offset);
        range.setStart(node, offset);
        before.removeAllRanges();
        before.addRange(range);
      } else {
        before.extend && before.extend(node, offset);
      }
      view.domObserver.setCurSelection();
      var state = view.state;
      setTimeout(function() {
        view.state == state && selectionToDOM(view);
      }, 50);
    }
  }
  function findDirection(view, pos) {
    var $pos = view.state.doc.resolve(pos);
    if (!chrome && !windows && $pos.parent.inlineContent) {
      var coords = view.coordsAtPos(pos);
      if (pos > $pos.start()) {
        var before = view.coordsAtPos(pos - 1), mid = (before.top + before.bottom) / 2;
        if (mid > coords.top && mid < coords.bottom && 1 < Math.abs(before.left - coords.left)) {
          return before.left < coords.left ? "ltr" : "rtl";
        }
      }
      if (pos < $pos.end() && (pos = view.coordsAtPos(pos + 1), $pos = (pos.top + pos.bottom) / 2, $pos > coords.top && $pos < coords.bottom && 1 < Math.abs(pos.left - coords.left))) {
        return pos.left > coords.left ? "ltr" : "rtl";
      }
    }
    return "rtl" == getComputedStyle(view.dom).direction ? "rtl" : "ltr";
  }
  function selectVertically(view, dir, mods) {
    var sel = view.state.selection;
    if (sel instanceof prosemirrorState.TextSelection && !sel.empty || -1 < mods.indexOf("s") || mac && -1 < mods.indexOf("m")) {
      return !1;
    }
    mods = sel.$from;
    var $to = sel.$to;
    if (!mods.parent.inlineContent || view.endOfTextblock(0 > dir ? "up" : "down")) {
      var next = moveSelectionBlock(view.state, dir);
      if (next && next instanceof prosemirrorState.NodeSelection) {
        return apply(view, next);
      }
    }
    return mods.parent.inlineContent ? !1 : (mods = 0 > dir ? mods : $to, (dir = sel instanceof prosemirrorState.AllSelection ? prosemirrorState.Selection.near(mods, dir) : prosemirrorState.Selection.findFrom(mods, dir)) ? apply(view, dir) : !1);
  }
  function stopNativeHorizontalDelete(view, dir) {
    if (!(view.state.selection instanceof prosemirrorState.TextSelection)) {
      return !0;
    }
    var _view$state$selection2 = view.state.selection, $head = _view$state$selection2.$head, empty = _view$state$selection2.empty;
    if (!$head.sameParent(_view$state$selection2.$anchor)) {
      return !0;
    }
    if (!empty) {
      return !1;
    }
    if (view.endOfTextblock(0 < dir ? "forward" : "backward")) {
      return !0;
    }
    if ((_view$state$selection2 = !$head.textOffset && (0 > dir ? $head.nodeBefore : $head.nodeAfter)) && !_view$state$selection2.isText) {
      empty = view.state.tr;
      if (0 > dir) {
        empty["delete"]($head.pos - _view$state$selection2.nodeSize, $head.pos);
      } else {
        empty["delete"]($head.pos, $head.pos + _view$state$selection2.nodeSize);
      }
      view.dispatch(empty);
      return !0;
    }
    return !1;
  }
  function switchEditable(view, node, state) {
    view.domObserver.stop();
    node.contentEditable = state;
    view.domObserver.start();
  }
  function safariDownArrowBug(view) {
    if (!safari || 0 < view.state.selection.$head.parentOffset) {
      return !1;
    }
    var _view$domSelectionRan3 = view.domSelectionRange(), focusNode = _view$domSelectionRan3.focusNode;
    _view$domSelectionRan3 = _view$domSelectionRan3.focusOffset;
    if (focusNode && 1 == focusNode.nodeType && 0 == _view$domSelectionRan3 && focusNode.firstChild && "false" == focusNode.firstChild.contentEditable) {
      var child = focusNode.firstChild;
      switchEditable(view, child, "true");
      setTimeout(function() {
        return switchEditable(view, child, "false");
      }, 20);
    }
    return !1;
  }
  function captureKeyDown(view, event) {
    var code = event.keyCode;
    var mods = "";
    event.ctrlKey && (mods += "c");
    event.metaKey && (mods += "m");
    event.altKey && (mods += "a");
    event.shiftKey && (mods += "s");
    return 8 == code || mac && 72 == code && "c" == mods ? stopNativeHorizontalDelete(view, -1) || skipIgnoredNodes(view, -1) : 46 == code && !event.shiftKey || mac && 68 == code && "c" == mods ? stopNativeHorizontalDelete(view, 1) || skipIgnoredNodes(view, 1) : 13 == code || 27 == code ? !0 : 37 == code || mac && 66 == code && "c" == mods ? (event = 37 == code ? "ltr" == findDirection(view, view.state.selection.from) ? -1 : 1 : -1, selectHorizontally(view, event, mods) || skipIgnoredNodes(view, 
    event)) : 39 == code || mac && 70 == code && "c" == mods ? (event = 39 == code ? "ltr" == findDirection(view, view.state.selection.from) ? 1 : -1 : 1, selectHorizontally(view, event, mods) || skipIgnoredNodes(view, event)) : 38 == code || mac && 80 == code && "c" == mods ? selectVertically(view, -1, mods) || skipIgnoredNodes(view, -1) : 40 == code || mac && 78 == code && "c" == mods ? safariDownArrowBug(view) || selectVertically(view, 1, mods) || skipIgnoredNodes(view, 1) : mods != (mac ? "m" : 
    "c") || 66 != code && 73 != code && 89 != code && 90 != code ? !1 : !0;
  }
  function serializeForClipboard(view, slice) {
    view.someProp("transformCopied", function(f) {
      slice = f(slice, view);
    });
    var context = [], _slice = slice, content = _slice.content, openStart = _slice.openStart;
    for (_slice = _slice.openEnd; 1 < openStart && 1 < _slice && 1 == content.childCount && 1 == content.firstChild.childCount;) {
      openStart--;
      _slice--;
      var node = content.firstChild;
      context.push(node.type.name, node.attrs != node.type.defaultAttrs ? node.attrs : null);
      content = node.content;
    }
    var serializer = view.someProp("clipboardSerializer") || prosemirrorModel.DOMSerializer.fromSchema(view.state.schema), doc = _detachedDoc || (_detachedDoc = document.implementation.createHTMLDocument("title"));
    node = doc.createElement("div");
    node.appendChild(serializer.serializeFragment(content, {document:doc}));
    serializer = node.firstChild;
    var needsWrap;
    for (content = 0; serializer && 1 == serializer.nodeType && (needsWrap = wrapMap[serializer.nodeName.toLowerCase()]);) {
      for (serializer = needsWrap.length - 1; 0 <= serializer; serializer--) {
        for (var wrapper = doc.createElement(needsWrap[serializer]); node.firstChild;) {
          wrapper.appendChild(node.firstChild);
        }
        node.appendChild(wrapper);
        content++;
      }
      serializer = node.firstChild;
    }
    serializer && 1 == serializer.nodeType && serializer.setAttribute("data-pm-slice", "".concat(openStart, " ").concat(_slice).concat(content ? " -".concat(content) : "", " ").concat(JSON.stringify(context)));
    context = view.someProp("clipboardTextSerializer", function(f) {
      return f(slice, view);
    }) || slice.content.textBetween(0, slice.content.size, "\n\n");
    return {dom:node, text:context, slice};
  }
  function parseFromClipboard(view, text, html, plainText, $context) {
    var inCode = $context.parent.type.spec.code;
    if (!html && !text) {
      return null;
    }
    var asText = text && (plainText || inCode || !html);
    if (asText) {
      view.someProp("transformPastedText", function(f) {
        text = f(text, inCode || plainText, view);
      });
      if (inCode) {
        return text ? new prosemirrorModel.Slice(prosemirrorModel.Fragment.from(view.state.schema.text(text.replace(/\r\n?/g, "\n"))), 0, 0) : prosemirrorModel.Slice.empty;
      }
      var parsed = view.someProp("clipboardTextParser", function(f) {
        return f(text, $context, plainText, view);
      });
      if (parsed) {
        var slice = parsed;
      } else {
        var marks = $context.marks(), schema = view.state.schema, serializer = prosemirrorModel.DOMSerializer.fromSchema(schema);
        var dom = document.createElement("div");
        text.split(/(?:\r\n?|\n)+/).forEach(function(block) {
          var p = dom.appendChild(document.createElement("p"));
          block && p.appendChild(serializer.serializeNode(schema.text(block, marks)));
        });
      }
    } else {
      view.someProp("transformPastedHTML", function(f) {
        html = f(html, view);
      }), dom = readHTML(html), webkit && restoreReplacedSpaces(dom);
    }
    if ((parsed = (parsed = dom && dom.querySelector("[data-pm-slice]")) && /^(\d+) (\d+)(?: -(\d+))? (.*)/.exec(parsed.getAttribute("data-pm-slice") || "")) && parsed[3]) {
      for (var i = +parsed[3]; 0 < i; i--) {
        for (var child = dom.firstChild; child && 1 != child.nodeType;) {
          child = child.nextSibling;
        }
        if (!child) {
          break;
        }
        dom = child;
      }
    }
    slice || (slice = (view.someProp("clipboardParser") || view.someProp("domParser") || prosemirrorModel.DOMParser.fromSchema(view.state.schema)).parseSlice(dom, {preserveWhitespace:!(!asText && !parsed), context:$context, ruleFromNode:function(dom) {
      return "BR" != dom.nodeName || dom.nextSibling || !dom.parentNode || inlineParents.test(dom.parentNode.nodeName) ? null : {ignore:!0};
    }}));
    if (parsed) {
      slice = addContext(closeSlice(slice, +parsed[1], +parsed[2]), parsed[4]);
    } else {
      if (slice = prosemirrorModel.Slice.maxOpen(normalizeSiblings(slice.content, $context), !0), slice.openStart || slice.openEnd) {
        parsed = asText = 0;
        for (i = slice.content.firstChild; asText < slice.openStart && !i.type.spec.isolating; asText++, i = i.firstChild) {
        }
        for (i = slice.content.lastChild; parsed < slice.openEnd && !i.type.spec.isolating; parsed++, i = i.lastChild) {
        }
        slice = closeSlice(slice, asText, parsed);
      }
    }
    view.someProp("transformPasted", function(f) {
      slice = f(slice, view);
    });
    return slice;
  }
  function normalizeSiblings(fragment, $context) {
    if (2 > fragment.childCount) {
      return fragment;
    }
    for (var _loop = function() {
      var match = $context.node(d).contentMatchAt($context.index(d)), lastWrap, result = [];
      fragment.forEach(function(node) {
        if (result) {
          var wrap = match.findWrapping(node.type), inLast;
          if (!wrap) {
            return result = null;
          }
          (inLast = result.length && lastWrap.length && addToSibling(wrap, lastWrap, node, result[result.length - 1], 0)) ? result[result.length - 1] = inLast : (result.length && (result[result.length - 1] = closeRight(result[result.length - 1], lastWrap.length)), node = withWrappers(node, wrap), result.push(node), match = match.matchType(node.type), lastWrap = wrap);
        }
      });
      if (result) {
        return {v:prosemirrorModel.Fragment.from(result)};
      }
    }, _ret, d = $context.depth; 0 <= d; d--) {
      if (_ret = _loop()) {
        return _ret.v;
      }
    }
    return fragment;
  }
  function withWrappers(node, wrap) {
    for (var from = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : 0, i = wrap.length - 1; i >= from; i--) {
      node = wrap[i].create(null, prosemirrorModel.Fragment.from(node));
    }
    return node;
  }
  function addToSibling(wrap, lastWrap, node, sibling, depth) {
    if (depth < wrap.length && depth < lastWrap.length && wrap[depth] == lastWrap[depth]) {
      if (lastWrap = addToSibling(wrap, lastWrap, node, sibling.lastChild, depth + 1)) {
        return sibling.copy(sibling.content.replaceChild(sibling.childCount - 1, lastWrap));
      }
      if (sibling.contentMatchAt(sibling.childCount).matchType(depth == wrap.length - 1 ? node.type : wrap[depth + 1])) {
        return sibling.copy(sibling.content.append(prosemirrorModel.Fragment.from(withWrappers(node, wrap, depth + 1))));
      }
    }
  }
  function closeRight(node, depth) {
    if (0 == depth) {
      return node;
    }
    depth = node.content.replaceChild(node.childCount - 1, closeRight(node.lastChild, depth - 1));
    var fill = node.contentMatchAt(node.childCount).fillBefore(prosemirrorModel.Fragment.empty, !0);
    return node.copy(depth.append(fill));
  }
  function closeRange(fragment, side, from, to, depth, openEnd) {
    var node = 0 > side ? fragment.firstChild : fragment.lastChild, inner = node.content;
    1 < fragment.childCount && (openEnd = 0);
    depth < to - 1 && (inner = closeRange(inner, side, from, to, depth + 1, openEnd));
    depth >= from && (inner = 0 > side ? node.contentMatchAt(0).fillBefore(inner, openEnd <= depth).append(inner) : inner.append(node.contentMatchAt(node.childCount).fillBefore(prosemirrorModel.Fragment.empty, !0)));
    return fragment.replaceChild(0 > side ? 0 : fragment.childCount - 1, node.copy(inner));
  }
  function closeSlice(slice, openStart, openEnd) {
    openStart < slice.openStart && (slice = new prosemirrorModel.Slice(closeRange(slice.content, -1, openStart, slice.openStart, 0, slice.openEnd), openStart, slice.openEnd));
    openEnd < slice.openEnd && (slice = new prosemirrorModel.Slice(closeRange(slice.content, 1, openEnd, slice.openEnd, 0, 0), slice.openStart, openEnd));
    return slice;
  }
  function maybeWrapTrusted(html) {
    var trustedTypes = window.trustedTypes;
    if (!trustedTypes) {
      return html;
    }
    _policy || (_policy = trustedTypes.createPolicy("ProseMirrorClipboard", {createHTML:function(s) {
      return s;
    }}));
    return _policy.createHTML(html);
  }
  function readHTML(html) {
    var metas = /^(\s*<meta [^>]*>)*/.exec(html);
    metas && (html = html.slice(metas[0].length));
    metas = (_detachedDoc || (_detachedDoc = document.implementation.createHTMLDocument("title"))).createElement("div");
    var firstTag = /<([a-z][^>\s]+)/i.exec(html);
    if (firstTag = firstTag && wrapMap[firstTag[1].toLowerCase()]) {
      html = firstTag.map(function(n) {
        return "\x3c" + n + "\x3e";
      }).join("") + html + firstTag.map(function(n) {
        return "\x3c/" + n + "\x3e";
      }).reverse().join("");
    }
    metas.innerHTML = maybeWrapTrusted(html);
    if (firstTag) {
      for (html = 0; html < firstTag.length; html++) {
        metas = metas.querySelector(firstTag[html]) || metas;
      }
    }
    return metas;
  }
  function restoreReplacedSpaces(dom) {
    for (var nodes = dom.querySelectorAll(chrome ? "span:not([class]):not([style])" : "span.Apple-converted-space"), i = 0; i < nodes.length; i++) {
      var node = nodes[i];
      1 == node.childNodes.length && " " == node.textContent && node.parentNode && node.parentNode.replaceChild(dom.ownerDocument.createTextNode(" "), node);
    }
  }
  function addContext(slice, context) {
    if (!slice.size) {
      return slice;
    }
    var schema = slice.content.firstChild.type.schema;
    try {
      var array = JSON.parse(context);
    } catch (e) {
      return slice;
    }
    context = slice.content;
    var openStart = slice.openStart;
    slice = slice.openEnd;
    for (var i = array.length - 2; 0 <= i; i -= 2) {
      var type = schema.nodes[array[i]];
      if (!type || type.hasRequiredAttrs()) {
        break;
      }
      context = prosemirrorModel.Fragment.from(type.create(array[i + 1], context));
      openStart++;
      slice++;
    }
    return new prosemirrorModel.Slice(context, openStart, slice);
  }
  function initInput(view) {
    var _loop2 = function() {
      var handler = handlers[event];
      view.dom.addEventListener(event, view.input.eventHandlers[event] = function(event) {
        !eventBelongsToView(view, event) || runCustomHandler(view, event) || !view.editable && event.type in editHandlers || handler(view, event);
      }, passiveHandlers[event] ? {passive:!0} : void 0);
    }, event;
    for (event in handlers) {
      _loop2();
    }
    safari && view.dom.addEventListener("input", function() {
      return null;
    });
    ensureListeners(view);
  }
  function setSelectionOrigin(view, origin) {
    view.input.lastSelectionOrigin = origin;
    view.input.lastSelectionTime = Date.now();
  }
  function ensureListeners(view) {
    view.someProp("handleDOMEvents", function(currentHandlers) {
      for (var type in currentHandlers) {
        view.input.eventHandlers[type] || view.dom.addEventListener(type, view.input.eventHandlers[type] = function(event) {
          return runCustomHandler(view, event);
        });
      }
    });
  }
  function runCustomHandler(view, event) {
    return view.someProp("handleDOMEvents", function(handlers) {
      return (handlers = handlers[event.type]) ? handlers(view, event) || event.defaultPrevented : !1;
    });
  }
  function eventBelongsToView(view, event) {
    if (!event.bubbles) {
      return !0;
    }
    if (event.defaultPrevented) {
      return !1;
    }
    for (var node = event.target; node != view.dom; node = node.parentNode) {
      if (!node || 11 == node.nodeType || node.pmViewDesc && node.pmViewDesc.stopEvent(event)) {
        return !1;
      }
    }
    return !0;
  }
  function eventCoords(event) {
    return {left:event.clientX, top:event.clientY};
  }
  function runHandlerOnContext(view, propName, pos, inside, event) {
    if (-1 == inside) {
      return !1;
    }
    var $pos = view.state.doc.resolve(inside);
    inside = function(i) {
      if (view.someProp(propName, function(f) {
        return i > $pos.depth ? f(view, pos, $pos.nodeAfter, $pos.before(i), event, !0) : f(view, pos, $pos.node(i), $pos.before(i), event, !1);
      })) {
        return {v:!0};
      }
    };
    for (var _ret2, i = $pos.depth + 1; 0 < i; i--) {
      if (_ret2 = inside(i)) {
        return _ret2.v;
      }
    }
    return !1;
  }
  function updateSelection(view, selection, origin) {
    view.focused || view.focus();
    view.state.selection.eq(selection) || (selection = view.state.tr.setSelection(selection), "pointer" == origin && selection.setMeta("pointer", !0), view.dispatch(selection));
  }
  function selectClickedLeaf(view, inside) {
    if (-1 == inside) {
      return !1;
    }
    inside = view.state.doc.resolve(inside);
    var node = inside.nodeAfter;
    return node && node.isAtom && prosemirrorState.NodeSelection.isSelectable(node) ? (updateSelection(view, new prosemirrorState.NodeSelection(inside), "pointer"), !0) : !1;
  }
  function selectClickedNode(view, inside) {
    if (-1 == inside) {
      return !1;
    }
    var sel = view.state.selection;
    if (sel instanceof prosemirrorState.NodeSelection) {
      var selectedNode = sel.node;
    }
    inside = view.state.doc.resolve(inside);
    for (var i = inside.depth + 1; 0 < i; i--) {
      var node = i > inside.depth ? inside.nodeAfter : inside.node(i);
      if (prosemirrorState.NodeSelection.isSelectable(node)) {
        var selectAt = selectedNode && 0 < sel.$from.depth && i >= sel.$from.depth && inside.before(sel.$from.depth + 1) == sel.$from.pos ? inside.before(sel.$from.depth) : inside.before(i);
        break;
      }
    }
    return null != selectAt ? (updateSelection(view, prosemirrorState.NodeSelection.create(view.state.doc, selectAt), "pointer"), !0) : !1;
  }
  function handleSingleClick(view, pos, inside, event, selectNode) {
    return runHandlerOnContext(view, "handleClickOn", pos, inside, event) || view.someProp("handleClick", function(f) {
      return f(view, pos, event);
    }) || (selectNode ? selectClickedNode(view, inside) : selectClickedLeaf(view, inside));
  }
  function handleDoubleClick(view, pos, inside, event) {
    return runHandlerOnContext(view, "handleDoubleClickOn", pos, inside, event) || view.someProp("handleDoubleClick", function(f) {
      return f(view, pos, event);
    });
  }
  function handleTripleClick(view, pos, inside, event) {
    return runHandlerOnContext(view, "handleTripleClickOn", pos, inside, event) || view.someProp("handleTripleClick", function(f) {
      return f(view, pos, event);
    }) || defaultTripleClick(view, inside, event);
  }
  function defaultTripleClick(view, inside, event) {
    if (0 != event.button) {
      return !1;
    }
    event = view.state.doc;
    if (-1 == inside) {
      return event.inlineContent ? (updateSelection(view, prosemirrorState.TextSelection.create(event, 0, event.content.size), "pointer"), !0) : !1;
    }
    inside = event.resolve(inside);
    for (var i = inside.depth + 1; 0 < i; i--) {
      var node = i > inside.depth ? inside.nodeAfter : inside.node(i), nodePos = inside.before(i);
      if (node.inlineContent) {
        updateSelection(view, prosemirrorState.TextSelection.create(event, nodePos + 1, nodePos + 1 + node.content.size), "pointer");
      } else if (prosemirrorState.NodeSelection.isSelectable(node)) {
        updateSelection(view, prosemirrorState.NodeSelection.create(event, nodePos), "pointer");
      } else {
        continue;
      }
      return !0;
    }
  }
  function inOrNearComposition(view, event) {
    return view.composing ? !0 : safari && 500 > Math.abs(event.timeStamp - view.input.compositionEndedAt) ? (view.input.compositionEndedAt = -2e8, !0) : !1;
  }
  function scheduleComposeEnd(view, delay) {
    clearTimeout(view.input.composingTimeout);
    -1 < delay && (view.input.composingTimeout = setTimeout(function() {
      return endComposition(view);
    }, delay));
  }
  function clearComposition(view) {
    if (view.composing) {
      view.input.composing = !1;
      var JSCompiler_temp_const = view.input, event = document.createEvent("Event");
      event.initEvent("event", !0, !0);
      JSCompiler_temp_const.compositionEndedAt = event.timeStamp;
    }
    for (; 0 < view.input.compositionNodes.length;) {
      view.input.compositionNodes.pop().markParentsDirty();
    }
  }
  function findCompositionNode(view) {
    var sel = view.domSelectionRange();
    if (!sel.focusNode) {
      return null;
    }
    var textBefore = textNodeBefore$1(sel.focusNode, sel.focusOffset);
    sel = textNodeAfter$1(sel.focusNode, sel.focusOffset);
    if (textBefore && sel && textBefore != sel) {
      var descAfter = sel.pmViewDesc, lastChanged = view.domObserver.lastChangedTextNode;
      if (textBefore == lastChanged || sel == lastChanged) {
        return lastChanged;
      }
      if (!descAfter || !descAfter.isText(sel.nodeValue) || view.input.compositionNode == sel && (view = textBefore.pmViewDesc) && view.isText(textBefore.nodeValue)) {
        return sel;
      }
    }
    return textBefore || sel;
  }
  function endComposition(view) {
    var restarting = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : !1;
    if (!(android && 0 <= view.domObserver.flushingSoon)) {
      view.domObserver.forceFlush();
      clearComposition(view);
      if (restarting || view.docView && view.docView.dirty) {
        var sel = selectionFromDOM(view);
        sel && !sel.eq(view.state.selection) ? view.dispatch(view.state.tr.setSelection(sel)) : !view.markCursor && !restarting || view.state.selection.empty ? view.updateState(view.state) : view.dispatch(view.state.tr.deleteSelection());
        return !0;
      }
      return !1;
    }
  }
  function captureCopy(view, dom) {
    if (view.dom.parentNode) {
      var wrap = view.dom.parentNode.appendChild(document.createElement("div"));
      wrap.appendChild(dom);
      wrap.style.cssText = "position: fixed; left: -10000px; top: 10px";
      var sel = getSelection(), range = document.createRange();
      range.selectNodeContents(dom);
      view.dom.blur();
      sel.removeAllRanges();
      sel.addRange(range);
      setTimeout(function() {
        wrap.parentNode && wrap.parentNode.removeChild(wrap);
        view.focus();
      }, 50);
    }
  }
  function capturePaste(view, event) {
    if (view.dom.parentNode) {
      var plainText = view.input.shiftKey || view.state.selection.$from.parent.type.spec.code, target = view.dom.parentNode.appendChild(document.createElement(plainText ? "textarea" : "div"));
      plainText || (target.contentEditable = "true");
      target.style.cssText = "position: fixed; left: -10000px; top: 10px";
      target.focus();
      var plain = view.input.shiftKey && 45 != view.input.lastKeyCode;
      setTimeout(function() {
        view.focus();
        target.parentNode && target.parentNode.removeChild(target);
        plainText ? doPaste(view, target.value, null, plain, event) : doPaste(view, target.textContent, target.innerHTML, plain, event);
      }, 50);
    }
  }
  function doPaste(view, text, html, preferPlain, event) {
    var slice = parseFromClipboard(view, text, html, preferPlain, view.state.selection.$from);
    if (view.someProp("handlePaste", function(f) {
      return f(view, event, slice || prosemirrorModel.Slice.empty);
    })) {
      return !0;
    }
    if (!slice) {
      return !1;
    }
    preferPlain = (text = 0 == slice.openStart && 0 == slice.openEnd && 1 == slice.content.childCount ? slice.content.firstChild : null) ? view.state.tr.replaceSelectionWith(text, preferPlain) : view.state.tr.replaceSelection(slice);
    view.dispatch(preferPlain.scrollIntoView().setMeta("paste", !0).setMeta("uiEvent", "paste"));
    return !0;
  }
  function getText(clipboardData) {
    var text = clipboardData.getData("text/plain") || clipboardData.getData("Text");
    return text ? text : (clipboardData = clipboardData.getData("text/uri-list")) ? clipboardData.replace(/\r?\n/g, " ") : "";
  }
  function compareObjs(a, b) {
    if (a == b) {
      return !0;
    }
    for (var p in a) {
      if (a[p] !== b[p]) {
        return !1;
      }
    }
    for (var _p in b) {
      if (!(_p in a)) {
        return !1;
      }
    }
    return !0;
  }
  function mapChildren(oldChildren, newLocal, mapping, node, offset, oldOffset, options) {
    for (var children = oldChildren.slice(), _loop4 = function(_baseOffset) {
      var moved = 0;
      mapping.maps[i].forEach(function(oldStart, oldEnd, newStart, newEnd) {
        newStart = newEnd - newStart - (oldEnd - oldStart);
        for (newEnd = 0; newEnd < children.length; newEnd += 3) {
          var end = children[newEnd + 1];
          0 > end || oldStart > end + _baseOffset - moved || (end = children[newEnd] + _baseOffset - moved, oldEnd >= end ? children[newEnd + 1] = oldStart <= end ? -2 : -1 : oldStart >= _baseOffset && newStart && (children[newEnd] += newStart, children[newEnd + 1] += newStart));
        }
        moved += newStart;
      });
      baseOffset = _baseOffset = mapping.maps[i].map(_baseOffset, -1);
    }, i = 0, baseOffset = oldOffset; i < mapping.maps.length; i++) {
      _loop4(baseOffset);
    }
    _loop4 = !1;
    for (var _i9 = 0; _i9 < children.length; _i9 += 3) {
      if (0 > children[_i9 + 1]) {
        if (-2 == children[_i9 + 1]) {
          _loop4 = !0, children[_i9 + 1] = -1;
        } else {
          var from = mapping.map(oldChildren[_i9] + oldOffset), fromLocal = from - offset;
          if (0 > fromLocal || fromLocal >= node.content.size) {
            _loop4 = !0;
          } else {
            var toLocal = mapping.map(oldChildren[_i9 + 1] + oldOffset, -1) - offset, _node$content$findInd2 = node.content.findIndex(fromLocal), childOffset = _node$content$findInd2.offset;
            (_node$content$findInd2 = node.maybeChild(_node$content$findInd2.index)) && childOffset == fromLocal && childOffset + _node$content$findInd2.nodeSize == toLocal ? (from = children[_i9 + 2].mapInner(mapping, _node$content$findInd2, from + 1, oldChildren[_i9] + oldOffset + 1, options), from != empty ? (children[_i9] = fromLocal, children[_i9 + 1] = toLocal, children[_i9 + 2] = from) : (children[_i9 + 1] = -2, _loop4 = !0)) : _loop4 = !0;
          }
        }
      }
    }
    if (_loop4) {
      newLocal = mapAndGatherRemainingDecorations(children, oldChildren, newLocal, mapping, offset, oldOffset, options);
      node = buildTree(newLocal, node, 0, options);
      newLocal = node.local;
      for (options = 0; options < children.length; options += 3) {
        0 > children[options + 1] && (children.splice(options, 3), options -= 3);
      }
      for (oldChildren = options = 0; options < node.children.length; options += 3) {
        for (offset = node.children[options]; oldChildren < children.length && children[oldChildren] < offset;) {
          oldChildren += 3;
        }
        children.splice(oldChildren, 0, node.children[options], node.children[options + 1], node.children[options + 2]);
      }
    }
    return new DecorationSet(newLocal.sort(byPos), children);
  }
  function moveSpans(spans, offset) {
    if (!offset || !spans.length) {
      return spans;
    }
    for (var result = [], i = 0; i < spans.length; i++) {
      var span = spans[i];
      result.push(new Decoration(span.from + offset, span.to + offset, span.type));
    }
    return result;
  }
  function mapAndGatherRemainingDecorations(children, oldChildren, decorations, mapping, offset, oldOffset, options) {
    function gather(set, oldOffset) {
      for (var i = 0; i < set.local.length; i++) {
        var mapped = set.local[i].map(mapping, offset, oldOffset);
        if (mapped) {
          decorations.push(mapped);
        } else if (options.onRemove) {
          options.onRemove(set.local[i].spec);
        }
      }
      for (i = 0; i < set.children.length; i += 3) {
        gather(set.children[i + 2], set.children[i] + oldOffset + 1);
      }
    }
    for (var i = 0; i < children.length; i += 3) {
      -1 == children[i + 1] && gather(children[i + 2], oldChildren[i] + oldOffset + 1);
    }
    return decorations;
  }
  function takeSpansForNode(spans, node, offset) {
    if (node.isLeaf) {
      return null;
    }
    node = offset + node.nodeSize;
    for (var found = null, i = 0, span; i < spans.length; i++) {
      (span = spans[i]) && span.from > offset && span.to < node && ((found || (found = [])).push(span), spans[i] = null);
    }
    return found;
  }
  function withoutNulls(array) {
    for (var result = [], i = 0; i < array.length; i++) {
      null != array[i] && result.push(array[i]);
    }
    return result;
  }
  function buildTree(spans, node, offset, options) {
    var children = [], hasNulls = !1;
    node.forEach(function(childNode, localStart) {
      var found = takeSpansForNode(spans, childNode, localStart + offset);
      found && (hasNulls = !0, found = buildTree(found, childNode, offset + localStart + 1, options), found != empty && children.push(localStart, localStart + childNode.nodeSize, found));
    });
    for (var locals = moveSpans(hasNulls ? withoutNulls(spans) : spans, -offset).sort(byPos), i = 0; i < locals.length; i++) {
      if (!locals[i].type.valid(node, locals[i])) {
        if (options.onRemove) {
          options.onRemove(locals[i].spec);
        }
        locals.splice(i--, 1);
      }
    }
    return locals.length || children.length ? new DecorationSet(locals, children) : empty;
  }
  function byPos(a, b) {
    return a.from - b.from || a.to - b.to;
  }
  function removeOverlap(spans) {
    for (var working = spans, i = 0; i < working.length - 1; i++) {
      var span = working[i];
      if (span.from != span.to) {
        for (var j = i + 1; j < working.length; j++) {
          var next = working[j];
          if (next.from == span.from) {
            next.to != span.to && (working == spans && (working = spans.slice()), working[j] = next.copy(next.from, span.to), insertAhead(working, j + 1, next.copy(span.to, next.to)));
          } else {
            next.from < span.to && (working == spans && (working = spans.slice()), working[i] = span.copy(span.from, next.from), insertAhead(working, j, span.copy(next.from, span.to)));
            break;
          }
        }
      }
    }
    return working;
  }
  function insertAhead(array, i, deco) {
    for (; i < array.length && 0 < byPos(deco, array[i]);) {
      i++;
    }
    array.splice(i, 0, deco);
  }
  function viewDecorations(view) {
    var found = [];
    view.someProp("decorations", function(f) {
      (f = f(view.state)) && f != empty && found.push(f);
    });
    view.cursorWrapper && found.push(DecorationSet.create(view.state.doc, [view.cursorWrapper.deco]));
    return DecorationGroup.from(found);
  }
  function checkCSS(view) {
    cssChecked.has(view) || (cssChecked.set(view, null), -1 !== ["normal", "nowrap", "pre-line"].indexOf(getComputedStyle(view.dom).whiteSpace) && (view.requiresGeckoHackNode = gecko, cssCheckWarned || (console.warn("ProseMirror expects the CSS white-space property to be set, preferably to 'pre-wrap'. It is recommended to load style/prosemirror.css from the prosemirror-view package."), cssCheckWarned = !0)));
  }
  function rangeToSelectionRange(view, range) {
    var anchorNode = range.startContainer, anchorOffset = range.startOffset, focusNode = range.endContainer;
    range = range.endOffset;
    view = view.domAtPos(view.state.selection.anchor);
    isEquivalentPosition(view.node, view.offset, focusNode, range) && (view = [focusNode, range, anchorNode, anchorOffset], anchorNode = view[0], anchorOffset = view[1], focusNode = view[2], range = view[3]);
    return {anchorNode, anchorOffset, focusNode, focusOffset:range};
  }
  function safariShadowSelectionRange(view, selection) {
    function read(event) {
      event.preventDefault();
      event.stopImmediatePropagation();
      found = event.getTargetRanges()[0];
    }
    if (selection.getComposedRanges && (selection = selection.getComposedRanges(view.root)[0])) {
      return rangeToSelectionRange(view, selection);
    }
    var found;
    view.dom.addEventListener("beforeinput", read, !0);
    document.execCommand("indent");
    view.dom.removeEventListener("beforeinput", read, !0);
    return found ? rangeToSelectionRange(view, found) : null;
  }
  function blockParent(view, node) {
    for (node = node.parentNode; node && node != view.dom; node = node.parentNode) {
      var desc = view.docView.nearestDesc(node, !0);
      if (desc && desc.node.isBlock) {
        return node;
      }
    }
    return null;
  }
  function parseBetween(view, from_, to_) {
    to_ = view.docView.parseRange(from_, to_);
    var parent = to_.node, fromOffset = to_.fromOffset, toOffset = to_.toOffset;
    from_ = to_.from;
    to_ = to_.to;
    var domSel = view.domSelectionRange(), anchor = domSel.anchorNode;
    if (anchor && view.dom.contains(1 == anchor.nodeType ? anchor : anchor.parentNode)) {
      var find = [{node:anchor, offset:domSel.anchorOffset}];
      selectionCollapsed(domSel) || find.push({node:domSel.focusNode, offset:domSel.focusOffset});
    }
    if (chrome && 8 === view.input.lastKeyCode) {
      for (domSel = toOffset; domSel > fromOffset; domSel--) {
        anchor = parent.childNodes[domSel - 1];
        var desc = anchor.pmViewDesc;
        if ("BR" == anchor.nodeName && !desc) {
          toOffset = domSel;
          break;
        }
        if (!desc || desc.size) {
          break;
        }
      }
    }
    domSel = view.state.doc;
    view = view.someProp("domParser") || prosemirrorModel.DOMParser.fromSchema(view.state.schema);
    anchor = domSel.resolve(from_);
    domSel = null;
    parent = view.parse(parent, {topNode:anchor.parent, topMatch:anchor.parent.contentMatchAt(anchor.index()), topOpen:!0, from:fromOffset, to:toOffset, preserveWhitespace:"pre" == anchor.parent.type.whitespace ? "full" : !0, findPositions:find, ruleFromNode, context:anchor});
    find && null != find[0].pos && (fromOffset = find[0].pos, find = find[1] && find[1].pos, null == find && (find = fromOffset), domSel = {anchor:fromOffset + from_, head:find + from_});
    return {doc:parent, sel:domSel, from:from_, to:to_};
  }
  function ruleFromNode(dom) {
    var desc = dom.pmViewDesc;
    if (desc) {
      return desc.parseRule();
    }
    if ("BR" == dom.nodeName && dom.parentNode) {
      if (safari && /^(ul|ol)$/i.test(dom.parentNode.nodeName)) {
        return dom = document.createElement("div"), dom.appendChild(document.createElement("li")), {skip:dom};
      }
      if (dom.parentNode.lastChild == dom || safari && /^(tr|table)$/i.test(dom.parentNode.nodeName)) {
        return {ignore:!0};
      }
    } else if ("IMG" == dom.nodeName && dom.getAttribute("mark-placeholder")) {
      return {ignore:!0};
    }
    return null;
  }
  function readDOMChange(view, from, to, typeOver, addedNodes) {
    var compositionID = view.input.compositionPendingChanges || (view.composing ? view.input.compositionID : 0);
    view.input.compositionPendingChanges = 0;
    if (0 > from) {
      var origin = view.input.lastSelectionTime > Date.now() - 50 ? view.input.lastSelectionOrigin : null, newSel = selectionFromDOM(view, origin);
      !newSel || view.state.selection.eq(newSel) || chrome && android && 13 === view.input.lastKeyCode && Date.now() - 100 < view.input.lastKeyCodeTime && view.someProp("handleKeyDown", function(f) {
        return f(view, keyEvent(13, "Enter"));
      }) || (newSel = view.state.tr.setSelection(newSel), "pointer" == origin ? newSel.setMeta("pointer", !0) : "key" == origin && newSel.scrollIntoView(), compositionID && newSel.setMeta("composition", compositionID), view.dispatch(newSel));
    } else {
      from = view.state.doc.resolve(from);
      var shared = from.sharedDepth(to);
      from = from.before(shared + 1);
      to = view.state.doc.resolve(to).after(shared + 1);
      shared = view.state.selection;
      to = parseBetween(view, from, to);
      from = view.state.doc;
      var compare = from.slice(to.from, to.to);
      if (8 === view.input.lastKeyCode && Date.now() - 100 < view.input.lastKeyCodeTime) {
        var preferredPos = view.state.selection.to;
        var preferredSide = "end";
      } else {
        preferredPos = view.state.selection.from, preferredSide = "start";
      }
      view.input.lastKeyCode = null;
      (compare = findDiff(compare.content, to.doc.content, to.from, preferredPos, preferredSide)) && view.input.domChangeCount++;
      if ((ios && view.input.lastIOSEnter > Date.now() - 225 || android) && addedNodes.some(function(n) {
        return 1 == n.nodeType && !isInline.test(n.nodeName);
      }) && (!compare || compare.endA >= compare.endB) && view.someProp("handleKeyDown", function(f) {
        return f(view, keyEvent(13, "Enter"));
      })) {
        view.input.lastIOSEnter = 0;
      } else {
        if (!compare) {
          if (!(typeOver && shared instanceof prosemirrorState.TextSelection && !shared.empty && shared.$head.sameParent(shared.$anchor)) || view.composing || to.sel && to.sel.anchor != to.sel.head) {
            to.sel && (origin = resolveSelection(view, view.state.doc, to.sel)) && !origin.eq(view.state.selection) && (origin = view.state.tr.setSelection(origin), compositionID && origin.setMeta("composition", compositionID), view.dispatch(origin));
            return;
          }
          compare = {start:shared.from, endA:shared.to, endB:shared.to};
        }
        view.state.selection.from < view.state.selection.to && compare.start == compare.endB && view.state.selection instanceof prosemirrorState.TextSelection && (compare.start > view.state.selection.from && compare.start <= view.state.selection.from + 2 && view.state.selection.from >= to.from ? compare.start = view.state.selection.from : compare.endA < view.state.selection.to && compare.endA >= view.state.selection.to - 2 && view.state.selection.to <= to.to && (compare.endB += view.state.selection.to - 
        compare.endA, compare.endA = view.state.selection.to));
        ie && 11 >= ie_version && compare.endB == compare.start + 1 && compare.endA == compare.start && compare.start > to.from && "  " == to.doc.textBetween(compare.start - to.from - 1, compare.start - to.from + 1) && (compare.start--, compare.endA--, compare.endB--);
        typeOver = to.doc.resolveNoCache(compare.start - to.from);
        shared = to.doc.resolveNoCache(compare.endB - to.from);
        preferredPos = from.resolve(compare.start);
        preferredSide = typeOver.sameParent(shared) && typeOver.parent.inlineContent && preferredPos.end() >= compare.endA;
        var nextSel;
        if ((ios && view.input.lastIOSEnter > Date.now() - 225 && (!preferredSide || addedNodes.some(function(n) {
          return "DIV" == n.nodeName || "P" == n.nodeName;
        })) || !preferredSide && typeOver.pos < to.doc.content.size && !typeOver.sameParent(shared) && (nextSel = prosemirrorState.Selection.findFrom(to.doc.resolve(typeOver.pos + 1), 1, !0)) && nextSel.head == shared.pos) && view.someProp("handleKeyDown", function(f) {
          return f(view, keyEvent(13, "Enter"));
        })) {
          view.input.lastIOSEnter = 0;
        } else {
          if (view.state.selection.anchor > compare.start && looksLikeBackspace(from, compare.start, compare.endA, typeOver, shared) && view.someProp("handleKeyDown", function(f) {
            return f(view, keyEvent(8, "Backspace"));
          })) {
            android && chrome && view.domObserver.suppressSelectionUpdates();
          } else {
            chrome && android && compare.endB == compare.start && (view.input.lastAndroidDelete = Date.now());
            android && !preferredSide && typeOver.start() != shared.start() && 0 == shared.parentOffset && typeOver.depth == shared.depth && to.sel && to.sel.anchor == to.sel.head && to.sel.head == compare.endA && (compare.endB -= 2, shared = to.doc.resolveNoCache(compare.endB - to.from), setTimeout(function() {
              view.someProp("handleKeyDown", function(f) {
                return f(view, keyEvent(13, "Enter"));
              });
            }, 20));
            var chFrom = compare.start, chTo = compare.endA, markChange;
            if (preferredSide) {
              if (typeOver.pos == shared.pos) {
                ie && 11 >= ie_version && 0 == typeOver.parentOffset && (view.domObserver.suppressSelectionUpdates(), setTimeout(function() {
                  return selectionToDOM(view);
                }, 20)), origin = view.state.tr["delete"](chFrom, chTo), newSel = from.resolve(compare.start).marksAcross(from.resolve(compare.endA));
              } else if (compare.endA == compare.endB && (markChange = isMarkChange(typeOver.parent.content.cut(typeOver.parentOffset, shared.parentOffset), preferredPos.parent.content.cut(preferredPos.parentOffset, compare.endA - preferredPos.start())))) {
                origin = view.state.tr, "add" == markChange.type ? origin.addMark(chFrom, chTo, markChange.mark) : origin.removeMark(chFrom, chTo, markChange.mark);
              } else if (typeOver.parent.child(typeOver.index()).isText && typeOver.index() == shared.index() - (shared.textOffset ? 0 : 1)) {
                var text = typeOver.parent.textBetween(typeOver.parentOffset, shared.parentOffset);
                if (view.someProp("handleTextInput", function(f) {
                  return f(view, chFrom, chTo, text);
                })) {
                  return;
                }
                origin = view.state.tr.insertText(text, chFrom, chTo);
              }
            }
            origin || (origin = view.state.tr.replace(chFrom, chTo, to.doc.slice(compare.start - to.from, compare.endB - to.from)));
            to.sel && (addedNodes = resolveSelection(view, origin.doc, to.sel), !addedNodes || chrome && android && view.composing && addedNodes.empty && (compare.start != compare.endB || view.input.lastAndroidDelete < Date.now() - 100) && (addedNodes.head == chFrom || addedNodes.head == origin.mapping.map(chTo) - 1) || ie && addedNodes.empty && addedNodes.head == chFrom || origin.setSelection(addedNodes));
            newSel && origin.ensureMarks(newSel);
            compositionID && origin.setMeta("composition", compositionID);
            view.dispatch(origin.scrollIntoView());
          }
        }
      }
    }
  }
  function resolveSelection(view, doc, parsedSel) {
    return Math.max(parsedSel.anchor, parsedSel.head) > doc.content.size ? null : selectionBetween(view, doc.resolve(parsedSel.anchor), doc.resolve(parsedSel.head));
  }
  function isMarkChange(cur, prev) {
    for (var curMarks = cur.firstChild.marks, prevMarks = prev.firstChild.marks, added = curMarks, removed = prevMarks, mark, i = 0; i < prevMarks.length; i++) {
      added = prevMarks[i].removeFromSet(added);
    }
    for (prevMarks = 0; prevMarks < curMarks.length; prevMarks++) {
      removed = curMarks[prevMarks].removeFromSet(removed);
    }
    if (1 == added.length && 0 == removed.length) {
      mark = added[0], curMarks = "add", added = function(node) {
        return node.mark(mark.addToSet(node.marks));
      };
    } else if (0 == added.length && 1 == removed.length) {
      mark = removed[0], curMarks = "remove", added = function(node) {
        return node.mark(mark.removeFromSet(node.marks));
      };
    } else {
      return null;
    }
    removed = [];
    for (prevMarks = 0; prevMarks < prev.childCount; prevMarks++) {
      removed.push(added(prev.child(prevMarks)));
    }
    if (prosemirrorModel.Fragment.from(removed).eq(cur)) {
      return {mark, type:curMarks};
    }
  }
  function looksLikeBackspace(old, start, end, $newStart, $newEnd) {
    if (end - start <= $newEnd.pos - $newStart.pos || skipClosingAndOpening($newStart, !0, !1) < $newEnd.pos) {
      return !1;
    }
    $newEnd = old.resolve(start);
    if (!$newStart.parent.isTextblock) {
      return $newStart = $newEnd.nodeAfter, null != $newStart && end == start + $newStart.nodeSize;
    }
    if ($newEnd.parentOffset < $newEnd.parent.content.size || !$newEnd.parent.isTextblock) {
      return !1;
    }
    start = old.resolve(skipClosingAndOpening($newEnd, !0, !0));
    return !start.parent.isTextblock || start.pos > end || skipClosingAndOpening(start, !0, !1) < end ? !1 : $newStart.parent.content.cut($newStart.parentOffset).eq(start.parent.content);
  }
  function skipClosingAndOpening($pos, fromEnd, mayOpen) {
    for (var depth = $pos.depth, end = fromEnd ? $pos.end() : $pos.pos; 0 < depth && (fromEnd || $pos.indexAfter(depth) == $pos.node(depth).childCount);) {
      depth--, end++, fromEnd = !1;
    }
    if (mayOpen) {
      for ($pos = $pos.node(depth).maybeChild($pos.indexAfter(depth)); $pos && !$pos.isLeaf;) {
        $pos = $pos.firstChild, end++;
      }
    }
    return end;
  }
  function findDiff(a, b, pos, preferredPos, preferredSide) {
    var start = a.findDiffStart(b, pos);
    if (null == start) {
      return null;
    }
    var _a$findDiffEnd = a.findDiffEnd(b, pos + a.size, pos + b.size);
    pos = _a$findDiffEnd.a;
    _a$findDiffEnd = _a$findDiffEnd.b;
    "end" == preferredSide && (preferredPos -= pos + Math.max(0, start - Math.min(pos, _a$findDiffEnd)) - start);
    pos < start && a.size < b.size ? (a = preferredPos <= start && preferredPos >= pos ? start - preferredPos : 0, (start -= a) && start < b.size && isSurrogatePair(b.textBetween(start - 1, start + 1)) && (start += a ? 1 : -1), _a$findDiffEnd = start + (_a$findDiffEnd - pos), pos = start) : _a$findDiffEnd < start && (b = preferredPos <= start && preferredPos >= _a$findDiffEnd ? start - preferredPos : 0, (start -= b) && start < a.size && isSurrogatePair(a.textBetween(start - 1, start + 1)) && (start += 
    b ? 1 : -1), pos = start + (pos - _a$findDiffEnd), _a$findDiffEnd = start);
    return {start, endA:pos, endB:_a$findDiffEnd};
  }
  function isSurrogatePair(str) {
    if (2 != str.length) {
      return !1;
    }
    var a = str.charCodeAt(0);
    str = str.charCodeAt(1);
    return 56320 <= a && 57343 >= a && 55296 <= str && 56319 >= str;
  }
  function computeDocDeco(view) {
    var attrs = Object.create(null);
    attrs["class"] = "ProseMirror";
    attrs.contenteditable = String(view.editable);
    view.someProp("attributes", function(value) {
      "function" == typeof value && (value = value(view.state));
      if (value) {
        for (var attr in value) {
          "class" == attr ? attrs["class"] += " " + value[attr] : "style" == attr ? attrs.style = (attrs.style ? attrs.style + ";" : "") + value[attr] : attrs[attr] || "contenteditable" == attr || "nodeName" == attr || (attrs[attr] = String(value[attr]));
        }
      }
    });
    attrs.translate || (attrs.translate = "no");
    return [Decoration.node(0, view.state.doc.content.size, attrs)];
  }
  function updateCursorWrapper(view) {
    if (view.markCursor) {
      var dom = document.createElement("img");
      dom.className = "ProseMirror-separator";
      dom.setAttribute("mark-placeholder", "true");
      dom.setAttribute("alt", "");
      view.cursorWrapper = {dom, deco:Decoration.widget(view.state.selection.from, dom, {raw:!0, marks:view.markCursor})};
    } else {
      view.cursorWrapper = null;
    }
  }
  function getEditable(view) {
    return !view.someProp("editable", function(value) {
      return !1 === value(view.state);
    });
  }
  function buildNodeViews(view) {
    function add(obj) {
      for (var _prop3 in obj) {
        Object.prototype.hasOwnProperty.call(result, _prop3) || (result[_prop3] = obj[_prop3]);
      }
    }
    var result = Object.create(null);
    view.someProp("nodeViews", add);
    view.someProp("markViews", add);
    return result;
  }
  function checkStateComponent(plugin) {
    if (plugin.spec.state || plugin.spec.filterTransaction || plugin.spec.appendTransaction) {
      throw new RangeError("Plugins passed directly to the view must not have a state component");
    }
  }
  var prosemirrorState = require("module$node_modules$prosemirror_state$dist$index_cjs"), prosemirrorModel = require("module$node_modules$prosemirror_model$dist$index_cjs"), prosemirrorTransform = require("module$node_modules$prosemirror_transform$dist$index_cjs"), domIndex = function(node) {
    for (var index = 0;; index++) {
      if (node = node.previousSibling, !node) {
        return index;
      }
    }
  }, parentNode = function(node) {
    return (node = node.assignedSlot || node.parentNode) && 11 == node.nodeType ? node.host : node;
  }, reusedRange = null, textRange = function(node, from, to) {
    var range = reusedRange || (reusedRange = document.createRange());
    range.setEnd(node, null == to ? node.nodeValue.length : to);
    range.setStart(node, from || 0);
    return range;
  }, isEquivalentPosition = function(node, off, targetNode, targetOff) {
    return targetNode && (scanFor(node, off, targetNode, targetOff, -1) || scanFor(node, off, targetNode, targetOff, 1));
  }, atomElements = /^(img|br|input|textarea|hr)$/i, selectionCollapsed = function(domSel) {
    return domSel.focusNode && isEquivalentPosition(domSel.focusNode, domSel.focusOffset, domSel.anchorNode, domSel.anchorOffset);
  }, nav = "undefined" != typeof navigator ? navigator : null, doc = "undefined" != typeof document ? document : null, agent = nav && nav.userAgent || "", ie_edge = /Edge\/(\d+)/.exec(agent), ie_upto10 = /MSIE \d/.exec(agent), ie_11up = /Trident\/(?:[7-9]|\d{2,})\..*rv:(\d+)/.exec(agent), ie = !!(ie_upto10 || ie_11up || ie_edge), ie_version = ie_upto10 ? document.documentMode : ie_11up ? +ie_11up[1] : ie_edge ? +ie_edge[1] : 0, gecko = !ie && /gecko\/(\d+)/i.test(agent);
  gecko && +(/Firefox\/(\d+)/.exec(agent) || [0, 0])[1];
  var _chrome = !ie && /Chrome\/(\d+)/.exec(agent), chrome = !!_chrome, chrome_version = _chrome ? +_chrome[1] : 0, safari = !ie && !!nav && /Apple Computer/.test(nav.vendor), ios = safari && (/Mobile\/\w+/.test(agent) || !!nav && 2 < nav.maxTouchPoints), mac = ios || (nav ? /Mac/.test(nav.platform) : !1), windows = nav ? /Win/.test(nav.platform) : !1, android = /Android \d/.test(agent), webkit = !!doc && "webkitFontSmoothing" in doc.documentElement.style, webkit_version = webkit ? +(/\bAppleWebKit\/(\d+)/.exec(navigator.userAgent) || 
  [0, 0])[1] : 0, preventScrollSupported = null, BIDI = /[\u0590-\u05f4\u0600-\u06ff\u0700-\u08ac]/, maybeRTL = /[\u0590-\u08ac]/, cachedState = null, cachedDir = null, cachedResult = !1, ViewDesc = function() {
    function ViewDesc(parent, children, dom, contentDOM) {
      _classCallCheck(this, ViewDesc);
      this.parent = parent;
      this.children = children;
      this.dom = dom;
      this.contentDOM = contentDOM;
      this.dirty = 0;
      dom.pmViewDesc = this;
    }
    _createClass(ViewDesc, [{key:"matchesWidget", value:function(widget) {
      return !1;
    }}, {key:"matchesMark", value:function(mark) {
      return !1;
    }}, {key:"matchesNode", value:function(node, outerDeco, innerDeco) {
      return !1;
    }}, {key:"matchesHack", value:function(nodeName) {
      return !1;
    }}, {key:"parseRule", value:function() {
      return null;
    }}, {key:"stopEvent", value:function(event) {
      return !1;
    }}, {key:"size", get:function() {
      for (var size = 0, i = 0; i < this.children.length; i++) {
        size += this.children[i].size;
      }
      return size;
    }}, {key:"border", get:function() {
      return 0;
    }}, {key:"destroy", value:function() {
      this.parent = void 0;
      this.dom.pmViewDesc == this && (this.dom.pmViewDesc = void 0);
      for (var i = 0; i < this.children.length; i++) {
        this.children[i].destroy();
      }
    }}, {key:"posBeforeChild", value:function(child) {
      for (var i = 0, pos = this.posAtStart;; i++) {
        var cur = this.children[i];
        if (cur == child) {
          return pos;
        }
        pos += cur.size;
      }
    }}, {key:"posBefore", get:function() {
      return this.parent.posBeforeChild(this);
    }}, {key:"posAtStart", get:function() {
      return this.parent ? this.parent.posBeforeChild(this) + this.border : 0;
    }}, {key:"posAfter", get:function() {
      return this.posBefore + this.size;
    }}, {key:"posAtEnd", get:function() {
      return this.posAtStart + this.size - 2 * this.border;
    }}, {key:"localPosFromDOM", value:function(dom, offset, bias) {
      if (this.contentDOM && this.contentDOM.contains(1 == dom.nodeType ? dom : dom.parentNode)) {
        if (0 > bias) {
          var desc;
          if (dom == this.contentDOM) {
            bias = dom.childNodes[offset - 1];
          } else {
            for (; dom.parentNode != this.contentDOM;) {
              dom = dom.parentNode;
            }
            bias = dom.previousSibling;
          }
          for (; bias && (!(desc = bias.pmViewDesc) || desc.parent != this);) {
            bias = bias.previousSibling;
          }
          return bias ? this.posBeforeChild(desc) + desc.size : this.posAtStart;
        }
        var _desc;
        if (dom == this.contentDOM) {
          bias = dom.childNodes[offset];
        } else {
          for (; dom.parentNode != this.contentDOM;) {
            dom = dom.parentNode;
          }
          bias = dom.nextSibling;
        }
        for (; bias && (!(_desc = bias.pmViewDesc) || _desc.parent != this);) {
          bias = bias.nextSibling;
        }
        return bias ? this.posBeforeChild(_desc) : this.posAtEnd;
      }
      if (dom == this.dom && this.contentDOM) {
        var atEnd = offset > domIndex(this.contentDOM);
      } else if (this.contentDOM && this.contentDOM != this.dom && this.dom.contains(this.contentDOM)) {
        atEnd = dom.compareDocumentPosition(this.contentDOM) & 2;
      } else if (this.dom.firstChild) {
        if (0 == offset) {
          for (desc = dom;; desc = desc.parentNode) {
            if (desc == this.dom) {
              atEnd = !1;
              break;
            }
            if (desc.previousSibling) {
              break;
            }
          }
        }
        if (null == atEnd && offset == dom.childNodes.length) {
          for (;; dom = dom.parentNode) {
            if (dom == this.dom) {
              atEnd = !0;
              break;
            }
            if (dom.nextSibling) {
              break;
            }
          }
        }
      }
      return (null == atEnd ? 0 < bias : atEnd) ? this.posAtEnd : this.posAtStart;
    }}, {key:"nearestDesc", value:function(dom) {
      for (var onlyNodes = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : !1, first = !0, cur = dom; cur; cur = cur.parentNode) {
        var desc = this.getDesc(cur), nodeDOM = void 0;
        if (desc && (!onlyNodes || desc.node)) {
          if (first && (nodeDOM = desc.nodeDOM) && (1 == nodeDOM.nodeType ? !nodeDOM.contains(1 == dom.nodeType ? dom : dom.parentNode) : nodeDOM != dom)) {
            first = !1;
          } else {
            return desc;
          }
        }
      }
    }}, {key:"getDesc", value:function(dom) {
      for (var cur = dom = dom.pmViewDesc; cur; cur = cur.parent) {
        if (cur == this) {
          return dom;
        }
      }
    }}, {key:"posFromDOM", value:function(dom, offset, bias) {
      for (var scan = dom; scan; scan = scan.parentNode) {
        var desc = this.getDesc(scan);
        if (desc) {
          return desc.localPosFromDOM(dom, offset, bias);
        }
      }
      return -1;
    }}, {key:"descAt", value:function(pos) {
      for (var i = 0, offset = 0; i < this.children.length; i++) {
        var child = this.children[i], end = offset + child.size;
        if (offset == pos && end != offset) {
          for (; !child.border && child.children.length;) {
            child = child.children[0];
          }
          return child;
        }
        if (pos < end) {
          return child.descAt(pos - offset - child.border);
        }
        offset = end;
      }
    }}, {key:"domFromPos", value:function(pos, side) {
      if (!this.contentDOM) {
        return {node:this.dom, offset:0, atom:pos + 1};
      }
      for (var i = 0, offset = 0, curPos = 0; i < this.children.length; i++) {
        var child = this.children[i], end = curPos + child.size;
        if (end > pos || child instanceof TrailingHackViewDesc) {
          offset = pos - curPos;
          break;
        }
        curPos = end;
      }
      if (offset) {
        return this.children[i].domFromPos(offset - this.children[i].border, side);
      }
      for (var prev; i && !(prev = this.children[i - 1]).size && prev instanceof WidgetViewDesc && 0 <= prev.side; i--) {
      }
      if (0 >= side) {
        for (offset = !0; pos = i ? this.children[i - 1] : null, pos && pos.dom.parentNode != this.contentDOM; i--, offset = !1) {
        }
        return pos && side && offset && !pos.border && !pos.domAtom ? pos.domFromPos(pos.size, side) : {node:this.contentDOM, offset:pos ? domIndex(pos.dom) + 1 : 0};
      }
      for (offset = !0; pos = i < this.children.length ? this.children[i] : null, pos && pos.dom.parentNode != this.contentDOM; i++, offset = !1) {
      }
      return pos && offset && !pos.border && !pos.domAtom ? pos.domFromPos(0, side) : {node:this.contentDOM, offset:pos ? domIndex(pos.dom) : this.contentDOM.childNodes.length};
    }}, {key:"parseRange", value:function(from, to) {
      if (0 == this.children.length) {
        return {node:this.contentDOM, from, to, fromOffset:0, toOffset:this.contentDOM.childNodes.length};
      }
      for (var fromOffset = -1, toOffset = -1, offset = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : 0, i = 0;; i++) {
        var child = this.children[i], end = offset + child.size;
        if (-1 == fromOffset && from <= end) {
          var childBase = offset + child.border;
          if (from >= childBase && to <= end - child.border && child.node && child.contentDOM && this.contentDOM.contains(child.contentDOM)) {
            return child.parseRange(from, to, childBase);
          }
          from = offset;
          for (offset = i; 0 < offset; offset--) {
            child = this.children[offset - 1];
            if (child.size && child.dom.parentNode == this.contentDOM && !child.emptyChildAt(1)) {
              fromOffset = domIndex(child.dom) + 1;
              break;
            }
            from -= child.size;
          }
          -1 == fromOffset && (fromOffset = 0);
        }
        if (-1 < fromOffset && (end > to || i == this.children.length - 1)) {
          to = end;
          for (i += 1; i < this.children.length; i++) {
            end = this.children[i];
            if (end.size && end.dom.parentNode == this.contentDOM && !end.emptyChildAt(-1)) {
              toOffset = domIndex(end.dom);
              break;
            }
            to += end.size;
          }
          -1 == toOffset && (toOffset = this.contentDOM.childNodes.length);
          break;
        }
        offset = end;
      }
      return {node:this.contentDOM, from, to, fromOffset, toOffset};
    }}, {key:"emptyChildAt", value:function(side) {
      if (this.border || !this.contentDOM || !this.children.length) {
        return !1;
      }
      var child = this.children[0 > side ? 0 : this.children.length - 1];
      return 0 == child.size || child.emptyChildAt(side);
    }}, {key:"domAfterPos", value:function(pos) {
      var _this$domFromPos = this.domFromPos(pos, 0), node = _this$domFromPos.node;
      _this$domFromPos = _this$domFromPos.offset;
      if (1 != node.nodeType || _this$domFromPos == node.childNodes.length) {
        throw new RangeError("No node after pos " + pos);
      }
      return node.childNodes[_this$domFromPos];
    }}, {key:"setSelection", value:function(anchor, head, root) {
      for (var force = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : !1, from = Math.min(anchor, head), to = Math.max(anchor, head), i = 0, offset = 0; i < this.children.length; i++) {
        var child = this.children[i], end = offset + child.size;
        if (from > offset && to < end) {
          return child.setSelection(anchor - offset - child.border, head - offset - child.border, root, force);
        }
        offset = end;
      }
      from = this.domFromPos(anchor, anchor ? -1 : 1);
      to = head == anchor ? from : this.domFromPos(head, head ? -1 : 1);
      i = root.getSelection();
      offset = !1;
      if ((gecko || safari) && anchor == head) {
        if (offset = from, child = offset.node, end = offset.offset, 3 == child.nodeType) {
          if ((offset = !(!end || "\n" != child.nodeValue[end - 1])) && end == child.nodeValue.length) {
            for (; child; child = child.parentNode) {
              if (end = child.nextSibling) {
                "BR" == end.nodeName && (from = to = {node:end.parentNode, offset:domIndex(end) + 1});
                break;
              }
              if ((end = child.pmViewDesc) && end.node && end.node.isBlock) {
                break;
              }
            }
          }
        } else {
          offset = (offset = child.childNodes[end - 1]) && ("BR" == offset.nodeName || "false" == offset.contentEditable);
        }
      }
      gecko && i.focusNode && i.focusNode != to.node && 1 == i.focusNode.nodeType && (child = i.focusNode.childNodes[i.focusOffset]) && "false" == child.contentEditable && (force = !0);
      if (force || offset && safari || !isEquivalentPosition(from.node, from.offset, i.anchorNode, i.anchorOffset) || !isEquivalentPosition(to.node, to.offset, i.focusNode, i.focusOffset)) {
        force = !1;
        if ((i.extend || anchor == head) && !offset) {
          i.collapse(from.node, from.offset);
          try {
            anchor != head && i.extend(to.node, to.offset), force = !0;
          } catch (_) {
          }
        }
        force || (anchor > head && (force = from, from = to, to = force), force = document.createRange(), force.setEnd(to.node, to.offset), force.setStart(from.node, from.offset), i.removeAllRanges(), i.addRange(force));
      }
    }}, {key:"ignoreMutation", value:function(mutation) {
      return !this.contentDOM && "selection" != mutation.type;
    }}, {key:"contentLost", get:function() {
      return this.contentDOM && this.contentDOM != this.dom && !this.dom.contains(this.contentDOM);
    }}, {key:"markDirty", value:function(from, to) {
      for (var offset = 0, i = 0; i < this.children.length; i++) {
        var child = this.children[i], end = offset + child.size;
        if (offset == end ? from <= end && to >= offset : from < end && to > offset) {
          var startInside = offset + child.border, endInside = end - child.border;
          if (from >= startInside && to <= endInside) {
            this.dirty = from == offset || to == end ? 2 : 1;
            from != startInside || to != endInside || !child.contentLost && child.dom.parentNode == this.contentDOM ? child.markDirty(from - startInside, to - startInside) : child.dirty = 3;
            return;
          }
          child.dirty = child.dom != child.contentDOM || child.dom.parentNode != this.contentDOM || child.children.length ? 3 : 2;
        }
        offset = end;
      }
      this.dirty = 2;
    }}, {key:"markParentsDirty", value:function() {
      for (var level = 1, node = this.parent; node; node = node.parent, level++) {
        var dirty = 1 == level ? 2 : 1;
        node.dirty < dirty && (node.dirty = dirty);
      }
    }}, {key:"domAtom", get:function() {
      return !1;
    }}, {key:"ignoreForCoords", get:function() {
      return !1;
    }}, {key:"isText", value:function(text) {
      return !1;
    }}]);
    return ViewDesc;
  }(), WidgetViewDesc = function(_ViewDesc) {
    function WidgetViewDesc(parent, widget, view, pos) {
      _classCallCheck(this, WidgetViewDesc);
      var dom = widget.type.toDOM;
      "function" == typeof dom && (dom = dom(view, function() {
        if (!self) {
          return pos;
        }
        if (self.parent) {
          return self.parent.posBeforeChild(self);
        }
      }));
      widget.type.spec.raw || (1 != dom.nodeType && (view = document.createElement("span"), view.appendChild(dom), dom = view), dom.contentEditable = "false", dom.classList.add("ProseMirror-widget"));
      parent = _super.call(this, parent, [], dom, null);
      parent.widget = widget;
      parent.widget = widget;
      var self = _assertThisInitialized(parent);
      return parent;
    }
    _inherits(WidgetViewDesc, _ViewDesc);
    var _super = _createSuper(WidgetViewDesc);
    _createClass(WidgetViewDesc, [{key:"matchesWidget", value:function(widget) {
      return 0 == this.dirty && widget.type.eq(this.widget.type);
    }}, {key:"parseRule", value:function() {
      return {ignore:!0};
    }}, {key:"stopEvent", value:function(event) {
      var stop = this.widget.spec.stopEvent;
      return stop ? stop(event) : !1;
    }}, {key:"ignoreMutation", value:function(mutation) {
      return "selection" != mutation.type || this.widget.spec.ignoreSelection;
    }}, {key:"destroy", value:function() {
      this.widget.type.destroy(this.dom);
      _get(_getPrototypeOf(WidgetViewDesc.prototype), "destroy", this).call(this);
    }}, {key:"domAtom", get:function() {
      return !0;
    }}, {key:"side", get:function() {
      return this.widget.type.side;
    }}]);
    return WidgetViewDesc;
  }(ViewDesc), CompositionViewDesc = function(_ViewDesc2) {
    function CompositionViewDesc(parent, dom, textDOM, text) {
      _classCallCheck(this, CompositionViewDesc);
      parent = _super2.call(this, parent, [], dom, null);
      parent.textDOM = textDOM;
      parent.text = text;
      return parent;
    }
    _inherits(CompositionViewDesc, _ViewDesc2);
    var _super2 = _createSuper(CompositionViewDesc);
    _createClass(CompositionViewDesc, [{key:"size", get:function() {
      return this.text.length;
    }}, {key:"localPosFromDOM", value:function(dom, offset) {
      return dom != this.textDOM ? this.posAtStart + (offset ? this.size : 0) : this.posAtStart + offset;
    }}, {key:"domFromPos", value:function(pos) {
      return {node:this.textDOM, offset:pos};
    }}, {key:"ignoreMutation", value:function(mut) {
      return "characterData" === mut.type && mut.target.nodeValue == mut.oldValue;
    }}]);
    return CompositionViewDesc;
  }(ViewDesc), MarkViewDesc = function(_ViewDesc3) {
    function MarkViewDesc(parent, mark, dom, contentDOM, spec) {
      _classCallCheck(this, MarkViewDesc);
      parent = _super3.call(this, parent, [], dom, contentDOM);
      parent.mark = mark;
      parent.spec = spec;
      return parent;
    }
    _inherits(MarkViewDesc, _ViewDesc3);
    var _super3 = _createSuper(MarkViewDesc);
    _createClass(MarkViewDesc, [{key:"parseRule", value:function() {
      return this.dirty & 3 || this.mark.type.spec.reparseInView ? null : {mark:this.mark.type.name, attrs:this.mark.attrs, contentElement:this.contentDOM};
    }}, {key:"matchesMark", value:function(mark) {
      return 3 != this.dirty && this.mark.eq(mark);
    }}, {key:"markDirty", value:function(from, to) {
      _get(_getPrototypeOf(MarkViewDesc.prototype), "markDirty", this).call(this, from, to);
      if (0 != this.dirty) {
        for (from = this.parent; !from.node;) {
          from = from.parent;
        }
        from.dirty < this.dirty && (from.dirty = this.dirty);
        this.dirty = 0;
      }
    }}, {key:"slice", value:function(from, to, view) {
      var copy = MarkViewDesc.create(this.parent, this.mark, !0, view), nodes = this.children, size = this.size;
      to < size && (nodes = replaceNodes(nodes, to, size, view));
      0 < from && (nodes = replaceNodes(nodes, 0, from, view));
      for (from = 0; from < nodes.length; from++) {
        nodes[from].parent = copy;
      }
      copy.children = nodes;
      return copy;
    }}, {key:"destroy", value:function() {
      this.spec.destroy && this.spec.destroy();
      _get(_getPrototypeOf(MarkViewDesc.prototype), "destroy", this).call(this);
    }}], [{key:"create", value:function(parent, mark, inline, view) {
      var custom = view.nodeViews[mark.type.name];
      (view = custom && custom(mark, view, inline)) && view.dom || (view = prosemirrorModel.DOMSerializer.renderSpec(document, mark.type.spec.toDOM(mark, inline), null, mark.attrs));
      return new MarkViewDesc(parent, mark, view.dom, view.contentDOM || view.dom, view);
    }}]);
    return MarkViewDesc;
  }(ViewDesc), NodeViewDesc = function(_ViewDesc4) {
    function NodeViewDesc(parent, node, outerDeco, innerDeco, dom, contentDOM, nodeDOM, view, pos) {
      _classCallCheck(this, NodeViewDesc);
      parent = _super4.call(this, parent, [], dom, contentDOM);
      parent.node = node;
      parent.outerDeco = outerDeco;
      parent.innerDeco = innerDeco;
      parent.nodeDOM = nodeDOM;
      return parent;
    }
    _inherits(NodeViewDesc, _ViewDesc4);
    var _super4 = _createSuper(NodeViewDesc);
    _createClass(NodeViewDesc, [{key:"parseRule", value:function() {
      var _this5 = this;
      if (this.node.type.spec.reparseInView) {
        return null;
      }
      var rule = {node:this.node.type.name, attrs:this.node.attrs};
      "pre" == this.node.type.whitespace && (rule.preserveWhitespace = "full");
      if (this.contentDOM) {
        if (this.contentLost) {
          for (var i = this.children.length - 1; 0 <= i; i--) {
            var child = this.children[i];
            if (this.dom.contains(child.dom.parentNode)) {
              rule.contentElement = child.dom.parentNode;
              break;
            }
          }
          rule.contentElement || (rule.getContent = function() {
            return prosemirrorModel.Fragment.empty;
          });
        } else {
          rule.contentElement = this.contentDOM;
        }
      } else {
        rule.getContent = function() {
          return _this5.node.content;
        };
      }
      return rule;
    }}, {key:"matchesNode", value:function(node, outerDeco, innerDeco) {
      return 0 == this.dirty && node.eq(this.node) && sameOuterDeco(outerDeco, this.outerDeco) && innerDeco.eq(this.innerDeco);
    }}, {key:"size", get:function() {
      return this.node.nodeSize;
    }}, {key:"border", get:function() {
      return this.node.isLeaf ? 0 : 1;
    }}, {key:"updateChildren", value:function(view, pos) {
      var _this6 = this, inline = this.node.inlineContent, off = pos, composition = view.composing ? this.localCompositionInfo(view, pos) : null;
      pos = composition && -1 < composition.pos ? composition : null;
      var compositionInChild = composition && 0 > composition.pos, updater = new ViewTreeUpdater(this, pos && pos.node, view);
      iterDeco(this.node, this.innerDeco, function(widget, i, insideNode) {
        widget.spec.marks ? updater.syncToMarks(widget.spec.marks, inline, view) : 0 <= widget.type.side && !insideNode && updater.syncToMarks(i == _this6.node.childCount ? prosemirrorModel.Mark.none : _this6.node.child(i).marks, inline, view);
        updater.placeWidget(widget, view, off);
      }, function(child, outerDeco, innerDeco, i) {
        updater.syncToMarks(child.marks, inline, view);
        var compIndex;
        updater.findNodeMatch(child, outerDeco, innerDeco, i) || compositionInChild && view.state.selection.from > off && view.state.selection.to < off + child.nodeSize && -1 < (compIndex = updater.findIndexWithChild(composition.node)) && updater.updateNodeAt(child, outerDeco, innerDeco, compIndex, view) || updater.updateNextNode(child, outerDeco, innerDeco, view, i, off) || updater.addNode(child, outerDeco, innerDeco, view, off);
        off += child.nodeSize;
      });
      updater.syncToMarks([], inline, view);
      this.node.isTextblock && updater.addTextblockHacks();
      updater.destroyRest();
      if (updater.changed || 2 == this.dirty) {
        pos && this.protectLocalComposition(view, pos), renderDescs(this.contentDOM, this.children, view), ios && iosHacks(this.dom);
      }
    }}, {key:"localCompositionInfo", value:function(view, pos$jscomp$0) {
      var _view$state$selection = view.state.selection, from = _view$state$selection.from, to = _view$state$selection.to;
      if (!(view.state.selection instanceof prosemirrorState.TextSelection) || from < pos$jscomp$0 || to > pos$jscomp$0 + this.node.content.size) {
        return null;
      }
      view = view.input.compositionNode;
      if (!view || !this.dom.contains(view.parentNode)) {
        return null;
      }
      if (this.node.inlineContent) {
        _view$state$selection = view.nodeValue;
        a: {
          var textPos = this.node.content;
          from -= pos$jscomp$0;
          pos$jscomp$0 = to - pos$jscomp$0;
          for (var pos = to = 0; to < textPos.childCount && pos <= pos$jscomp$0;) {
            var child = textPos.child(to++), childStart = pos;
            pos += child.nodeSize;
            if (child.isText) {
              for (child = child.text; to < textPos.childCount;) {
                var next = textPos.child(to++);
                pos += next.nodeSize;
                if (!next.isText) {
                  break;
                }
                child += next.text;
              }
              if (pos >= from) {
                if (pos >= pos$jscomp$0 && child.slice(pos$jscomp$0 - _view$state$selection.length - childStart, pos$jscomp$0 - childStart) == _view$state$selection) {
                  textPos = pos$jscomp$0 - _view$state$selection.length;
                  break a;
                }
                next = childStart < pos$jscomp$0 ? child.lastIndexOf(_view$state$selection, pos$jscomp$0 - childStart - 1) : -1;
                if (0 <= next && next + _view$state$selection.length + childStart >= from) {
                  textPos = childStart + next;
                  break a;
                }
                if (from == pos$jscomp$0 && child.length >= pos$jscomp$0 + _view$state$selection.length - childStart && child.slice(pos$jscomp$0 - childStart, pos$jscomp$0 - childStart + _view$state$selection.length) == _view$state$selection) {
                  textPos = pos$jscomp$0;
                  break a;
                }
              }
            }
          }
          textPos = -1;
        }
        return 0 > textPos ? null : {node:view, pos:textPos, text:_view$state$selection};
      }
      return {node:view, pos:-1, text:""};
    }}, {key:"protectLocalComposition", value:function(view, _ref2) {
      var node = _ref2.node, pos = _ref2.pos;
      _ref2 = _ref2.text;
      if (!this.getDesc(node)) {
        for (var topNode = node; topNode.parentNode != this.contentDOM; topNode = topNode.parentNode) {
          for (; topNode.previousSibling;) {
            topNode.parentNode.removeChild(topNode.previousSibling);
          }
          for (; topNode.nextSibling;) {
            topNode.parentNode.removeChild(topNode.nextSibling);
          }
          topNode.pmViewDesc && (topNode.pmViewDesc = void 0);
        }
        node = new CompositionViewDesc(this, topNode, node, _ref2);
        view.input.compositionNodes.push(node);
        this.children = replaceNodes(this.children, pos, pos + _ref2.length, view, node);
      }
    }}, {key:"update", value:function(node, outerDeco, innerDeco, view) {
      if (3 == this.dirty || !node.sameMarkup(this.node)) {
        return !1;
      }
      this.updateInner(node, outerDeco, innerDeco, view);
      return !0;
    }}, {key:"updateInner", value:function(node, outerDeco, innerDeco, view) {
      this.updateOuterDeco(outerDeco);
      this.node = node;
      this.innerDeco = innerDeco;
      this.contentDOM && this.updateChildren(view, this.posAtStart);
      this.dirty = 0;
    }}, {key:"updateOuterDeco", value:function(outerDeco) {
      if (!sameOuterDeco(outerDeco, this.outerDeco)) {
        var needsWrap = 1 != this.nodeDOM.nodeType, oldDOM = this.dom;
        this.dom = patchOuterDeco(this.dom, this.nodeDOM, computeOuterDeco(this.outerDeco, this.node, needsWrap), computeOuterDeco(outerDeco, this.node, needsWrap));
        this.dom != oldDOM && (oldDOM.pmViewDesc = void 0, this.dom.pmViewDesc = this);
        this.outerDeco = outerDeco;
      }
    }}, {key:"selectNode", value:function() {
      1 == this.nodeDOM.nodeType && this.nodeDOM.classList.add("ProseMirror-selectednode");
      if (this.contentDOM || !this.node.type.spec.draggable) {
        this.dom.draggable = !0;
      }
    }}, {key:"deselectNode", value:function() {
      1 == this.nodeDOM.nodeType && (this.nodeDOM.classList.remove("ProseMirror-selectednode"), !this.contentDOM && this.node.type.spec.draggable || this.dom.removeAttribute("draggable"));
    }}, {key:"domAtom", get:function() {
      return this.node.isAtom;
    }}], [{key:"create", value:function(parent, node, outerDeco, innerDeco, view, pos) {
      var custom = view.nodeViews[node.type.name], descObj, dom = (custom = custom && custom(node, view, function() {
        if (!descObj) {
          return pos;
        }
        if (descObj.parent) {
          return descObj.parent.posBeforeChild(descObj);
        }
      }, outerDeco, innerDeco)) && custom.dom, contentDOM = custom && custom.contentDOM;
      if (node.isText) {
        if (!dom) {
          dom = document.createTextNode(node.text);
        } else if (3 != dom.nodeType) {
          throw new RangeError("Text must be rendered as a DOM text node");
        }
      } else {
        dom || (contentDOM = prosemirrorModel.DOMSerializer.renderSpec(document, node.type.spec.toDOM(node), null, node.attrs), dom = contentDOM.dom, contentDOM = contentDOM.contentDOM);
      }
      contentDOM || node.isText || "BR" == dom.nodeName || (dom.hasAttribute("contenteditable") || (dom.contentEditable = "false"), node.type.spec.draggable && (dom.draggable = !0));
      var nodeDOM = dom;
      dom = patchOuterDeco(dom, dom, noDeco, computeOuterDeco(outerDeco, node, 1 != dom.nodeType));
      return custom ? descObj = new CustomNodeViewDesc(parent, node, outerDeco, innerDeco, dom, contentDOM || null, nodeDOM, custom, view, pos + 1) : node.isText ? new TextViewDesc(parent, node, outerDeco, innerDeco, dom, nodeDOM, view) : new NodeViewDesc(parent, node, outerDeco, innerDeco, dom, contentDOM || null, nodeDOM, view, pos + 1);
    }}]);
    return NodeViewDesc;
  }(ViewDesc), TextViewDesc = function(_NodeViewDesc) {
    function TextViewDesc(parent, node, outerDeco, innerDeco, dom, nodeDOM, view) {
      _classCallCheck(this, TextViewDesc);
      return _super5.call(this, parent, node, outerDeco, innerDeco, dom, null, nodeDOM, view, 0);
    }
    _inherits(TextViewDesc, _NodeViewDesc);
    var _super5 = _createSuper(TextViewDesc);
    _createClass(TextViewDesc, [{key:"parseRule", value:function() {
      for (var skip = this.nodeDOM.parentNode; skip && skip != this.dom && !skip.pmIsDeco;) {
        skip = skip.parentNode;
      }
      return {skip:skip || !0};
    }}, {key:"update", value:function(node, outerDeco, innerDeco, view) {
      if (3 == this.dirty || 0 != this.dirty && !this.inParent() || !node.sameMarkup(this.node)) {
        return !1;
      }
      this.updateOuterDeco(outerDeco);
      0 == this.dirty && node.text == this.node.text || node.text == this.nodeDOM.nodeValue || (this.nodeDOM.nodeValue = node.text, view.trackWrites == this.nodeDOM && (view.trackWrites = null));
      this.node = node;
      this.dirty = 0;
      return !0;
    }}, {key:"inParent", value:function() {
      for (var parentDOM = this.parent.contentDOM, n = this.nodeDOM; n; n = n.parentNode) {
        if (n == parentDOM) {
          return !0;
        }
      }
      return !1;
    }}, {key:"domFromPos", value:function(pos) {
      return {node:this.nodeDOM, offset:pos};
    }}, {key:"localPosFromDOM", value:function(dom, offset, bias) {
      return dom == this.nodeDOM ? this.posAtStart + Math.min(offset, this.node.text.length) : _get(_getPrototypeOf(TextViewDesc.prototype), "localPosFromDOM", this).call(this, dom, offset, bias);
    }}, {key:"ignoreMutation", value:function(mutation) {
      return "characterData" != mutation.type && "selection" != mutation.type;
    }}, {key:"slice", value:function(from, to, view) {
      from = this.node.cut(from, to);
      to = document.createTextNode(from.text);
      return new TextViewDesc(this.parent, from, this.outerDeco, this.innerDeco, to, to, view);
    }}, {key:"markDirty", value:function(from, to) {
      _get(_getPrototypeOf(TextViewDesc.prototype), "markDirty", this).call(this, from, to);
      this.dom == this.nodeDOM || 0 != from && to != this.nodeDOM.nodeValue.length || (this.dirty = 3);
    }}, {key:"domAtom", get:function() {
      return !1;
    }}, {key:"isText", value:function(text) {
      return this.node.text == text;
    }}]);
    return TextViewDesc;
  }(NodeViewDesc), TrailingHackViewDesc = function(_ViewDesc5) {
    function TrailingHackViewDesc() {
      _classCallCheck(this, TrailingHackViewDesc);
      return _super6.apply(this, arguments);
    }
    _inherits(TrailingHackViewDesc, _ViewDesc5);
    var _super6 = _createSuper(TrailingHackViewDesc);
    _createClass(TrailingHackViewDesc, [{key:"parseRule", value:function() {
      return {ignore:!0};
    }}, {key:"matchesHack", value:function(nodeName) {
      return 0 == this.dirty && this.dom.nodeName == nodeName;
    }}, {key:"domAtom", get:function() {
      return !0;
    }}, {key:"ignoreForCoords", get:function() {
      return "IMG" == this.dom.nodeName;
    }}]);
    return TrailingHackViewDesc;
  }(ViewDesc), CustomNodeViewDesc = function(_NodeViewDesc2) {
    function CustomNodeViewDesc(parent, node, outerDeco, innerDeco, dom, contentDOM, nodeDOM, spec, view, pos) {
      _classCallCheck(this, CustomNodeViewDesc);
      parent = _super7.call(this, parent, node, outerDeco, innerDeco, dom, contentDOM, nodeDOM, view, pos);
      parent.spec = spec;
      return parent;
    }
    _inherits(CustomNodeViewDesc, _NodeViewDesc2);
    var _super7 = _createSuper(CustomNodeViewDesc);
    _createClass(CustomNodeViewDesc, [{key:"update", value:function(node, outerDeco, innerDeco, view) {
      if (3 == this.dirty) {
        return !1;
      }
      if (this.spec.update && (this.node.type == node.type || this.spec.multiType)) {
        var result = this.spec.update(node, outerDeco, innerDeco);
        result && this.updateInner(node, outerDeco, innerDeco, view);
        return result;
      }
      return this.contentDOM || node.isLeaf ? _get(_getPrototypeOf(CustomNodeViewDesc.prototype), "update", this).call(this, node, outerDeco, innerDeco, view) : !1;
    }}, {key:"selectNode", value:function() {
      this.spec.selectNode ? this.spec.selectNode() : _get(_getPrototypeOf(CustomNodeViewDesc.prototype), "selectNode", this).call(this);
    }}, {key:"deselectNode", value:function() {
      this.spec.deselectNode ? this.spec.deselectNode() : _get(_getPrototypeOf(CustomNodeViewDesc.prototype), "deselectNode", this).call(this);
    }}, {key:"setSelection", value:function(anchor, head, root, force) {
      this.spec.setSelection ? this.spec.setSelection(anchor, head, root) : _get(_getPrototypeOf(CustomNodeViewDesc.prototype), "setSelection", this).call(this, anchor, head, root, force);
    }}, {key:"destroy", value:function() {
      this.spec.destroy && this.spec.destroy();
      _get(_getPrototypeOf(CustomNodeViewDesc.prototype), "destroy", this).call(this);
    }}, {key:"stopEvent", value:function(event) {
      return this.spec.stopEvent ? this.spec.stopEvent(event) : !1;
    }}, {key:"ignoreMutation", value:function(mutation) {
      return this.spec.ignoreMutation ? this.spec.ignoreMutation(mutation) : _get(_getPrototypeOf(CustomNodeViewDesc.prototype), "ignoreMutation", this).call(this, mutation);
    }}]);
    return CustomNodeViewDesc;
  }(NodeViewDesc), OuterDecoLevel = function(nodeName) {
    nodeName && (this.nodeName = nodeName);
  };
  OuterDecoLevel.prototype = Object.create(null);
  var noDeco = [new OuterDecoLevel()], ViewTreeUpdater = function() {
    function ViewTreeUpdater(top, lock, view) {
      _classCallCheck(this, ViewTreeUpdater);
      this.lock = lock;
      this.view = view;
      this.index = 0;
      this.stack = [];
      this.changed = !1;
      this.top = top;
      lock = top.node.content;
      view = top;
      var descI = view.children.length, fI = lock.childCount, matched = new Map(), matches = [];
      a: for (; 0 < fI;) {
        for (var desc;;) {
          if (descI) {
            if (desc = view.children[descI - 1], desc instanceof MarkViewDesc) {
              view = desc, descI = desc.children.length;
            } else {
              descI--;
              break;
            }
          } else if (view == top) {
            break a;
          } else {
            descI = view.parent.children.indexOf(view), view = view.parent;
          }
        }
        var node = desc.node;
        if (node) {
          if (node != lock.child(fI - 1)) {
            break;
          }
          --fI;
          matched.set(desc, fI);
          matches.push(desc);
        }
      }
      this.preMatch = {index:fI, matched, matches:matches.reverse()};
    }
    _createClass(ViewTreeUpdater, [{key:"destroyBetween", value:function(start, end) {
      if (start != end) {
        for (var i = start; i < end; i++) {
          this.top.children[i].destroy();
        }
        this.top.children.splice(start, end - start);
        this.changed = !0;
      }
    }}, {key:"destroyRest", value:function() {
      this.destroyBetween(this.index, this.top.children.length);
    }}, {key:"syncToMarks", value:function(marks, inline, view) {
      for (var keep = 0, depth = this.stack.length >> 1, maxKeep = Math.min(depth, marks.length); keep < maxKeep && (keep == depth - 1 ? this.top : this.stack[keep + 1 << 1]).matchesMark(marks[keep]) && !1 !== marks[keep].type.spec.spanning;) {
        keep++;
      }
      for (; keep < depth;) {
        this.destroyRest(), this.top.dirty = 0, this.index = this.stack.pop(), this.top = this.stack.pop(), depth--;
      }
      for (; depth < marks.length;) {
        this.stack.push(this.top, this.index + 1);
        keep = -1;
        for (maxKeep = this.index; maxKeep < Math.min(this.index + 3, this.top.children.length); maxKeep++) {
          var next = this.top.children[maxKeep];
          if (next.matchesMark(marks[depth]) && !this.isLocked(next.dom)) {
            keep = maxKeep;
            break;
          }
        }
        -1 < keep ? (keep > this.index && (this.changed = !0, this.destroyBetween(this.index, keep)), this.top = this.top.children[this.index]) : (keep = MarkViewDesc.create(this.top, marks[depth], inline, view), this.top.children.splice(this.index, 0, keep), this.top = keep, this.changed = !0);
        this.index = 0;
        depth++;
      }
    }}, {key:"findNodeMatch", value:function(node, outerDeco, innerDeco, index) {
      var found = -1, targetDesc;
      if (index >= this.preMatch.index && (targetDesc = this.preMatch.matches[index - this.preMatch.index]).parent == this.top && targetDesc.matchesNode(node, outerDeco, innerDeco)) {
        found = this.top.children.indexOf(targetDesc, this.index);
      } else {
        for (index = this.index, targetDesc = Math.min(this.top.children.length, index + 5); index < targetDesc; index++) {
          var child = this.top.children[index];
          if (child.matchesNode(node, outerDeco, innerDeco) && !this.preMatch.matched.has(child)) {
            found = index;
            break;
          }
        }
      }
      if (0 > found) {
        return !1;
      }
      this.destroyBetween(this.index, found);
      this.index++;
      return !0;
    }}, {key:"updateNodeAt", value:function(node, outerDeco, innerDeco, index, view) {
      var child = this.top.children[index];
      3 == child.dirty && child.dom == child.contentDOM && (child.dirty = 2);
      if (!child.update(node, outerDeco, innerDeco, view)) {
        return !1;
      }
      this.destroyBetween(this.index, index);
      this.index++;
      return !0;
    }}, {key:"findIndexWithChild", value:function(domNode) {
      for (;;) {
        var parent = domNode.parentNode;
        if (!parent) {
          return -1;
        }
        if (parent == this.top.contentDOM) {
          if (domNode = domNode.pmViewDesc) {
            for (parent = this.index; parent < this.top.children.length; parent++) {
              if (this.top.children[parent] == domNode) {
                return parent;
              }
            }
          }
          return -1;
        }
        domNode = parent;
      }
    }}, {key:"updateNextNode", value:function(node, outerDeco, innerDeco, view, index, pos) {
      for (var i = this.index; i < this.top.children.length; i++) {
        var next = this.top.children[i];
        if (next instanceof NodeViewDesc) {
          var _preMatch = this.preMatch.matched.get(next);
          if (null != _preMatch && _preMatch != index) {
            break;
          }
          index = next.dom;
          _preMatch = void 0;
          var locked = this.isLocked(index) && !(node.isText && next.node && next.node.isText && next.nodeDOM.nodeValue == node.text && 3 != next.dirty && sameOuterDeco(outerDeco, next.outerDeco));
          if (!locked && next.update(node, outerDeco, innerDeco, view)) {
            return this.destroyBetween(this.index, i), next.dom != index && (this.changed = !0), this.index++, !0;
          }
          if (!locked && (_preMatch = this.recreateWrapper(next, node, outerDeco, innerDeco, view, pos))) {
            return this.destroyBetween(this.index, i), this.top.children[this.index] = _preMatch, _preMatch.contentDOM && (_preMatch.dirty = 2, _preMatch.updateChildren(view, pos + 1), _preMatch.dirty = 0), this.changed = !0, this.index++, !0;
          }
          break;
        }
      }
      return !1;
    }}, {key:"recreateWrapper", value:function(next, node, outerDeco, innerDeco, view, pos) {
      if (next.dirty || node.isAtom || !next.children.length || !next.node.content.eq(node.content) || !sameOuterDeco(outerDeco, next.outerDeco) || !innerDeco.eq(next.innerDeco)) {
        return null;
      }
      node = NodeViewDesc.create(this.top, node, outerDeco, innerDeco, view, pos);
      if (node.contentDOM) {
        node.children = next.children;
        next.children = [];
        outerDeco = _createForOfIteratorHelper(node.children);
        var _step;
        try {
          for (outerDeco.s(); !(_step = outerDeco.n()).done;) {
            _step.value.parent = node;
          }
        } catch (err) {
          outerDeco.e(err);
        } finally {
          outerDeco.f();
        }
      }
      next.destroy();
      return node;
    }}, {key:"addNode", value:function(node, outerDeco, innerDeco, view, pos) {
      node = NodeViewDesc.create(this.top, node, outerDeco, innerDeco, view, pos);
      node.contentDOM && node.updateChildren(view, pos + 1);
      this.top.children.splice(this.index++, 0, node);
      this.changed = !0;
    }}, {key:"placeWidget", value:function(widget, view, pos) {
      var next = this.index < this.top.children.length ? this.top.children[this.index] : null;
      !next || !next.matchesWidget(widget) || widget != next.widget && next.widget.type.toDOM.parentNode ? (widget = new WidgetViewDesc(this.top, widget, view, pos), this.top.children.splice(this.index++, 0, widget), this.changed = !0) : this.index++;
    }}, {key:"addTextblockHacks", value:function() {
      for (var lastChild = this.top.children[this.index - 1], parent = this.top; lastChild instanceof MarkViewDesc;) {
        parent = lastChild, lastChild = parent.children[parent.children.length - 1];
      }
      if (!(lastChild && lastChild instanceof TextViewDesc) || /\n$/.test(lastChild.node.text) || this.view.requiresGeckoHackNode && /\s$/.test(lastChild.node.text)) {
        (safari || chrome) && lastChild && "false" == lastChild.dom.contentEditable && this.addHackNode("IMG", parent), this.addHackNode("BR", this.top);
      }
    }}, {key:"addHackNode", value:function(nodeName, parent) {
      if (parent == this.top && this.index < parent.children.length && parent.children[this.index].matchesHack(nodeName)) {
        this.index++;
      } else {
        var dom = document.createElement(nodeName);
        "IMG" == nodeName && (dom.className = "ProseMirror-separator", dom.alt = "");
        "BR" == nodeName && (dom.className = "ProseMirror-trailingBreak");
        nodeName = new TrailingHackViewDesc(this.top, [], dom, null);
        parent != this.top ? parent.children.push(nodeName) : parent.children.splice(this.index++, 0, nodeName);
        this.changed = !0;
      }
    }}, {key:"isLocked", value:function(node) {
      return this.lock && (node == this.lock || 1 == node.nodeType && node.contains(this.lock.parentNode));
    }}]);
    return ViewTreeUpdater;
  }(), brokenSelectBetweenUneditable = safari || chrome && 63 > chrome_version, inlineParents = /^(a|abbr|acronym|b|cite|code|del|em|i|ins|kbd|label|output|q|ruby|s|samp|span|strong|sub|sup|time|u|tt|var)$/i, wrapMap = {thead:["table"], tbody:["table"], tfoot:["table"], caption:["table"], colgroup:["table"], col:["table", "colgroup"], tr:["table", "tbody"], td:["table", "tbody", "tr"], th:["table", "tbody", "tr"]}, _detachedDoc = null, _policy = null, handlers = {}, editHandlers = {}, passiveHandlers = 
  {touchstart:!0, touchmove:!0}, InputState = _createClass(function InputState() {
    _classCallCheck(this, InputState);
    this.shiftKey = !1;
    this.lastKeyCode = this.mouseDown = null;
    this.lastKeyCodeTime = 0;
    this.lastClick = {time:0, x:0, y:0, type:""};
    this.lastSelectionOrigin = null;
    this.lastIOSEnter = this.lastSelectionTime = 0;
    this.lastIOSEnterFallbackTimeout = -1;
    this.lastAndroidDelete = this.lastTouch = this.lastFocus = 0;
    this.composing = !1;
    this.compositionNode = null;
    this.composingTimeout = -1;
    this.compositionNodes = [];
    this.compositionEndedAt = -2e8;
    this.compositionID = 1;
    this.domChangeCount = this.compositionPendingChanges = 0;
    this.eventHandlers = Object.create(null);
    this.hideSelectionGuard = null;
  });
  editHandlers.keydown = function(view, _event) {
    view.input.shiftKey = 16 == _event.keyCode || _event.shiftKey;
    if (!inOrNearComposition(view, _event) && (view.input.lastKeyCode = _event.keyCode, view.input.lastKeyCodeTime = Date.now(), !android || !chrome || 13 != _event.keyCode)) {
      if (229 != _event.keyCode && view.domObserver.forceFlush(), !ios || 13 != _event.keyCode || _event.ctrlKey || _event.altKey || _event.metaKey) {
        view.someProp("handleKeyDown", function(f) {
          return f(view, _event);
        }) || captureKeyDown(view, _event) ? _event.preventDefault() : setSelectionOrigin(view, "key");
      } else {
        var now = Date.now();
        view.input.lastIOSEnter = now;
        view.input.lastIOSEnterFallbackTimeout = setTimeout(function() {
          view.input.lastIOSEnter == now && (view.someProp("handleKeyDown", function(f) {
            return f(view, keyEvent(13, "Enter"));
          }), view.input.lastIOSEnter = 0);
        }, 200);
      }
    }
  };
  editHandlers.keyup = function(view, event) {
    16 == event.keyCode && (view.input.shiftKey = !1);
  };
  editHandlers.keypress = function(view, _event) {
    if (!(inOrNearComposition(view, _event) || !_event.charCode || _event.ctrlKey && !_event.altKey || mac && _event.metaKey)) {
      if (view.someProp("handleKeyPress", function(f) {
        return f(view, _event);
      })) {
        _event.preventDefault();
      } else {
        var sel = view.state.selection;
        if (!(sel instanceof prosemirrorState.TextSelection && sel.$from.sameParent(sel.$to))) {
          var text = String.fromCharCode(_event.charCode);
          /[\r\n]/.test(text) || view.someProp("handleTextInput", function(f) {
            return f(view, sel.$from.pos, sel.$to.pos, text);
          }) || view.dispatch(view.state.tr.insertText(text).scrollIntoView());
          _event.preventDefault();
        }
      }
    }
  };
  var selectNodeModifier = mac ? "metaKey" : "ctrlKey";
  handlers.mousedown = function(view, _event) {
    view.input.shiftKey = _event.shiftKey;
    var flushed = endComposition(view), now = Date.now(), type = "singleClick", JSCompiler_temp;
    if (JSCompiler_temp = 500 > now - view.input.lastClick.time) {
      var click = view.input.lastClick;
      JSCompiler_temp = click.x - _event.clientX;
      click = click.y - _event.clientY;
      JSCompiler_temp = 100 > JSCompiler_temp * JSCompiler_temp + click * click;
    }
    JSCompiler_temp && !_event[selectNodeModifier] && ("singleClick" == view.input.lastClick.type ? type = "doubleClick" : "doubleClick" == view.input.lastClick.type && (type = "tripleClick"));
    view.input.lastClick = {time:now, x:_event.clientX, y:_event.clientY, type};
    if (now = view.posAtCoords(eventCoords(_event))) {
      "singleClick" == type ? (view.input.mouseDown && view.input.mouseDown.done(), view.input.mouseDown = new MouseDown(view, now, _event, !!flushed)) : ("doubleClick" == type ? handleDoubleClick : handleTripleClick)(view, now.pos, now.inside, _event) ? _event.preventDefault() : setSelectionOrigin(view, "pointer");
    }
  };
  var MouseDown = function() {
    function MouseDown(view, pos, event, flushed) {
      var _this8 = this;
      _classCallCheck(this, MouseDown);
      this.view = view;
      this.pos = pos;
      this.event = event;
      this.flushed = flushed;
      this.delayedSelectionSync = !1;
      this.mightDrag = null;
      this.startDoc = view.state.doc;
      this.selectNode = !!event[selectNodeModifier];
      this.allowDefault = event.shiftKey;
      if (-1 < pos.inside) {
        var targetNode = view.state.doc.nodeAt(pos.inside);
        pos = pos.inside;
      } else {
        pos = view.state.doc.resolve(pos.pos), targetNode = pos.parent, pos = pos.depth ? pos.before() : 0;
      }
      this.target = (flushed = (flushed = flushed ? null : event.target) ? view.docView.nearestDesc(flushed, !0) : null) && 1 == flushed.dom.nodeType ? flushed.dom : null;
      flushed = view.state.selection;
      if (0 == event.button && targetNode.type.spec.draggable && !1 !== targetNode.type.spec.selectable || flushed instanceof prosemirrorState.NodeSelection && flushed.from <= pos && flushed.to > pos) {
        this.mightDrag = {node:targetNode, pos, addAttr:!(!this.target || this.target.draggable), setUneditable:!(!this.target || !gecko || this.target.hasAttribute("contentEditable"))};
      }
      this.target && this.mightDrag && (this.mightDrag.addAttr || this.mightDrag.setUneditable) && (this.view.domObserver.stop(), this.mightDrag.addAttr && (this.target.draggable = !0), this.mightDrag.setUneditable && setTimeout(function() {
        _this8.view.input.mouseDown == _this8 && _this8.target.setAttribute("contentEditable", "false");
      }, 20), this.view.domObserver.start());
      view.root.addEventListener("mouseup", this.up = this.up.bind(this));
      view.root.addEventListener("mousemove", this.move = this.move.bind(this));
      setSelectionOrigin(view, "pointer");
    }
    _createClass(MouseDown, [{key:"done", value:function() {
      var _this9 = this;
      this.view.root.removeEventListener("mouseup", this.up);
      this.view.root.removeEventListener("mousemove", this.move);
      this.mightDrag && this.target && (this.view.domObserver.stop(), this.mightDrag.addAttr && this.target.removeAttribute("draggable"), this.mightDrag.setUneditable && this.target.removeAttribute("contentEditable"), this.view.domObserver.start());
      this.delayedSelectionSync && setTimeout(function() {
        return selectionToDOM(_this9.view);
      });
      this.view.input.mouseDown = null;
    }}, {key:"up", value:function(event) {
      this.done();
      if (this.view.dom.contains(event.target)) {
        var pos = this.pos;
        this.view.state.doc != this.startDoc && (pos = this.view.posAtCoords(eventCoords(event)));
        this.updateAllowDefault(event);
        this.allowDefault || !pos ? setSelectionOrigin(this.view, "pointer") : handleSingleClick(this.view, pos.pos, pos.inside, event, this.selectNode) ? event.preventDefault() : 0 == event.button && (this.flushed || safari && this.mightDrag && !this.mightDrag.node.isAtom || chrome && !this.view.state.selection.visible && 2 >= Math.min(Math.abs(pos.pos - this.view.state.selection.from), Math.abs(pos.pos - this.view.state.selection.to))) ? (updateSelection(this.view, prosemirrorState.Selection.near(this.view.state.doc.resolve(pos.pos)), 
        "pointer"), event.preventDefault()) : setSelectionOrigin(this.view, "pointer");
      }
    }}, {key:"move", value:function(event) {
      this.updateAllowDefault(event);
      setSelectionOrigin(this.view, "pointer");
      0 == event.buttons && this.done();
    }}, {key:"updateAllowDefault", value:function(event) {
      !this.allowDefault && (4 < Math.abs(this.event.x - event.clientX) || 4 < Math.abs(this.event.y - event.clientY)) && (this.allowDefault = !0);
    }}]);
    return MouseDown;
  }();
  handlers.touchstart = function(view) {
    view.input.lastTouch = Date.now();
    endComposition(view);
    setSelectionOrigin(view, "pointer");
  };
  handlers.touchmove = function(view) {
    view.input.lastTouch = Date.now();
    setSelectionOrigin(view, "pointer");
  };
  handlers.contextmenu = function(view) {
    return endComposition(view);
  };
  var timeoutComposition = android ? 5000 : -1;
  editHandlers.compositionstart = editHandlers.compositionupdate = function(view) {
    if (!view.composing) {
      view.domObserver.flush();
      var state = view.state, $pos = state.selection.$to;
      if (state.selection instanceof prosemirrorState.TextSelection && (state.storedMarks || !$pos.textOffset && $pos.parentOffset && $pos.nodeBefore.marks.some(function(m) {
        return !1 === m.type.spec.inclusive;
      }))) {
        view.markCursor = view.state.storedMarks || $pos.marks(), endComposition(view, !0), view.markCursor = null;
      } else {
        if (endComposition(view, !state.selection.empty), gecko && state.selection.empty && $pos.parentOffset && !$pos.textOffset && $pos.nodeBefore.marks.length) {
          for ($pos = view.domSelectionRange(), state = $pos.focusNode, $pos = $pos.focusOffset; state && 1 == state.nodeType && 0 != $pos;) {
            state = 0 > $pos ? state.lastChild : state.childNodes[$pos - 1];
            if (!state) {
              break;
            }
            if (3 == state.nodeType) {
              ($pos = view.domSelection()) && $pos.collapse(state, state.nodeValue.length);
              break;
            } else {
              $pos = -1;
            }
          }
        }
      }
      view.input.composing = !0;
    }
    scheduleComposeEnd(view, timeoutComposition);
  };
  editHandlers.compositionend = function(view, event) {
    view.composing && (view.input.composing = !1, view.input.compositionEndedAt = event.timeStamp, view.input.compositionPendingChanges = view.domObserver.pendingRecords().length ? view.input.compositionID : 0, view.input.compositionNode = null, view.input.compositionPendingChanges && Promise.resolve().then(function() {
      return view.domObserver.flush();
    }), view.input.compositionID++, scheduleComposeEnd(view, 20));
  };
  var brokenClipboardAPI = ie && 15 > ie_version || ios && 604 > webkit_version;
  handlers.copy = editHandlers.cut = function(view, _event) {
    var sel = view.state.selection, cut = "cut" == _event.type;
    if (!sel.empty) {
      var data = brokenClipboardAPI ? null : _event.clipboardData;
      sel = sel.content();
      var _serializeForClipboar = serializeForClipboard(view, sel);
      sel = _serializeForClipboar.dom;
      _serializeForClipboar = _serializeForClipboar.text;
      data ? (_event.preventDefault(), data.clearData(), data.setData("text/html", sel.innerHTML), data.setData("text/plain", _serializeForClipboar)) : captureCopy(view, sel);
      cut && view.dispatch(view.state.tr.deleteSelection().scrollIntoView().setMeta("uiEvent", "cut"));
    }
  };
  editHandlers.paste = function(view, _event) {
    if (!view.composing || android) {
      var data = brokenClipboardAPI ? null : _event.clipboardData, plain = view.input.shiftKey && 45 != view.input.lastKeyCode;
      data && doPaste(view, getText(data), data.getData("text/html"), plain, _event) ? _event.preventDefault() : capturePaste(view, _event);
    }
  };
  var Dragging = _createClass(function Dragging(slice, move, node) {
    _classCallCheck(this, Dragging);
    this.slice = slice;
    this.move = move;
    this.node = node;
  }), dragCopyModifier = mac ? "altKey" : "ctrlKey";
  handlers.dragstart = function(view, _event) {
    var mouseDown = view.input.mouseDown;
    mouseDown && mouseDown.done();
    if (_event.dataTransfer) {
      var sel = view.state.selection, pos = sel.empty ? null : view.posAtCoords(eventCoords(_event));
      if (!(pos && pos.pos >= sel.from && pos.pos <= (sel instanceof prosemirrorState.NodeSelection ? sel.to - 1 : sel.to))) {
        if (mouseDown && mouseDown.mightDrag) {
          var node = prosemirrorState.NodeSelection.create(view.state.doc, mouseDown.mightDrag.pos);
        } else {
          _event.target && 1 == _event.target.nodeType && (mouseDown = view.docView.nearestDesc(_event.target, !0)) && mouseDown.node.type.spec.draggable && mouseDown != view.docView && (node = prosemirrorState.NodeSelection.create(view.state.doc, mouseDown.posBefore));
        }
      }
      mouseDown = (node || view.state.selection).content();
      pos = serializeForClipboard(view, mouseDown);
      mouseDown = pos.dom;
      sel = pos.text;
      pos = pos.slice;
      (!_event.dataTransfer.files.length || !chrome || 120 < chrome_version) && _event.dataTransfer.clearData();
      _event.dataTransfer.setData(brokenClipboardAPI ? "Text" : "text/html", mouseDown.innerHTML);
      _event.dataTransfer.effectAllowed = "copyMove";
      brokenClipboardAPI || _event.dataTransfer.setData("text/plain", sel);
      view.dragging = new Dragging(pos, !_event[dragCopyModifier], node);
    }
  };
  handlers.dragend = function(view) {
    var dragging = view.dragging;
    window.setTimeout(function() {
      view.dragging == dragging && (view.dragging = null);
    }, 50);
  };
  editHandlers.dragover = editHandlers.dragenter = function(_, e) {
    return e.preventDefault();
  };
  editHandlers.drop = function(view, _event) {
    var dragging = view.dragging;
    view.dragging = null;
    if (_event.dataTransfer) {
      var eventPos = view.posAtCoords(eventCoords(_event));
      if (eventPos) {
        var $mouse = view.state.doc.resolve(eventPos.pos), slice = dragging && dragging.slice;
        slice ? view.someProp("transformPasted", function(f) {
          slice = f(slice, view);
        }) : slice = parseFromClipboard(view, getText(_event.dataTransfer), brokenClipboardAPI ? null : _event.dataTransfer.getData("text/html"), !1, $mouse);
        var move = !(!dragging || _event[dragCopyModifier]);
        if (view.someProp("handleDrop", function(f) {
          return f(view, _event, slice || prosemirrorModel.Slice.empty, move);
        })) {
          _event.preventDefault();
        } else {
          if (slice) {
            _event.preventDefault();
            eventPos = slice ? prosemirrorTransform.dropPoint(view.state.doc, $mouse.pos, slice) : $mouse.pos;
            null == eventPos && (eventPos = $mouse.pos);
            $mouse = view.state.tr;
            move && ((dragging = dragging.node) ? dragging.replace($mouse) : $mouse.deleteSelection());
            var pos = $mouse.mapping.map(eventPos);
            dragging = 0 == slice.openStart && 0 == slice.openEnd && 1 == slice.content.childCount;
            var beforeInsert = $mouse.doc;
            dragging ? $mouse.replaceRangeWith(pos, pos, slice.content.firstChild) : $mouse.replaceRange(pos, pos, slice);
            if (!$mouse.doc.eq(beforeInsert)) {
              pos = $mouse.doc.resolve(pos);
              if (dragging && prosemirrorState.NodeSelection.isSelectable(slice.content.firstChild) && pos.nodeAfter && pos.nodeAfter.sameMarkup(slice.content.firstChild)) {
                $mouse.setSelection(new prosemirrorState.NodeSelection(pos));
              } else {
                var end = $mouse.mapping.map(eventPos);
                $mouse.mapping.maps[$mouse.mapping.maps.length - 1].forEach(function(_from, _to, _newFrom, newTo) {
                  return end = newTo;
                });
                $mouse.setSelection(selectionBetween(view, pos, $mouse.doc.resolve(end)));
              }
              view.focus();
              view.dispatch($mouse.setMeta("uiEvent", "drop"));
            }
          }
        }
      }
    }
  };
  handlers.focus = function(view) {
    view.input.lastFocus = Date.now();
    view.focused || (view.domObserver.stop(), view.dom.classList.add("ProseMirror-focused"), view.domObserver.start(), view.focused = !0, setTimeout(function() {
      view.docView && view.hasFocus() && !view.domObserver.currentSelection.eq(view.domSelectionRange()) && selectionToDOM(view);
    }, 20));
  };
  handlers.blur = function(view, _event) {
    view.focused && (view.domObserver.stop(), view.dom.classList.remove("ProseMirror-focused"), view.domObserver.start(), _event.relatedTarget && view.dom.contains(_event.relatedTarget) && view.domObserver.currentSelection.clear(), view.focused = !1);
  };
  handlers.beforeinput = function(view, _event) {
    if (chrome && android && "deleteContentBackward" == _event.inputType) {
      view.domObserver.flushSoon();
      var domChangeCount = view.input.domChangeCount;
      setTimeout(function() {
        if (view.input.domChangeCount == domChangeCount && (view.dom.blur(), view.focus(), !view.someProp("handleKeyDown", function(f) {
          return f(view, keyEvent(8, "Backspace"));
        }))) {
          var $cursor = view.state.selection.$cursor;
          $cursor && 0 < $cursor.pos && view.dispatch(view.state.tr["delete"]($cursor.pos - 1, $cursor.pos).scrollIntoView());
        }
      }, 50);
    }
  };
  for (var prop in editHandlers) {
    handlers[prop] = editHandlers[prop];
  }
  var WidgetType = function() {
    function WidgetType(toDOM, spec) {
      _classCallCheck(this, WidgetType);
      this.toDOM = toDOM;
      this.spec = spec || noSpec;
      this.side = this.spec.side || 0;
    }
    _createClass(WidgetType, [{key:"map", value:function(mapping, span, offset, oldOffset) {
      mapping = mapping.mapResult(span.from + oldOffset, 0 > this.side ? -1 : 1);
      span = mapping.pos;
      return mapping.deleted ? null : new Decoration(span - offset, span - offset, this);
    }}, {key:"valid", value:function() {
      return !0;
    }}, {key:"eq", value:function(other) {
      return this == other || other instanceof WidgetType && (this.spec.key && this.spec.key == other.spec.key || this.toDOM == other.toDOM && compareObjs(this.spec, other.spec));
    }}, {key:"destroy", value:function(node) {
      this.spec.destroy && this.spec.destroy(node);
    }}]);
    return WidgetType;
  }(), InlineType = function() {
    function InlineType(attrs, spec) {
      _classCallCheck(this, InlineType);
      this.attrs = attrs;
      this.spec = spec || noSpec;
    }
    _createClass(InlineType, [{key:"map", value:function(mapping, span, offset, oldOffset) {
      var from = mapping.map(span.from + oldOffset, this.spec.inclusiveStart ? -1 : 1) - offset;
      mapping = mapping.map(span.to + oldOffset, this.spec.inclusiveEnd ? 1 : -1) - offset;
      return from >= mapping ? null : new Decoration(from, mapping, this);
    }}, {key:"valid", value:function(_, span) {
      return span.from < span.to;
    }}, {key:"eq", value:function(other) {
      return this == other || other instanceof InlineType && compareObjs(this.attrs, other.attrs) && compareObjs(this.spec, other.spec);
    }}, {key:"destroy", value:function() {
    }}], [{key:"is", value:function(span) {
      return span.type instanceof InlineType;
    }}]);
    return InlineType;
  }(), NodeType = function() {
    function NodeType(attrs, spec) {
      _classCallCheck(this, NodeType);
      this.attrs = attrs;
      this.spec = spec || noSpec;
    }
    _createClass(NodeType, [{key:"map", value:function(mapping, span, offset, oldOffset) {
      var from = mapping.mapResult(span.from + oldOffset, 1);
      if (from.deleted) {
        return null;
      }
      mapping = mapping.mapResult(span.to + oldOffset, -1);
      return mapping.deleted || mapping.pos <= from.pos ? null : new Decoration(from.pos - offset, mapping.pos - offset, this);
    }}, {key:"valid", value:function(node, span) {
      var _node$content$findInd = node.content.findIndex(span.from), index = _node$content$findInd.index;
      _node$content$findInd = _node$content$findInd.offset;
      var child;
      return _node$content$findInd == span.from && !(child = node.child(index)).isText && _node$content$findInd + child.nodeSize == span.to;
    }}, {key:"eq", value:function(other) {
      return this == other || other instanceof NodeType && compareObjs(this.attrs, other.attrs) && compareObjs(this.spec, other.spec);
    }}, {key:"destroy", value:function() {
    }}]);
    return NodeType;
  }(), Decoration = function() {
    function Decoration(from, to, type) {
      _classCallCheck(this, Decoration);
      this.from = from;
      this.to = to;
      this.type = type;
    }
    _createClass(Decoration, [{key:"copy", value:function(from, to) {
      return new Decoration(from, to, this.type);
    }}, {key:"eq", value:function(other) {
      var offset = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 0;
      return this.type.eq(other.type) && this.from + offset == other.from && this.to + offset == other.to;
    }}, {key:"map", value:function(mapping, offset, oldOffset) {
      return this.type.map(mapping, this, offset, oldOffset);
    }}, {key:"spec", get:function() {
      return this.type.spec;
    }}, {key:"inline", get:function() {
      return this.type instanceof InlineType;
    }}, {key:"widget", get:function() {
      return this.type instanceof WidgetType;
    }}], [{key:"widget", value:function(pos, toDOM, spec) {
      return new Decoration(pos, pos, new WidgetType(toDOM, spec));
    }}, {key:"inline", value:function(from, to, attrs, spec) {
      return new Decoration(from, to, new InlineType(attrs, spec));
    }}, {key:"node", value:function(from, to, attrs, spec) {
      return new Decoration(from, to, new NodeType(attrs, spec));
    }}]);
    return Decoration;
  }(), none = [], noSpec = {}, DecorationSet = function() {
    function DecorationSet(local, children) {
      _classCallCheck(this, DecorationSet);
      this.local = local.length ? local : none;
      this.children = children.length ? children : none;
    }
    _createClass(DecorationSet, [{key:"find", value:function(start, end, predicate) {
      var result = [];
      this.findInner(null == start ? 0 : start, null == end ? 1e9 : end, result, 0, predicate);
      return result;
    }}, {key:"findInner", value:function(start, end, result, offset, predicate) {
      for (var i = 0; i < this.local.length; i++) {
        var span = this.local[i];
        span.from <= end && span.to >= start && (!predicate || predicate(span.spec)) && result.push(span.copy(span.from + offset, span.to + offset));
      }
      for (i = 0; i < this.children.length; i += 3) {
        this.children[i] < end && this.children[i + 1] > start && (span = this.children[i] + 1, this.children[i + 2].findInner(start - span, end - span, result, offset + span, predicate));
      }
    }}, {key:"map", value:function(mapping, doc, options) {
      return this == empty || 0 == mapping.maps.length ? this : this.mapInner(mapping, doc, 0, 0, options || noSpec);
    }}, {key:"mapInner", value:function(mapping, node, offset, oldOffset, options) {
      for (var newLocal, i = 0; i < this.local.length; i++) {
        var mapped = this.local[i].map(mapping, offset, oldOffset);
        if (mapped && mapped.type.valid(node, mapped)) {
          (newLocal || (newLocal = [])).push(mapped);
        } else if (options.onRemove) {
          options.onRemove(this.local[i].spec);
        }
      }
      return this.children.length ? mapChildren(this.children, newLocal || [], mapping, node, offset, oldOffset, options) : newLocal ? new DecorationSet(newLocal.sort(byPos), none) : empty;
    }}, {key:"add", value:function(doc, decorations) {
      return decorations.length ? this == empty ? DecorationSet.create(doc, decorations) : this.addInner(doc, decorations, 0) : this;
    }}, {key:"addInner", value:function(doc, decorations, offset) {
      var _this10 = this, children, childIndex = 0;
      doc.forEach(function(childNode, childOffset) {
        var baseOffset = childOffset + offset, found;
        if (found = takeSpansForNode(decorations, childNode, baseOffset)) {
          for (children || (children = _this10.children.slice()); childIndex < children.length && children[childIndex] < childOffset;) {
            childIndex += 3;
          }
          children[childIndex] == childOffset ? children[childIndex + 2] = children[childIndex + 2].addInner(childNode, found, baseOffset + 1) : children.splice(childIndex, 0, childOffset, childOffset + childNode.nodeSize, buildTree(found, childNode, baseOffset + 1, noSpec));
          childIndex += 3;
        }
      });
      for (var local = moveSpans(childIndex ? withoutNulls(decorations) : decorations, -offset), i = 0; i < local.length; i++) {
        local[i].type.valid(doc, local[i]) || local.splice(i--, 1);
      }
      return new DecorationSet(local.length ? this.local.concat(local).sort(byPos) : this.local, children || this.children);
    }}, {key:"remove", value:function(decorations) {
      return 0 == decorations.length || this == empty ? this : this.removeInner(decorations, 0);
    }}, {key:"removeInner", value:function(decorations, offset) {
      for (var children = this.children, local = this.local, i = 0; i < children.length; i += 3) {
        for (var found = void 0, from = children[i] + offset, to = children[i + 1] + offset, j = 0, span; j < decorations.length; j++) {
          (span = decorations[j]) && span.from > from && span.to < to && (decorations[j] = null, (found || (found = [])).push(span));
        }
        found && (children == this.children && (children = this.children.slice()), found = children[i + 2].removeInner(found, from + 1), found != empty ? children[i + 2] = found : (children.splice(i, 3), i -= 3));
      }
      if (local.length) {
        for (i = 0; i < decorations.length; i++) {
          if (span = decorations[i]) {
            for (found = 0; found < local.length; found++) {
              local[found].eq(span, offset) && (local == this.local && (local = this.local.slice()), local.splice(found--, 1));
            }
          }
        }
      }
      return children == this.children && local == this.local ? this : local.length || children.length ? new DecorationSet(local, children) : empty;
    }}, {key:"forChild", value:function(offset, node) {
      if (this == empty) {
        return this;
      }
      if (node.isLeaf) {
        return DecorationSet.empty;
      }
      for (var child, local, i = 0; i < this.children.length; i += 3) {
        if (this.children[i] >= offset) {
          this.children[i] == offset && (child = this.children[i + 2]);
          break;
        }
      }
      offset += 1;
      node = offset + node.content.size;
      for (i = 0; i < this.local.length; i++) {
        var dec = this.local[i];
        if (dec.from < node && dec.to > offset && dec.type instanceof InlineType) {
          var from = Math.max(offset, dec.from) - offset, to = Math.min(node, dec.to) - offset;
          from < to && (local || (local = [])).push(dec.copy(from, to));
        }
      }
      return local ? (local = new DecorationSet(local.sort(byPos), none), child ? new DecorationGroup([local, child]) : local) : child || empty;
    }}, {key:"eq", value:function(other) {
      if (this == other) {
        return !0;
      }
      if (!(other instanceof DecorationSet) || this.local.length != other.local.length || this.children.length != other.children.length) {
        return !1;
      }
      for (var i = 0; i < this.local.length; i++) {
        if (!this.local[i].eq(other.local[i])) {
          return !1;
        }
      }
      for (i = 0; i < this.children.length; i += 3) {
        if (this.children[i] != other.children[i] || this.children[i + 1] != other.children[i + 1] || !this.children[i + 2].eq(other.children[i + 2])) {
          return !1;
        }
      }
      return !0;
    }}, {key:"locals", value:function(node) {
      return removeOverlap(this.localsInner(node));
    }}, {key:"localsInner", value:function(node) {
      if (this == empty) {
        return none;
      }
      if (node.inlineContent || !this.local.some(InlineType.is)) {
        return this.local;
      }
      node = [];
      for (var i = 0; i < this.local.length; i++) {
        this.local[i].type instanceof InlineType || node.push(this.local[i]);
      }
      return node;
    }}, {key:"forEachSet", value:function(f) {
      f(this);
    }}], [{key:"create", value:function(doc, decorations) {
      return decorations.length ? buildTree(decorations, doc, 0, noSpec) : empty;
    }}]);
    return DecorationSet;
  }();
  DecorationSet.empty = new DecorationSet([], []);
  DecorationSet.removeOverlap = removeOverlap;
  var empty = DecorationSet.empty, DecorationGroup = function() {
    function DecorationGroup(members) {
      _classCallCheck(this, DecorationGroup);
      this.members = members;
    }
    _createClass(DecorationGroup, [{key:"map", value:function(mapping, doc) {
      var mappedDecos = this.members.map(function(member) {
        return member.map(mapping, doc, noSpec);
      });
      return DecorationGroup.from(mappedDecos);
    }}, {key:"forChild", value:function(offset, child) {
      if (child.isLeaf) {
        return DecorationSet.empty;
      }
      for (var found = [], i = 0; i < this.members.length; i++) {
        var result = this.members[i].forChild(offset, child);
        result != empty && (result instanceof DecorationGroup ? found = found.concat(result.members) : found.push(result));
      }
      return DecorationGroup.from(found);
    }}, {key:"eq", value:function(other) {
      if (!(other instanceof DecorationGroup) || other.members.length != this.members.length) {
        return !1;
      }
      for (var i = 0; i < this.members.length; i++) {
        if (!this.members[i].eq(other.members[i])) {
          return !1;
        }
      }
      return !0;
    }}, {key:"locals", value:function(node) {
      for (var result, sorted = !0, i = 0; i < this.members.length; i++) {
        var locals = this.members[i].localsInner(node);
        if (locals.length) {
          if (result) {
            sorted && (result = result.slice(), sorted = !1);
            for (var j = 0; j < locals.length; j++) {
              result.push(locals[j]);
            }
          } else {
            result = locals;
          }
        }
      }
      return result ? removeOverlap(sorted ? result : result.sort(byPos)) : none;
    }}, {key:"forEachSet", value:function(f) {
      for (var i = 0; i < this.members.length; i++) {
        this.members[i].forEachSet(f);
      }
    }}], [{key:"from", value:function(members) {
      switch(members.length) {
        case 0:
          return empty;
        case 1:
          return members[0];
        default:
          return new DecorationGroup(members.every(function(m) {
            return m instanceof DecorationSet;
          }) ? members : members.reduce(function(r, m) {
            return r.concat(m instanceof DecorationSet ? m : m.members);
          }, []));
      }
    }}]);
    return DecorationGroup;
  }(), observeOptions = {childList:!0, characterData:!0, characterDataOldValue:!0, attributes:!0, attributeOldValue:!0, subtree:!0}, useCharData = ie && 11 >= ie_version, SelectionState = function() {
    function SelectionState() {
      _classCallCheck(this, SelectionState);
      this.anchorNode = null;
      this.anchorOffset = 0;
      this.focusNode = null;
      this.focusOffset = 0;
    }
    _createClass(SelectionState, [{key:"set", value:function(sel) {
      this.anchorNode = sel.anchorNode;
      this.anchorOffset = sel.anchorOffset;
      this.focusNode = sel.focusNode;
      this.focusOffset = sel.focusOffset;
    }}, {key:"clear", value:function() {
      this.anchorNode = this.focusNode = null;
    }}, {key:"eq", value:function(sel) {
      return sel.anchorNode == this.anchorNode && sel.anchorOffset == this.anchorOffset && sel.focusNode == this.focusNode && sel.focusOffset == this.focusOffset;
    }}]);
    return SelectionState;
  }(), DOMObserver = function() {
    function DOMObserver(view, handleDOMChange) {
      var _this11 = this;
      _classCallCheck(this, DOMObserver);
      this.view = view;
      this.handleDOMChange = handleDOMChange;
      this.queue = [];
      this.flushingSoon = -1;
      this.observer = null;
      this.currentSelection = new SelectionState();
      this.onCharData = null;
      this.suppressingSelectionUpdates = !1;
      this.lastChangedTextNode = null;
      this.observer = window.MutationObserver && new window.MutationObserver(function(mutations) {
        for (var i = 0; i < mutations.length; i++) {
          _this11.queue.push(mutations[i]);
        }
        ie && 11 >= ie_version && mutations.some(function(m) {
          return "childList" == m.type && m.removedNodes.length || "characterData" == m.type && m.oldValue.length > m.target.nodeValue.length;
        }) ? _this11.flushSoon() : _this11.flush();
      });
      useCharData && (this.onCharData = function(e) {
        _this11.queue.push({target:e.target, type:"characterData", oldValue:e.prevValue});
        _this11.flushSoon();
      });
      this.onSelectionChange = this.onSelectionChange.bind(this);
    }
    _createClass(DOMObserver, [{key:"flushSoon", value:function() {
      var _this12 = this;
      0 > this.flushingSoon && (this.flushingSoon = window.setTimeout(function() {
        _this12.flushingSoon = -1;
        _this12.flush();
      }, 20));
    }}, {key:"forceFlush", value:function() {
      -1 < this.flushingSoon && (window.clearTimeout(this.flushingSoon), this.flushingSoon = -1, this.flush());
    }}, {key:"start", value:function() {
      this.observer && (this.observer.takeRecords(), this.observer.observe(this.view.dom, observeOptions));
      this.onCharData && this.view.dom.addEventListener("DOMCharacterDataModified", this.onCharData);
      this.connectSelection();
    }}, {key:"stop", value:function() {
      var _this13 = this;
      if (this.observer) {
        var take = this.observer.takeRecords();
        if (take.length) {
          for (var i = 0; i < take.length; i++) {
            this.queue.push(take[i]);
          }
          window.setTimeout(function() {
            return _this13.flush();
          }, 20);
        }
        this.observer.disconnect();
      }
      this.onCharData && this.view.dom.removeEventListener("DOMCharacterDataModified", this.onCharData);
      this.disconnectSelection();
    }}, {key:"connectSelection", value:function() {
      this.view.dom.ownerDocument.addEventListener("selectionchange", this.onSelectionChange);
    }}, {key:"disconnectSelection", value:function() {
      this.view.dom.ownerDocument.removeEventListener("selectionchange", this.onSelectionChange);
    }}, {key:"suppressSelectionUpdates", value:function() {
      var _this14 = this;
      this.suppressingSelectionUpdates = !0;
      setTimeout(function() {
        return _this14.suppressingSelectionUpdates = !1;
      }, 50);
    }}, {key:"onSelectionChange", value:function() {
      if (hasFocusAndSelection(this.view)) {
        if (this.suppressingSelectionUpdates) {
          return selectionToDOM(this.view);
        }
        if (ie && 11 >= ie_version && !this.view.state.selection.empty) {
          var sel = this.view.domSelectionRange();
          if (sel.focusNode && isEquivalentPosition(sel.focusNode, sel.focusOffset, sel.anchorNode, sel.anchorOffset)) {
            return this.flushSoon();
          }
        }
        this.flush();
      }
    }}, {key:"setCurSelection", value:function() {
      this.currentSelection.set(this.view.domSelectionRange());
    }}, {key:"ignoreSelectionChange", value:function(sel) {
      if (!sel.focusNode) {
        return !0;
      }
      for (var ancestors = new Set(), container, scan = sel.focusNode; scan; scan = parentNode(scan)) {
        ancestors.add(scan);
      }
      for (sel = sel.anchorNode; sel; sel = parentNode(sel)) {
        if (ancestors.has(sel)) {
          container = sel;
          break;
        }
      }
      if ((ancestors = container && this.view.docView.nearestDesc(container)) && ancestors.ignoreMutation({type:"selection", target:3 == container.nodeType ? container.parentNode : container})) {
        return this.setCurSelection(), !0;
      }
    }}, {key:"pendingRecords", value:function() {
      if (this.observer) {
        var _iterator2 = _createForOfIteratorHelper(this.observer.takeRecords()), _step2;
        try {
          for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
            this.queue.push(_step2.value);
          }
        } catch (err) {
          _iterator2.e(err);
        } finally {
          _iterator2.f();
        }
      }
      return this.queue;
    }}, {key:"flush", value:function() {
      var view = this.view;
      if (view.docView && !(-1 < this.flushingSoon)) {
        var mutations = this.pendingRecords();
        mutations.length && (this.queue = []);
        var sel = view.domSelectionRange(), newSel = !this.suppressingSelectionUpdates && !this.currentSelection.eq(sel) && hasFocusAndSelection(view) && !this.ignoreSelectionChange(sel), from = -1, to = -1, typeOver = !1, added = [];
        if (view.editable) {
          for (var i = 0; i < mutations.length; i++) {
            var result = this.registerMutation(mutations[i], added);
            result && (from = 0 > from ? result.from : Math.min(result.from, from), to = 0 > to ? result.to : Math.max(result.to, to), result.typeOver && (typeOver = !0));
          }
        }
        if (gecko && added.length) {
          if (i = added.filter(function(n) {
            return "BR" == n.nodeName;
          }), 2 == i.length) {
            var _brs = _slicedToArray(i, 2), a = _brs[0];
            _brs = _brs[1];
            a.parentNode && a.parentNode.parentNode == _brs.parentNode ? _brs.remove() : a.remove();
          } else {
            mutations = this.currentSelection.focusNode;
            i = _createForOfIteratorHelper(i);
            try {
              for (i.s(); !(a = i.n()).done;) {
                _brs = a.value;
                var parent = _brs.parentNode;
                !parent || "LI" != parent.nodeName || mutations && blockParent(view, mutations) == parent || _brs.remove();
              }
            } catch (err) {
              i.e(err);
            } finally {
              i.f();
            }
          }
        }
        a = null;
        if (0 > from && newSel && view.input.lastFocus > Date.now() - 200 && Math.max(view.input.lastTouch, view.input.lastClick.time) < Date.now() - 300 && selectionCollapsed(sel) && (a = selectionFromDOM(view)) && a.eq(prosemirrorState.Selection.near(view.state.doc.resolve(0), 1))) {
          view.input.lastFocus = 0, selectionToDOM(view), this.currentSelection.set(sel), view.scrollToSelection();
        } else if (-1 < from || newSel) {
          -1 < from && (view.docView.markDirty(from, to), checkCSS(view)), this.handleDOMChange(from, to, typeOver, added), view.docView && view.docView.dirty ? view.updateState(view.state) : this.currentSelection.eq(sel) || selectionToDOM(view), this.currentSelection.set(sel);
        }
      }
    }}, {key:"registerMutation", value:function(mut, added) {
      if (-1 < added.indexOf(mut.target)) {
        return null;
      }
      var desc = this.view.docView.nearestDesc(mut.target);
      if ("attributes" == mut.type && (desc == this.view.docView || "contenteditable" == mut.attributeName || "style" == mut.attributeName && !mut.oldValue && !mut.target.getAttribute("style")) || !desc || desc.ignoreMutation(mut)) {
        return null;
      }
      if ("childList" == mut.type) {
        for (var i = 0; i < mut.addedNodes.length; i++) {
          var node = mut.addedNodes[i];
          added.push(node);
          3 == node.nodeType && (this.lastChangedTextNode = node);
        }
        if (desc.contentDOM && desc.contentDOM != desc.dom && !desc.contentDOM.contains(mut.target)) {
          return {from:desc.posBefore, to:desc.posAfter};
        }
        i = mut.previousSibling;
        added = mut.nextSibling;
        if (ie && 11 >= ie_version && mut.addedNodes.length) {
          for (node = 0; node < mut.addedNodes.length; node++) {
            var _mut$addedNodes$_i = mut.addedNodes[node], previousSibling = _mut$addedNodes$_i.previousSibling;
            _mut$addedNodes$_i = _mut$addedNodes$_i.nextSibling;
            if (!previousSibling || 0 > Array.prototype.indexOf.call(mut.addedNodes, previousSibling)) {
              i = previousSibling;
            }
            if (!_mut$addedNodes$_i || 0 > Array.prototype.indexOf.call(mut.addedNodes, _mut$addedNodes$_i)) {
              added = _mut$addedNodes$_i;
            }
          }
        }
        i = i && i.parentNode == mut.target ? domIndex(i) + 1 : 0;
        i = desc.localPosFromDOM(mut.target, i, -1);
        added = added && added.parentNode == mut.target ? domIndex(added) : mut.target.childNodes.length;
        mut = desc.localPosFromDOM(mut.target, added, 1);
        return {from:i, to:mut};
      }
      if ("attributes" == mut.type) {
        return {from:desc.posAtStart - desc.border, to:desc.posAtEnd + desc.border};
      }
      this.lastChangedTextNode = mut.target;
      return {from:desc.posAtStart, to:desc.posAtEnd, typeOver:mut.target.nodeValue == mut.oldValue};
    }}]);
    return DOMObserver;
  }(), cssChecked = new WeakMap(), cssCheckWarned = !1, isInline = /^(a|abbr|acronym|b|bd[io]|big|br|button|cite|code|data(list)?|del|dfn|em|i|ins|kbd|label|map|mark|meter|output|q|ruby|s|samp|small|span|strong|su[bp]|time|u|tt|var)$/i, __endComposition = endComposition, EditorView = function() {
    function EditorView(place, props) {
      var _this15 = this;
      _classCallCheck(this, EditorView);
      this._root = null;
      this.focused = !1;
      this.trackWrites = null;
      this.mounted = !1;
      this.cursorWrapper = this.markCursor = null;
      this.lastSelectedViewDesc = void 0;
      this.input = new InputState();
      this.prevDirectPlugins = [];
      this.pluginViews = [];
      this.requiresGeckoHackNode = !1;
      this.dragging = null;
      this._props = props;
      this.state = props.state;
      this.directPlugins = props.plugins || [];
      this.directPlugins.forEach(checkStateComponent);
      this.dispatch = this.dispatch.bind(this);
      this.dom = place && place.mount || document.createElement("div");
      place && (place.appendChild ? place.appendChild(this.dom) : "function" == typeof place ? place(this.dom) : place.mount && (this.mounted = !0));
      this.editable = getEditable(this);
      updateCursorWrapper(this);
      this.nodeViews = buildNodeViews(this);
      this.docView = docViewDesc(this.state.doc, computeDocDeco(this), viewDecorations(this), this.dom, this);
      this.domObserver = new DOMObserver(this, function(from, to, typeOver, added) {
        return readDOMChange(_this15, from, to, typeOver, added);
      });
      this.domObserver.start();
      initInput(this);
      this.updatePluginViews();
    }
    _createClass(EditorView, [{key:"composing", get:function() {
      return this.input.composing;
    }}, {key:"props", get:function() {
      if (this._props.state != this.state) {
        var prev = this._props;
        this._props = {};
        for (var name in prev) {
          this._props[name] = prev[name];
        }
        this._props.state = this.state;
      }
      return this._props;
    }}, {key:"update", value:function(props) {
      props.handleDOMEvents != this._props.handleDOMEvents && ensureListeners(this);
      var prevProps = this._props;
      this._props = props;
      props.plugins && (props.plugins.forEach(checkStateComponent), this.directPlugins = props.plugins);
      this.updateStateInner(props.state, prevProps);
    }}, {key:"setProps", value:function(props) {
      var updated = {}, name;
      for (name in this._props) {
        updated[name] = this._props[name];
      }
      updated.state = this.state;
      for (var _name2 in props) {
        updated[_name2] = props[_name2];
      }
      this.update(updated);
    }}, {key:"updateState", value:function(state) {
      this.updateStateInner(state, this._props);
    }}, {key:"updateStateInner", value:function(state, prevProps) {
      var _a, prev = this.state, redraw = !1, updateSel = !1;
      state.storedMarks && this.composing && (clearComposition(this), updateSel = !0);
      this.state = state;
      var pluginsChanged = prev.plugins != state.plugins || this._props.plugins != prevProps.plugins;
      if (pluginsChanged || this._props.plugins != prevProps.plugins || this._props.nodeViews != prevProps.nodeViews) {
        var nodeViews = buildNodeViews(this);
        a: {
          var JSCompiler_inline_result = this.nodeViews;
          var nA = 0, nB = 0;
          for (_prop4 in nodeViews) {
            if (nodeViews[_prop4] != JSCompiler_inline_result[_prop4]) {
              JSCompiler_inline_result = !0;
              break a;
            }
            nA++;
          }
          for (var _ in JSCompiler_inline_result) {
            nB++;
          }
          JSCompiler_inline_result = nA != nB;
        }
        JSCompiler_inline_result && (this.nodeViews = nodeViews, redraw = !0);
      }
      (pluginsChanged || prevProps.handleDOMEvents != this._props.handleDOMEvents) && ensureListeners(this);
      this.editable = getEditable(this);
      updateCursorWrapper(this);
      pluginsChanged = viewDecorations(this);
      nodeViews = computeDocDeco(this);
      prevProps = prev.plugins == state.plugins || prev.doc.eq(state.doc) ? state.scrollToSelection > prev.scrollToSelection ? "to selection" : "preserve" : "reset";
      if ((JSCompiler_inline_result = redraw || !this.docView.matchesNode(state.doc, nodeViews, pluginsChanged)) || !state.selection.eq(prev.selection)) {
        updateSel = !0;
      }
      if (nA = "preserve" == prevProps && updateSel && null == this.dom.style.overflowAnchor) {
        nA = this.dom.getBoundingClientRect();
        nB = Math.max(0, nA.top);
        var _prop4 = (nA.left + nA.right) / 2;
        for (_ = nB + 1; _ < Math.min(innerHeight, nA.bottom); _ += 5) {
          var dom = this.root.elementFromPoint(_prop4, _);
          if (dom && dom != this.dom && this.dom.contains(dom)) {
            var localRect = dom.getBoundingClientRect();
            if (localRect.top >= nB - 20) {
              var refDOM = dom;
              var refTop = localRect.top;
              break;
            }
          }
        }
        nA = {refDOM, refTop, stack:scrollStack(this.dom)};
      }
      refDOM = nA;
      if (updateSel) {
        this.domObserver.stop();
        if (updateSel = JSCompiler_inline_result && (ie || chrome) && !this.composing && !prev.selection.empty && !state.selection.empty) {
          updateSel = prev.selection, refTop = state.selection, nA = Math.min(updateSel.$anchor.sharedDepth(updateSel.head), refTop.$anchor.sharedDepth(refTop.head)), updateSel = updateSel.$anchor.start(nA) != refTop.$anchor.start(nA);
        }
        if (JSCompiler_inline_result) {
          refTop = chrome ? this.trackWrites = this.domSelectionRange().focusNode : null;
          this.composing && (this.input.compositionNode = findCompositionNode(this));
          if (redraw || !this.docView.update(state.doc, nodeViews, pluginsChanged, this)) {
            this.docView.updateOuterDeco(nodeViews), this.docView.destroy(), this.docView = docViewDesc(state.doc, nodeViews, pluginsChanged, this.dom, this);
          }
          refTop && !this.trackWrites && (updateSel = !0);
        }
        if (!(redraw = updateSel)) {
          if (redraw = this.input.mouseDown && this.domObserver.currentSelection.eq(this.domSelectionRange())) {
            redraw = this.docView.domFromPos(this.state.selection.anchor, 0), refTop = this.domSelectionRange(), redraw = isEquivalentPosition(redraw.node, redraw.offset, refTop.anchorNode, refTop.anchorOffset);
          }
          redraw = !redraw;
        }
        redraw ? selectionToDOM(this, updateSel) : (syncNodeSelection(this, state.selection), this.domObserver.setCurSelection());
        this.domObserver.start();
      }
      this.updatePluginViews(prev);
      (null === (_a = this.dragging) || void 0 === _a ? 0 : _a.node) && !prev.doc.eq(state.doc) && this.updateDraggedNode(this.dragging, prev);
      "reset" == prevProps ? this.dom.scrollTop = 0 : "to selection" == prevProps ? this.scrollToSelection() : refDOM && (_a = refDOM.refDOM, state = refDOM.refTop, prev = refDOM.stack, _a = _a ? _a.getBoundingClientRect().top : 0, restoreScrollStack(prev, 0 == _a ? 0 : _a - state));
    }}, {key:"scrollToSelection", value:function() {
      var _this16 = this, startDOM = this.domSelectionRange().focusNode;
      if (!this.someProp("handleScrollToSelection", function(f) {
        return f(_this16);
      })) {
        if (this.state.selection instanceof prosemirrorState.NodeSelection) {
          var target = this.docView.domAfterPos(this.state.selection.from);
          1 == target.nodeType && scrollRectIntoView(this, target.getBoundingClientRect(), startDOM);
        } else {
          scrollRectIntoView(this, this.coordsAtPos(this.state.selection.head, 1), startDOM);
        }
      }
    }}, {key:"destroyPluginViews", value:function() {
      for (var view; view = this.pluginViews.pop();) {
        view.destroy && view.destroy();
      }
    }}, {key:"updatePluginViews", value:function(prevState) {
      if (prevState && prevState.plugins == this.state.plugins && this.directPlugins == this.prevDirectPlugins) {
        for (var _i18 = 0; _i18 < this.pluginViews.length; _i18++) {
          var pluginView = this.pluginViews[_i18];
          pluginView.update && pluginView.update(this, prevState);
        }
      } else {
        this.prevDirectPlugins = this.directPlugins;
        this.destroyPluginViews();
        for (prevState = 0; prevState < this.directPlugins.length; prevState++) {
          _i18 = this.directPlugins[prevState], _i18.spec.view && this.pluginViews.push(_i18.spec.view(this));
        }
        for (prevState = 0; prevState < this.state.plugins.length; prevState++) {
          _i18 = this.state.plugins[prevState], _i18.spec.view && this.pluginViews.push(_i18.spec.view(this));
        }
      }
    }}, {key:"updateDraggedNode", value:function(dragging, prev) {
      var sel = dragging.node, found = -1;
      this.state.doc.nodeAt(sel.from) == sel.node ? found = sel.from : (prev = sel.from + (this.state.doc.content.size - prev.doc.content.size), (0 < prev && this.state.doc.nodeAt(prev)) == sel.node && (found = prev));
      this.dragging = new Dragging(dragging.slice, dragging.move, 0 > found ? void 0 : prosemirrorState.NodeSelection.create(this.state.doc, found));
    }}, {key:"someProp", value:function(propName, f) {
      var prop = this._props && this._props[propName], value;
      if (null != prop && (value = f ? f(prop) : prop)) {
        return value;
      }
      for (prop = 0; prop < this.directPlugins.length; prop++) {
        var _prop = this.directPlugins[prop].props[propName];
        if (null != _prop && (value = f ? f(_prop) : _prop)) {
          return value;
        }
      }
      if (prop = this.state.plugins) {
        for (_prop = 0; _prop < prop.length; _prop++) {
          var _prop2 = prop[_prop].props[propName];
          if (null != _prop2 && (value = f ? f(_prop2) : _prop2)) {
            return value;
          }
        }
      }
    }}, {key:"hasFocus", value:function() {
      if (ie) {
        var node = this.root.activeElement;
        if (node == this.dom) {
          return !0;
        }
        if (!node || !this.dom.contains(node)) {
          return !1;
        }
        for (; node && this.dom != node && this.dom.contains(node);) {
          if ("false" == node.contentEditable) {
            return !1;
          }
          node = node.parentElement;
        }
        return !0;
      }
      return this.root.activeElement == this.dom;
    }}, {key:"focus", value:function() {
      this.domObserver.stop();
      this.editable && focusPreventScroll(this.dom);
      selectionToDOM(this);
      this.domObserver.start();
    }}, {key:"root", get:function() {
      var _this17 = this, cached = this._root;
      if (null == cached) {
        for (var _loop5 = function(search) {
          if (9 == search.nodeType || 11 == search.nodeType && search.host) {
            return search.getSelection || (Object.getPrototypeOf(search).getSelection = function() {
              return search.ownerDocument.getSelection();
            }), {v:_this17._root = search};
          }
        }, _ret3, search = this.dom.parentNode; search; search = search.parentNode) {
          if (_ret3 = _loop5(search)) {
            return _ret3.v;
          }
        }
      }
      return cached || document;
    }}, {key:"updateRoot", value:function() {
      this._root = null;
    }}, {key:"posAtCoords", value:function(coords) {
      a: {
        var doc = this.dom.ownerDocument, offset = 0;
        b: {
          var x = coords.left, y = coords.top;
          if (doc.caretPositionFromPoint) {
            try {
              var pos = doc.caretPositionFromPoint(x, y);
              if (pos) {
                var caret = {node:pos.offsetNode, offset:Math.min(nodeSize(pos.offsetNode), pos.offset)};
                break b;
              }
            } catch (_) {
            }
          }
          if (doc.caretRangeFromPoint && (caret = doc.caretRangeFromPoint(x, y))) {
            caret = {node:caret.startContainer, offset:Math.min(nodeSize(caret.startContainer), caret.startOffset)};
            break b;
          }
          caret = void 0;
        }
        if (caret) {
          var node = caret.node;
          offset = caret.offset;
        }
        doc = (this.root.elementFromPoint ? this.root : doc).elementFromPoint(coords.left, coords.top);
        if (!doc || !this.dom.contains(1 != doc.nodeType ? doc.parentNode : doc)) {
          doc = this.dom.getBoundingClientRect();
          if (!inRect(coords, doc)) {
            coords = null;
            break a;
          }
          doc = elementFromPoint(this.dom, coords, doc);
          if (!doc) {
            coords = null;
            break a;
          }
        }
        if (safari) {
          for (caret = doc; node && caret; caret = parentNode(caret)) {
            caret.draggable && (node = void 0);
          }
        }
        doc = (caret = doc.parentNode) && /^li$/i.test(caret.nodeName) && coords.left < doc.getBoundingClientRect().left ? caret : doc;
        if (node) {
          if (gecko && 1 == node.nodeType && (offset = Math.min(offset, node.childNodes.length), offset < node.childNodes.length)) {
            caret = node.childNodes[offset];
            var _box;
            "IMG" == caret.nodeName && (_box = caret.getBoundingClientRect()).right <= coords.left && _box.bottom > coords.top && offset++;
          }
          var prev;
          webkit && offset && 1 == node.nodeType && 1 == (prev = node.childNodes[offset - 1]).nodeType && "false" == prev.contentEditable && prev.getBoundingClientRect().top >= coords.top && offset--;
          if (node == this.dom && offset == node.childNodes.length - 1 && 1 == node.lastChild.nodeType && coords.top > node.lastChild.getBoundingClientRect().bottom) {
            var pos$jscomp$0 = this.state.doc.content.size;
          } else if (0 == offset || 1 != node.nodeType || "BR" != node.childNodes[offset - 1].nodeName) {
            b: {
              pos$jscomp$0 = node;
              node = -1;
              prev = pos$jscomp$0;
              for (_box = !1; prev != this.dom;) {
                prev = this.docView.nearestDesc(prev, !0);
                if (!prev) {
                  pos$jscomp$0 = null;
                  break b;
                }
                if (1 == prev.dom.nodeType && (prev.node.isBlock && prev.parent || !prev.contentDOM)) {
                  caret = prev.dom.getBoundingClientRect();
                  if (prev.node.isBlock && prev.parent) {
                    if (!_box && caret.left > coords.left || caret.top > coords.top) {
                      node = prev.posBefore;
                    } else if (!_box && caret.right < coords.left || caret.bottom < coords.top) {
                      node = prev.posAfter;
                    }
                    _box = !0;
                  }
                  if (!prev.contentDOM && 0 > node && !prev.node.isText) {
                    pos$jscomp$0 = (prev.node.isBlock ? coords.top < (caret.top + caret.bottom) / 2 : coords.left < (caret.left + caret.right) / 2) ? prev.posBefore : prev.posAfter;
                    break b;
                  }
                }
                prev = prev.dom.parentNode;
              }
              pos$jscomp$0 = -1 < node ? node : this.docView.posFromDOM(pos$jscomp$0, offset, -1);
            }
          }
        }
        null == pos$jscomp$0 && (pos$jscomp$0 = findOffsetInNode(doc, coords), offset = pos$jscomp$0.node, pos$jscomp$0 = pos$jscomp$0.offset, node = -1, 1 != offset.nodeType || offset.firstChild || (node = offset.getBoundingClientRect(), node = node.left != node.right && coords.left > (node.left + node.right) / 2 ? 1 : -1), pos$jscomp$0 = this.docView.posFromDOM(offset, pos$jscomp$0, node));
        coords = this.docView.nearestDesc(doc, !0);
        coords = {pos:pos$jscomp$0, inside:coords ? coords.posAtStart - coords.border : -1};
      }
      return coords;
    }}, {key:"coordsAtPos", value:function(pos) {
      return _coordsAtPos(this, pos, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 1);
    }}, {key:"domAtPos", value:function(pos) {
      return this.docView.domFromPos(pos, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 0);
    }}, {key:"nodeDOM", value:function(pos) {
      return (pos = this.docView.descAt(pos)) ? pos.nodeDOM : null;
    }}, {key:"posAtDOM", value:function(node, offset) {
      var pos = this.docView.posFromDOM(node, offset, 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : -1);
      if (null == pos) {
        throw new RangeError("DOM position not inside the editor");
      }
      return pos;
    }}, {key:"endOfTextblock", value:function(dir, state) {
      state = state || this.state;
      cachedState == state && cachedDir == dir ? dir = cachedResult : (cachedState = state, cachedDir = dir, dir = cachedResult = "up" == dir || "down" == dir ? endOfTextblockVertical(this, state, dir) : endOfTextblockHorizontal(this, state, dir));
      return dir;
    }}, {key:"pasteHTML", value:function(html, event) {
      return doPaste(this, "", html, !1, event || new ClipboardEvent("paste"));
    }}, {key:"pasteText", value:function(text, event) {
      return doPaste(this, text, null, !0, event || new ClipboardEvent("paste"));
    }}, {key:"destroy", value:function() {
      if (this.docView) {
        this.domObserver.stop();
        for (var type in this.input.eventHandlers) {
          this.dom.removeEventListener(type, this.input.eventHandlers[type]);
        }
        clearTimeout(this.input.composingTimeout);
        clearTimeout(this.input.lastIOSEnterFallbackTimeout);
        this.destroyPluginViews();
        this.mounted ? (this.docView.update(this.state.doc, [], viewDecorations(this), this), this.dom.textContent = "") : this.dom.parentNode && this.dom.parentNode.removeChild(this.dom);
        this.docView.destroy();
        reusedRange = this.docView = null;
      }
    }}, {key:"isDestroyed", get:function() {
      return null == this.docView;
    }}, {key:"dispatchEvent", value:function(event) {
      if (!(runCustomHandler(this, event) || !handlers[event.type] || !this.editable && event.type in editHandlers)) {
        handlers[event.type](this, event);
      }
    }}, {key:"dispatch", value:function(tr) {
      var dispatchTransaction = this._props.dispatchTransaction;
      dispatchTransaction ? dispatchTransaction.call(this, tr) : this.updateState(this.state.apply(tr));
    }}, {key:"domSelectionRange", value:function() {
      var sel = this.domSelection();
      if (!sel) {
        return {focusNode:null, focusOffset:0, anchorNode:null, anchorOffset:0};
      }
      var JSCompiler_temp;
      if (JSCompiler_temp = safari && 11 === this.root.nodeType) {
        for (JSCompiler_temp = this.dom.ownerDocument.activeElement; JSCompiler_temp && JSCompiler_temp.shadowRoot;) {
          JSCompiler_temp = JSCompiler_temp.shadowRoot.activeElement;
        }
        JSCompiler_temp = JSCompiler_temp == this.dom;
      }
      return JSCompiler_temp && safariShadowSelectionRange(this, sel) || sel;
    }}, {key:"domSelection", value:function() {
      return this.root.getSelection();
    }}]);
    return EditorView;
  }();
  exports.Decoration = Decoration;
  exports.DecorationSet = DecorationSet;
  exports.EditorView = EditorView;
  exports.__endComposition = __endComposition;
  exports.__parseFromClipboard = parseFromClipboard;
  exports.__serializeForClipboard = serializeForClipboard;
};

//# sourceMappingURL=module$node_modules$prosemirror_view$dist$index_cjs.js.map
