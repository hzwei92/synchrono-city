shadow$provide.module$node_modules$nostr_tools$node_modules$$noble$hashes$hkdf = function(global, require, module, exports) {
  function extract(hash, ikm, salt) {
    _assert_js_1.default.hash(hash);
    void 0 === salt && (salt = new Uint8Array(hash.outputLen));
    return (0,hmac_js_1.hmac)(hash, (0,utils_js_1.toBytes)(salt), (0,utils_js_1.toBytes)(ikm));
  }
  function expand(hash, prk, info, length = 32) {
    _assert_js_1.default.hash(hash);
    _assert_js_1.default.number(length);
    if (length > 255 * hash.outputLen) {
      throw Error("Length should be \x3c\x3d 255*HashLen");
    }
    const blocks = Math.ceil(length / hash.outputLen);
    void 0 === info && (info = EMPTY_BUFFER);
    const okm = new Uint8Array(blocks * hash.outputLen);
    prk = hmac_js_1.hmac.create(hash, prk);
    const HMACTmp = prk._cloneInto(), T = new Uint8Array(prk.outputLen);
    for (let counter = 0; counter < blocks; counter++) {
      HKDF_COUNTER[0] = counter + 1, HMACTmp.update(0 === counter ? EMPTY_BUFFER : T).update(info).update(HKDF_COUNTER).digestInto(T), okm.set(T, hash.outputLen * counter), prk._cloneInto(HMACTmp);
    }
    prk.destroy();
    HMACTmp.destroy();
    T.fill(0);
    HKDF_COUNTER.fill(0);
    return okm.slice(0, length);
  }
  Object.defineProperty(exports, "__esModule", {value:!0});
  exports.hkdf = exports.expand = exports.extract = void 0;
  const _assert_js_1 = require("module$node_modules$nostr_tools$node_modules$$noble$hashes$_assert"), utils_js_1 = require("module$node_modules$nostr_tools$node_modules$$noble$hashes$utils"), hmac_js_1 = require("module$node_modules$nostr_tools$node_modules$$noble$hashes$hmac");
  exports.extract = extract;
  const HKDF_COUNTER = new Uint8Array([0]), EMPTY_BUFFER = new Uint8Array();
  exports.expand = expand;
  exports.hkdf = (hash, ikm, salt, info, length) => expand(hash, extract(hash, ikm, salt), info, length);
};

//# sourceMappingURL=module$node_modules$nostr_tools$node_modules$$noble$hashes$hkdf.js.map
