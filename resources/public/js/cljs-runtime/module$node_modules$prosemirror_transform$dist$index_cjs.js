shadow$provide.module$node_modules$prosemirror_transform$dist$index_cjs = function(global, require, module, exports) {
  function _wrapNativeSuper(Class) {
    var _cache = "function" === typeof Map ? new Map() : void 0;
    _wrapNativeSuper = function(Class) {
      function Wrapper() {
        return _construct(Class, arguments, _getPrototypeOf(this).constructor);
      }
      if (null === Class || !_isNativeFunction(Class)) {
        return Class;
      }
      if ("function" !== typeof Class) {
        throw new TypeError("Super expression must either be null or a function");
      }
      if ("undefined" !== typeof _cache) {
        if (_cache.has(Class)) {
          return _cache.get(Class);
        }
        _cache.set(Class, Wrapper);
      }
      Wrapper.prototype = Object.create(Class.prototype, {constructor:{value:Wrapper, enumerable:!1, writable:!0, configurable:!0}});
      return _setPrototypeOf(Wrapper, Class);
    };
    return _wrapNativeSuper(Class);
  }
  function _construct(Parent, args, Class) {
    _construct = _isNativeReflectConstruct() ? Reflect.construct.bind() : function(Parent, args, Class) {
      var a = [null];
      a.push.apply(a, args);
      Parent = new (Function.bind.apply(Parent, a))();
      Class && _setPrototypeOf(Parent, Class.prototype);
      return Parent;
    };
    return _construct.apply(null, arguments);
  }
  function _isNativeFunction(fn) {
    try {
      return -1 !== Function.toString.call(fn).indexOf("[native code]");
    } catch (e) {
      return "function" === typeof fn;
    }
  }
  function _inherits(subClass, superClass) {
    if ("function" !== typeof superClass && null !== superClass) {
      throw new TypeError("Super expression must either be null or a function");
    }
    subClass.prototype = Object.create(superClass && superClass.prototype, {constructor:{value:subClass, writable:!0, configurable:!0}});
    Object.defineProperty(subClass, "prototype", {writable:!1});
    superClass && _setPrototypeOf(subClass, superClass);
  }
  function _setPrototypeOf(o, p) {
    _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(o, p) {
      o.__proto__ = p;
      return o;
    };
    return _setPrototypeOf(o, p);
  }
  function _createSuper(Derived) {
    var hasNativeReflectConstruct = _isNativeReflectConstruct();
    return function() {
      var Super = _getPrototypeOf(Derived);
      if (hasNativeReflectConstruct) {
        var NewTarget = _getPrototypeOf(this).constructor;
        Super = Reflect.construct(Super, arguments, NewTarget);
      } else {
        Super = Super.apply(this, arguments);
      }
      if (!Super || "object" !== _typeof(Super) && "function" !== typeof Super) {
        if (void 0 !== Super) {
          throw new TypeError("Derived constructors may only return object or undefined");
        }
        if (void 0 === this) {
          throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
        }
        Super = this;
      }
      return Super;
    };
  }
  function _isNativeReflectConstruct() {
    if ("undefined" === typeof Reflect || !Reflect.construct || Reflect.construct.sham) {
      return !1;
    }
    if ("function" === typeof Proxy) {
      return !0;
    }
    try {
      return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
      })), !0;
    } catch (e) {
      return !1;
    }
  }
  function _getPrototypeOf(o) {
    _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(o) {
      return o.__proto__ || Object.getPrototypeOf(o);
    };
    return _getPrototypeOf(o);
  }
  function _typeof(o) {
    "@babel/helpers - typeof";
    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
      return typeof o;
    } : function(o) {
      return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
    }, _typeof(o);
  }
  function _classCallCheck(instance, Constructor) {
    if (!(instance instanceof Constructor)) {
      throw new TypeError("Cannot call a class as a function");
    }
  }
  function _defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || !1;
      descriptor.configurable = !0;
      "value" in descriptor && (descriptor.writable = !0);
      var JSCompiler_temp_const = Object, JSCompiler_temp_const$jscomp$0 = JSCompiler_temp_const.defineProperty;
      a: {
        var JSCompiler_inline_result = descriptor.key;
        if ("object" === _typeof(JSCompiler_inline_result) && null !== JSCompiler_inline_result) {
          var prim = JSCompiler_inline_result[Symbol.toPrimitive];
          if (void 0 !== prim) {
            JSCompiler_inline_result = prim.call(JSCompiler_inline_result, "string");
            if ("object" !== _typeof(JSCompiler_inline_result)) {
              break a;
            }
            throw new TypeError("@@toPrimitive must return a primitive value.");
          }
          JSCompiler_inline_result = String(JSCompiler_inline_result);
        }
      }
      JSCompiler_inline_result = "symbol" === _typeof(JSCompiler_inline_result) ? JSCompiler_inline_result : String(JSCompiler_inline_result);
      JSCompiler_temp_const$jscomp$0.call(JSCompiler_temp_const, target, JSCompiler_inline_result, descriptor);
    }
  }
  function _createClass(Constructor, protoProps, staticProps) {
    protoProps && _defineProperties(Constructor.prototype, protoProps);
    staticProps && _defineProperties(Constructor, staticProps);
    Object.defineProperty(Constructor, "prototype", {writable:!1});
    return Constructor;
  }
  function mapFragment(fragment, f, parent) {
    for (var mapped = [], i = 0; i < fragment.childCount; i++) {
      var child = fragment.child(i);
      child.content.size && (child = child.copy(mapFragment(child.content, f, child)));
      child.isInline && (child = f(child, parent, i));
      mapped.push(child);
    }
    return prosemirrorModel.Fragment.fromArray(mapped);
  }
  function contentBetween(doc, from, to) {
    doc = doc.resolve(from);
    from = to - from;
    for (to = doc.depth; 0 < from && 0 < to && doc.indexAfter(to) == doc.node(to).childCount;) {
      to--, from--;
    }
    if (0 < from) {
      for (doc = doc.node(to).maybeChild(doc.indexAfter(to)); 0 < from;) {
        if (!doc || doc.isLeaf) {
          return !0;
        }
        doc = doc.firstChild;
        from--;
      }
    }
    return !1;
  }
  function _addMark(tr, from, to, mark) {
    var removed = [], added = [], removing, adding;
    tr.doc.nodesBetween(from, to, function(node, pos, parent) {
      if (node.isInline) {
        var marks = node.marks;
        if (!mark.isInSet(marks) && parent.type.allowsMarkType(mark.type)) {
          parent = Math.max(pos, from);
          node = Math.min(pos + node.nodeSize, to);
          pos = mark.addToSet(marks);
          for (var i = 0; i < marks.length; i++) {
            marks[i].isInSet(pos) || (removing && removing.to == parent && removing.mark.eq(marks[i]) ? removing.to = node : removed.push(removing = new RemoveMarkStep(parent, node, marks[i])));
          }
          adding && adding.to == parent ? adding.to = node : added.push(adding = new AddMarkStep(parent, node, mark));
        }
      }
    });
    removed.forEach(function(s) {
      return tr.step(s);
    });
    added.forEach(function(s) {
      return tr.step(s);
    });
  }
  function _removeMark(tr, from, to, mark) {
    var matched = [], step = 0;
    tr.doc.nodesBetween(from, to, function(node, pos) {
      if (node.isInline) {
        step++;
        var toRemove = null;
        if (mark instanceof prosemirrorModel.MarkType) {
          for (var set = node.marks, found; found = mark.isInSet(set);) {
            (toRemove || (toRemove = [])).push(found), set = found.removeFromSet(set);
          }
        } else {
          mark ? mark.isInSet(node.marks) && (toRemove = [mark]) : toRemove = node.marks;
        }
        if (toRemove && toRemove.length) {
          for (node = Math.min(pos + node.nodeSize, to), set = 0; set < toRemove.length; set++) {
            found = toRemove[set];
            for (var _found = void 0, j = 0; j < matched.length; j++) {
              var m = matched[j];
              m.step == step - 1 && found.eq(matched[j].style) && (_found = m);
            }
            _found ? (_found.to = node, _found.step = step) : matched.push({style:found, from:Math.max(pos, from), to:node, step});
          }
        }
      }
    });
    matched.forEach(function(m) {
      return tr.step(new RemoveMarkStep(m.from, m.to, m.style));
    });
  }
  function _clearIncompatible(tr, pos, parentType) {
    for (var match = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : parentType.contentMatch, clearNewlines = 4 < arguments.length && void 0 !== arguments[4] ? arguments[4] : !0, node = tr.doc.nodeAt(pos), replSteps = [], cur = pos + 1, i = 0; i < node.childCount; i++) {
      var child = node.child(i), end = cur + child.nodeSize, allowed = match.matchType(child.type);
      if (allowed) {
        match = allowed;
        for (allowed = 0; allowed < child.marks.length; allowed++) {
          parentType.allowsMarkType(child.marks[allowed].type) || tr.step(new RemoveMarkStep(cur, end, child.marks[allowed]));
        }
        if (clearNewlines && child.isText && "pre" != parentType.whitespace) {
          for (var newline = /\r?\n|\r/g, slice = void 0; allowed = newline.exec(child.text);) {
            slice || (slice = new prosemirrorModel.Slice(prosemirrorModel.Fragment.from(parentType.schema.text(" ", parentType.allowedMarks(child.marks))), 0, 0)), replSteps.push(new ReplaceStep(cur + allowed.index, cur + allowed.index + allowed[0].length, slice));
          }
        }
      } else {
        replSteps.push(new ReplaceStep(cur, end, prosemirrorModel.Slice.empty));
      }
      cur = end;
    }
    match.validEnd || (match = match.fillBefore(prosemirrorModel.Fragment.empty, !0), tr.replace(cur, cur, new prosemirrorModel.Slice(match, 0, 0)));
    for (cur = replSteps.length - 1; 0 <= cur; cur--) {
      tr.step(replSteps[cur]);
    }
  }
  function withAttrs(type) {
    return {type, attrs:null};
  }
  function _setBlockType(tr, from, to, type, attrs) {
    if (!type.isTextblock) {
      throw new RangeError("Type given to setBlockType should be a textblock");
    }
    var mapFrom = tr.steps.length;
    tr.doc.nodesBetween(from, to, function(node, pos$jscomp$0) {
      var attrsHere = "function" == typeof attrs ? attrs(node) : attrs, JSCompiler_temp;
      if (JSCompiler_temp = node.isTextblock && !node.hasMarkup(type, attrsHere)) {
        JSCompiler_temp = tr.doc;
        var pos = tr.mapping.slice(mapFrom).map(pos$jscomp$0);
        JSCompiler_temp = JSCompiler_temp.resolve(pos);
        pos = JSCompiler_temp.index();
        JSCompiler_temp = JSCompiler_temp.parent.canReplaceWith(pos, pos + 1, type);
      }
      if (JSCompiler_temp) {
        JSCompiler_temp = null;
        if (type.schema.linebreakReplacement) {
          pos = "pre" == type.whitespace;
          var supportLinebreak = !!type.contentMatch.matchType(type.schema.linebreakReplacement);
          pos && !supportLinebreak ? JSCompiler_temp = !1 : !pos && supportLinebreak && (JSCompiler_temp = !0);
        }
        !1 === JSCompiler_temp && replaceLinebreaks(tr, node, pos$jscomp$0, mapFrom);
        _clearIncompatible(tr, tr.mapping.slice(mapFrom).map(pos$jscomp$0, 1), type, void 0, null === JSCompiler_temp);
        supportLinebreak = tr.mapping.slice(mapFrom);
        pos = supportLinebreak.map(pos$jscomp$0, 1);
        supportLinebreak = supportLinebreak.map(pos$jscomp$0 + node.nodeSize, 1);
        tr.step(new ReplaceAroundStep(pos, supportLinebreak, pos + 1, supportLinebreak - 1, new prosemirrorModel.Slice(prosemirrorModel.Fragment.from(type.create(attrsHere, null, node.marks)), 0, 0), 1, !0));
        !0 === JSCompiler_temp && replaceNewlines(tr, node, pos$jscomp$0, mapFrom);
        return !1;
      }
    });
  }
  function replaceNewlines(tr, node, pos, mapFrom) {
    node.forEach(function(child, offset) {
      if (child.isText) {
        for (var m, newline = /\r?\n|\r/g; m = newline.exec(child.text);) {
          m = tr.mapping.slice(mapFrom).map(pos + 1 + offset + m.index), tr.replaceWith(m, m + 1, node.type.schema.linebreakReplacement.create());
        }
      }
    });
  }
  function replaceLinebreaks(tr, node, pos, mapFrom) {
    node.forEach(function(child, offset) {
      child.type == child.type.schema.linebreakReplacement && (child = tr.mapping.slice(mapFrom).map(pos + 1 + offset), tr.replaceWith(child, child + 1, node.type.schema.text("\n")));
    });
  }
  function _split(tr, pos) {
    for (var depth = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : 1, typesAfter = 3 < arguments.length ? arguments[3] : void 0, $pos = tr.doc.resolve(pos), before = prosemirrorModel.Fragment.empty, after = prosemirrorModel.Fragment.empty, d = $pos.depth, e = $pos.depth - depth, i = depth - 1; d > e; d--, i--) {
      before = prosemirrorModel.Fragment.from($pos.node(d).copy(before));
      var typeAfter = typesAfter && typesAfter[i];
      after = prosemirrorModel.Fragment.from(typeAfter ? typeAfter.type.create(typeAfter.attrs, after) : $pos.node(d).copy(after));
    }
    tr.step(new ReplaceStep(pos, pos, new prosemirrorModel.Slice(before.append(after), depth, depth), !0));
  }
  function canAppendWithSubstitutedLinebreaks(a, b) {
    b.content.size || a.type.compatibleContent(b.type);
    for (var match = a.contentMatchAt(a.childCount), linebreakReplacement = a.type.schema.linebreakReplacement, i = 0; i < b.childCount; i++) {
      var child = b.child(i);
      match = match.matchType(child.type == linebreakReplacement ? a.type.schema.nodes.text : child.type);
      if (!match || !a.type.allowsMarks(child.marks)) {
        return !1;
      }
    }
    return match.validEnd;
  }
  function insertPoint(doc, pos, nodeType) {
    doc = doc.resolve(pos);
    if (doc.parent.canReplaceWith(doc.index(), doc.index(), nodeType)) {
      return pos;
    }
    if (0 == doc.parentOffset) {
      for (pos = doc.depth - 1; 0 <= pos; pos--) {
        var index = doc.index(pos);
        if (doc.node(pos).canReplaceWith(index, index, nodeType)) {
          return doc.before(pos + 1);
        }
        if (0 < index) {
          return null;
        }
      }
    }
    if (doc.parentOffset == doc.parent.content.size) {
      for (pos = doc.depth - 1; 0 <= pos; pos--) {
        index = doc.indexAfter(pos);
        if (doc.node(pos).canReplaceWith(index, index, nodeType)) {
          return doc.after(pos + 1);
        }
        if (index < doc.node(pos).childCount) {
          break;
        }
      }
    }
    return null;
  }
  function replaceStep(doc, from) {
    var to = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : from, slice = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : prosemirrorModel.Slice.empty;
    if (from == to && !slice.size) {
      return null;
    }
    var $from = doc.resolve(from), $to = doc.resolve(to);
    return fitsTrivially($from, $to, slice) ? new ReplaceStep(from, to, slice) : (new Fitter($from, $to, slice)).fit();
  }
  function fitsTrivially($from, $to, slice) {
    return !slice.openStart && !slice.openEnd && $from.start() == $to.start() && $from.parent.canReplace($from.index(), $to.index(), slice.content);
  }
  function dropFromFragment(fragment, depth, count) {
    return 0 == depth ? fragment.cutByIndex(count, fragment.childCount) : fragment.replaceChild(0, fragment.firstChild.copy(dropFromFragment(fragment.firstChild.content, depth - 1, count)));
  }
  function addToFragment(fragment, depth, content) {
    return 0 == depth ? fragment.append(content) : fragment.replaceChild(fragment.childCount - 1, fragment.lastChild.copy(addToFragment(fragment.lastChild.content, depth - 1, content)));
  }
  function contentAt(fragment, depth) {
    for (var i = 0; i < depth; i++) {
      fragment = fragment.firstChild.content;
    }
    return fragment;
  }
  function closeNodeStart(node, openStart, openEnd) {
    if (0 >= openStart) {
      return node;
    }
    var frag = node.content;
    1 < openStart && (frag = frag.replaceChild(0, closeNodeStart(frag.firstChild, openStart - 1, 1 == frag.childCount ? openEnd - 1 : 0)));
    0 < openStart && (frag = node.type.contentMatch.fillBefore(frag).append(frag), 0 >= openEnd && (frag = frag.append(node.type.contentMatch.matchFragment(frag).fillBefore(prosemirrorModel.Fragment.empty, !0))));
    return node.copy(frag);
  }
  function contentAfterFits($to, depth, type, match, open) {
    var node = $to.node(depth);
    $to = open ? $to.indexAfter(depth) : $to.index(depth);
    if ($to == node.childCount && !type.compatibleContent(node.type)) {
      return null;
    }
    if (depth = match = match.fillBefore(node.content, !0, $to)) {
      a: {
        for (node = node.content; $to < node.childCount; $to++) {
          if (!type.allowsMarks(node.child($to).marks)) {
            type = !0;
            break a;
          }
        }
        type = !1;
      }
      depth = !type;
    }
    return depth ? match : null;
  }
  function closeFragment(fragment, depth, oldOpen, newOpen, parent) {
    if (depth < oldOpen) {
      var first = fragment.firstChild;
      fragment = fragment.replaceChild(0, first.copy(closeFragment(first.content, depth + 1, oldOpen, newOpen, first)));
    }
    depth > newOpen && (depth = parent.contentMatchAt(0), fragment = depth.fillBefore(fragment).append(fragment), fragment = fragment.append(depth.matchFragment(fragment).fillBefore(prosemirrorModel.Fragment.empty, !0)));
    return fragment;
  }
  function coveredDepths($from, $to) {
    for (var result = [], d = Math.min($from.depth, $to.depth); 0 <= d; d--) {
      var start = $from.start(d);
      if (start < $from.pos - ($from.depth - d) || $to.end(d) > $to.pos + ($to.depth - d) || $from.node(d).type.spec.isolating || $to.node(d).type.spec.isolating) {
        break;
      }
      (start == $to.start(d) || d == $from.depth && d == $to.depth && $from.parent.inlineContent && $to.parent.inlineContent && d && $to.start(d - 1) == start - 1) && result.push(d);
    }
    return result;
  }
  var prosemirrorModel = require("module$node_modules$prosemirror_model$dist$index_cjs"), factor16 = Math.pow(2, 16), MapResult = function() {
    function MapResult(pos, delInfo, recover) {
      _classCallCheck(this, MapResult);
      this.pos = pos;
      this.delInfo = delInfo;
      this.recover = recover;
    }
    _createClass(MapResult, [{key:"deleted", get:function() {
      return 0 < (this.delInfo & 8);
    }}, {key:"deletedBefore", get:function() {
      return 0 < (this.delInfo & 5);
    }}, {key:"deletedAfter", get:function() {
      return 0 < (this.delInfo & 6);
    }}, {key:"deletedAcross", get:function() {
      return 0 < (this.delInfo & 4);
    }}]);
    return MapResult;
  }(), StepMap = function() {
    function StepMap(ranges) {
      var inverted = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : !1;
      _classCallCheck(this, StepMap);
      this.ranges = ranges;
      this.inverted = inverted;
      if (!ranges.length && StepMap.empty) {
        return StepMap.empty;
      }
    }
    _createClass(StepMap, [{key:"recover", value:function(value) {
      var diff = 0, index = value & 65535;
      if (!this.inverted) {
        for (var i = 0; i < index; i++) {
          diff += this.ranges[3 * i + 2] - this.ranges[3 * i + 1];
        }
      }
      return this.ranges[3 * index] + diff + (value - (value & 65535)) / factor16;
    }}, {key:"mapResult", value:function(pos) {
      return this._map(pos, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 1, !1);
    }}, {key:"map", value:function(pos) {
      return this._map(pos, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 1, !0);
    }}, {key:"_map", value:function(pos, assoc, simple) {
      for (var diff = 0, oldIndex = this.inverted ? 2 : 1, newIndex = this.inverted ? 1 : 2, i = 0; i < this.ranges.length; i += 3) {
        var start = this.ranges[i] - (this.inverted ? diff : 0);
        if (start > pos) {
          break;
        }
        var oldSize = this.ranges[i + oldIndex], newSize = this.ranges[i + newIndex], end = start + oldSize;
        if (pos <= end) {
          diff = start + diff + (0 > (oldSize ? pos == start ? -1 : pos == end ? 1 : assoc : assoc) ? 0 : newSize);
          if (simple) {
            return diff;
          }
          simple = pos == (0 > assoc ? start : end) ? null : i / 3 + (pos - start) * factor16;
          i = pos == start ? 2 : pos == end ? 1 : 4;
          if (0 > assoc ? pos != start : pos != end) {
            i |= 8;
          }
          return new MapResult(diff, i, simple);
        }
        diff += newSize - oldSize;
      }
      return simple ? pos + diff : new MapResult(pos + diff, 0, null);
    }}, {key:"touches", value:function(pos, recover) {
      var diff = 0;
      recover &= 65535;
      for (var oldIndex = this.inverted ? 2 : 1, newIndex = this.inverted ? 1 : 2, i = 0; i < this.ranges.length; i += 3) {
        var start = this.ranges[i] - (this.inverted ? diff : 0);
        if (start > pos) {
          break;
        }
        var oldSize = this.ranges[i + oldIndex];
        if (pos <= start + oldSize && i == 3 * recover) {
          return !0;
        }
        diff += this.ranges[i + newIndex] - oldSize;
      }
      return !1;
    }}, {key:"forEach", value:function(f) {
      for (var oldIndex = this.inverted ? 2 : 1, newIndex = this.inverted ? 1 : 2, i = 0, diff = 0; i < this.ranges.length; i += 3) {
        var start = this.ranges[i], oldStart = start - (this.inverted ? diff : 0);
        start += this.inverted ? 0 : diff;
        var oldSize = this.ranges[i + oldIndex], newSize = this.ranges[i + newIndex];
        f(oldStart, oldStart + oldSize, start, start + newSize);
        diff += newSize - oldSize;
      }
    }}, {key:"invert", value:function() {
      return new StepMap(this.ranges, !this.inverted);
    }}, {key:"toString", value:function() {
      return (this.inverted ? "-" : "") + JSON.stringify(this.ranges);
    }}], [{key:"offset", value:function(n) {
      return 0 == n ? StepMap.empty : new StepMap(0 > n ? [0, -n, 0] : [0, 0, n]);
    }}]);
    return StepMap;
  }();
  StepMap.empty = new StepMap([]);
  var Mapping = function() {
    function Mapping() {
      var maps = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : [], mirror = 1 < arguments.length ? arguments[1] : void 0, from = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : 0, to = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : maps.length;
      _classCallCheck(this, Mapping);
      this.maps = maps;
      this.mirror = mirror;
      this.from = from;
      this.to = to;
    }
    _createClass(Mapping, [{key:"slice", value:function() {
      return new Mapping(this.maps, this.mirror, 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : 0, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : this.maps.length);
    }}, {key:"copy", value:function() {
      return new Mapping(this.maps.slice(), this.mirror && this.mirror.slice(), this.from, this.to);
    }}, {key:"appendMap", value:function(map, mirrors) {
      this.to = this.maps.push(map);
      null != mirrors && this.setMirror(this.maps.length - 1, mirrors);
    }}, {key:"appendMapping", value:function(mapping) {
      for (var i = 0, startSize = this.maps.length; i < mapping.maps.length; i++) {
        var mirr = mapping.getMirror(i);
        this.appendMap(mapping.maps[i], null != mirr && mirr < i ? startSize + mirr : void 0);
      }
    }}, {key:"getMirror", value:function(n) {
      if (this.mirror) {
        for (var i = 0; i < this.mirror.length; i++) {
          if (this.mirror[i] == n) {
            return this.mirror[i + (i % 2 ? -1 : 1)];
          }
        }
      }
    }}, {key:"setMirror", value:function(n, m) {
      this.mirror || (this.mirror = []);
      this.mirror.push(n, m);
    }}, {key:"appendMappingInverted", value:function(mapping) {
      for (var i = mapping.maps.length - 1, totalSize = this.maps.length + mapping.maps.length; 0 <= i; i--) {
        var mirr = mapping.getMirror(i);
        this.appendMap(mapping.maps[i].invert(), null != mirr && mirr > i ? totalSize - mirr - 1 : void 0);
      }
    }}, {key:"invert", value:function() {
      var inverse = new Mapping();
      inverse.appendMappingInverted(this);
      return inverse;
    }}, {key:"map", value:function(pos) {
      var assoc = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 1;
      if (this.mirror) {
        return this._map(pos, assoc, !0);
      }
      for (var i = this.from; i < this.to; i++) {
        pos = this.maps[i].map(pos, assoc);
      }
      return pos;
    }}, {key:"mapResult", value:function(pos) {
      return this._map(pos, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 1, !1);
    }}, {key:"_map", value:function(pos, assoc, simple) {
      for (var delInfo = 0, i = this.from; i < this.to; i++) {
        pos = this.maps[i].mapResult(pos, assoc);
        if (null != pos.recover) {
          var corr = this.getMirror(i);
          if (null != corr && corr > i && corr < this.to) {
            i = corr;
            pos = this.maps[corr].recover(pos.recover);
            continue;
          }
        }
        delInfo |= pos.delInfo;
        pos = pos.pos;
      }
      return simple ? pos : new MapResult(pos, delInfo, null);
    }}]);
    return Mapping;
  }(), stepsByID = Object.create(null);
  global = function() {
    function Step() {
      _classCallCheck(this, Step);
    }
    _createClass(Step, [{key:"getMap", value:function() {
      return StepMap.empty;
    }}, {key:"merge", value:function(other) {
      return null;
    }}], [{key:"fromJSON", value:function(schema, json) {
      if (!json || !json.stepType) {
        throw new RangeError("Invalid input for Step.fromJSON");
      }
      var type = stepsByID[json.stepType];
      if (!type) {
        throw new RangeError("No step type ".concat(json.stepType, " defined"));
      }
      return type.fromJSON(schema, json);
    }}, {key:"jsonID", value:function(id, stepClass) {
      if (id in stepsByID) {
        throw new RangeError("Duplicate use of step JSON ID " + id);
      }
      stepsByID[id] = stepClass;
      stepClass.prototype.jsonID = id;
      return stepClass;
    }}]);
    return Step;
  }();
  var StepResult = function() {
    function StepResult(doc, failed) {
      _classCallCheck(this, StepResult);
      this.doc = doc;
      this.failed = failed;
    }
    _createClass(StepResult, null, [{key:"ok", value:function(doc) {
      return new StepResult(doc, null);
    }}, {key:"fail", value:function(message) {
      return new StepResult(null, message);
    }}, {key:"fromReplace", value:function(doc, from, to, slice) {
      try {
        return StepResult.ok(doc.replace(from, to, slice));
      } catch (e) {
        if (e instanceof prosemirrorModel.ReplaceError) {
          return StepResult.fail(e.message);
        }
        throw e;
      }
    }}]);
    return StepResult;
  }(), AddMarkStep = function(_Step) {
    function AddMarkStep(from, to, mark) {
      _classCallCheck(this, AddMarkStep);
      var _this = _super.call(this);
      _this.from = from;
      _this.to = to;
      _this.mark = mark;
      return _this;
    }
    _inherits(AddMarkStep, _Step);
    var _super = _createSuper(AddMarkStep);
    _createClass(AddMarkStep, [{key:"apply", value:function(doc) {
      var _this2 = this, oldSlice = doc.slice(this.from, this.to), $from = doc.resolve(this.from);
      $from = $from.node($from.sharedDepth(this.to));
      oldSlice = new prosemirrorModel.Slice(mapFragment(oldSlice.content, function(node, parent) {
        return node.isAtom && parent.type.allowsMarkType(_this2.mark.type) ? node.mark(_this2.mark.addToSet(node.marks)) : node;
      }, $from), oldSlice.openStart, oldSlice.openEnd);
      return StepResult.fromReplace(doc, this.from, this.to, oldSlice);
    }}, {key:"invert", value:function() {
      return new RemoveMarkStep(this.from, this.to, this.mark);
    }}, {key:"map", value:function(mapping) {
      var from = mapping.mapResult(this.from, 1);
      mapping = mapping.mapResult(this.to, -1);
      return from.deleted && mapping.deleted || from.pos >= mapping.pos ? null : new AddMarkStep(from.pos, mapping.pos, this.mark);
    }}, {key:"merge", value:function(other) {
      return other instanceof AddMarkStep && other.mark.eq(this.mark) && this.from <= other.to && this.to >= other.from ? new AddMarkStep(Math.min(this.from, other.from), Math.max(this.to, other.to), this.mark) : null;
    }}, {key:"toJSON", value:function() {
      return {stepType:"addMark", mark:this.mark.toJSON(), from:this.from, to:this.to};
    }}], [{key:"fromJSON", value:function(schema, json) {
      if ("number" != typeof json.from || "number" != typeof json.to) {
        throw new RangeError("Invalid input for AddMarkStep.fromJSON");
      }
      return new AddMarkStep(json.from, json.to, schema.markFromJSON(json.mark));
    }}]);
    return AddMarkStep;
  }(global);
  global.jsonID("addMark", AddMarkStep);
  var RemoveMarkStep = function(_Step2) {
    function RemoveMarkStep(from, to, mark) {
      _classCallCheck(this, RemoveMarkStep);
      var _this3 = _super2.call(this);
      _this3.from = from;
      _this3.to = to;
      _this3.mark = mark;
      return _this3;
    }
    _inherits(RemoveMarkStep, _Step2);
    var _super2 = _createSuper(RemoveMarkStep);
    _createClass(RemoveMarkStep, [{key:"apply", value:function(doc) {
      var _this4 = this, oldSlice = doc.slice(this.from, this.to);
      oldSlice = new prosemirrorModel.Slice(mapFragment(oldSlice.content, function(node) {
        return node.mark(_this4.mark.removeFromSet(node.marks));
      }, doc), oldSlice.openStart, oldSlice.openEnd);
      return StepResult.fromReplace(doc, this.from, this.to, oldSlice);
    }}, {key:"invert", value:function() {
      return new AddMarkStep(this.from, this.to, this.mark);
    }}, {key:"map", value:function(mapping) {
      var from = mapping.mapResult(this.from, 1);
      mapping = mapping.mapResult(this.to, -1);
      return from.deleted && mapping.deleted || from.pos >= mapping.pos ? null : new RemoveMarkStep(from.pos, mapping.pos, this.mark);
    }}, {key:"merge", value:function(other) {
      return other instanceof RemoveMarkStep && other.mark.eq(this.mark) && this.from <= other.to && this.to >= other.from ? new RemoveMarkStep(Math.min(this.from, other.from), Math.max(this.to, other.to), this.mark) : null;
    }}, {key:"toJSON", value:function() {
      return {stepType:"removeMark", mark:this.mark.toJSON(), from:this.from, to:this.to};
    }}], [{key:"fromJSON", value:function(schema, json) {
      if ("number" != typeof json.from || "number" != typeof json.to) {
        throw new RangeError("Invalid input for RemoveMarkStep.fromJSON");
      }
      return new RemoveMarkStep(json.from, json.to, schema.markFromJSON(json.mark));
    }}]);
    return RemoveMarkStep;
  }(global);
  global.jsonID("removeMark", RemoveMarkStep);
  var AddNodeMarkStep = function(_Step3) {
    function AddNodeMarkStep(pos, mark) {
      _classCallCheck(this, AddNodeMarkStep);
      var _this5 = _super3.call(this);
      _this5.pos = pos;
      _this5.mark = mark;
      return _this5;
    }
    _inherits(AddNodeMarkStep, _Step3);
    var _super3 = _createSuper(AddNodeMarkStep);
    _createClass(AddNodeMarkStep, [{key:"apply", value:function(doc) {
      var node = doc.nodeAt(this.pos);
      if (!node) {
        return StepResult.fail("No node at mark step's position");
      }
      var updated = node.type.create(node.attrs, null, this.mark.addToSet(node.marks));
      return StepResult.fromReplace(doc, this.pos, this.pos + 1, new prosemirrorModel.Slice(prosemirrorModel.Fragment.from(updated), 0, node.isLeaf ? 0 : 1));
    }}, {key:"invert", value:function(doc) {
      if (doc = doc.nodeAt(this.pos)) {
        var newSet = this.mark.addToSet(doc.marks);
        if (newSet.length == doc.marks.length) {
          for (var i = 0; i < doc.marks.length; i++) {
            if (!doc.marks[i].isInSet(newSet)) {
              return new AddNodeMarkStep(this.pos, doc.marks[i]);
            }
          }
          return new AddNodeMarkStep(this.pos, this.mark);
        }
      }
      return new RemoveNodeMarkStep(this.pos, this.mark);
    }}, {key:"map", value:function(mapping) {
      mapping = mapping.mapResult(this.pos, 1);
      return mapping.deletedAfter ? null : new AddNodeMarkStep(mapping.pos, this.mark);
    }}, {key:"toJSON", value:function() {
      return {stepType:"addNodeMark", pos:this.pos, mark:this.mark.toJSON()};
    }}], [{key:"fromJSON", value:function(schema, json) {
      if ("number" != typeof json.pos) {
        throw new RangeError("Invalid input for AddNodeMarkStep.fromJSON");
      }
      return new AddNodeMarkStep(json.pos, schema.markFromJSON(json.mark));
    }}]);
    return AddNodeMarkStep;
  }(global);
  global.jsonID("addNodeMark", AddNodeMarkStep);
  var RemoveNodeMarkStep = function(_Step4) {
    function RemoveNodeMarkStep(pos, mark) {
      _classCallCheck(this, RemoveNodeMarkStep);
      var _this6 = _super4.call(this);
      _this6.pos = pos;
      _this6.mark = mark;
      return _this6;
    }
    _inherits(RemoveNodeMarkStep, _Step4);
    var _super4 = _createSuper(RemoveNodeMarkStep);
    _createClass(RemoveNodeMarkStep, [{key:"apply", value:function(doc) {
      var node = doc.nodeAt(this.pos);
      if (!node) {
        return StepResult.fail("No node at mark step's position");
      }
      var updated = node.type.create(node.attrs, null, this.mark.removeFromSet(node.marks));
      return StepResult.fromReplace(doc, this.pos, this.pos + 1, new prosemirrorModel.Slice(prosemirrorModel.Fragment.from(updated), 0, node.isLeaf ? 0 : 1));
    }}, {key:"invert", value:function(doc) {
      return (doc = doc.nodeAt(this.pos)) && this.mark.isInSet(doc.marks) ? new AddNodeMarkStep(this.pos, this.mark) : this;
    }}, {key:"map", value:function(mapping) {
      mapping = mapping.mapResult(this.pos, 1);
      return mapping.deletedAfter ? null : new RemoveNodeMarkStep(mapping.pos, this.mark);
    }}, {key:"toJSON", value:function() {
      return {stepType:"removeNodeMark", pos:this.pos, mark:this.mark.toJSON()};
    }}], [{key:"fromJSON", value:function(schema, json) {
      if ("number" != typeof json.pos) {
        throw new RangeError("Invalid input for RemoveNodeMarkStep.fromJSON");
      }
      return new RemoveNodeMarkStep(json.pos, schema.markFromJSON(json.mark));
    }}]);
    return RemoveNodeMarkStep;
  }(global);
  global.jsonID("removeNodeMark", RemoveNodeMarkStep);
  var ReplaceStep = function(_Step5) {
    function ReplaceStep(from, to, slice) {
      var structure = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : !1;
      _classCallCheck(this, ReplaceStep);
      var _this7 = _super5.call(this);
      _this7.from = from;
      _this7.to = to;
      _this7.slice = slice;
      _this7.structure = structure;
      return _this7;
    }
    _inherits(ReplaceStep, _Step5);
    var _super5 = _createSuper(ReplaceStep);
    _createClass(ReplaceStep, [{key:"apply", value:function(doc) {
      return this.structure && contentBetween(doc, this.from, this.to) ? StepResult.fail("Structure replace would overwrite content") : StepResult.fromReplace(doc, this.from, this.to, this.slice);
    }}, {key:"getMap", value:function() {
      return new StepMap([this.from, this.to - this.from, this.slice.size]);
    }}, {key:"invert", value:function(doc) {
      return new ReplaceStep(this.from, this.from + this.slice.size, doc.slice(this.from, this.to));
    }}, {key:"map", value:function(mapping) {
      var from = mapping.mapResult(this.from, 1);
      mapping = mapping.mapResult(this.to, -1);
      return from.deletedAcross && mapping.deletedAcross ? null : new ReplaceStep(from.pos, Math.max(from.pos, mapping.pos), this.slice);
    }}, {key:"merge", value:function(other) {
      if (!(other instanceof ReplaceStep) || other.structure || this.structure) {
        return null;
      }
      if (this.from + this.slice.size != other.from || this.slice.openEnd || other.slice.openStart) {
        if (other.to != this.from || this.slice.openStart || other.slice.openEnd) {
          return null;
        }
        var _slice = 0 == this.slice.size + other.slice.size ? prosemirrorModel.Slice.empty : new prosemirrorModel.Slice(other.slice.content.append(this.slice.content), other.slice.openStart, this.slice.openEnd);
        return new ReplaceStep(other.from, this.to, _slice, this.structure);
      }
      _slice = 0 == this.slice.size + other.slice.size ? prosemirrorModel.Slice.empty : new prosemirrorModel.Slice(this.slice.content.append(other.slice.content), this.slice.openStart, other.slice.openEnd);
      return new ReplaceStep(this.from, this.to + (other.to - other.from), _slice, this.structure);
    }}, {key:"toJSON", value:function() {
      var json = {stepType:"replace", from:this.from, to:this.to};
      this.slice.size && (json.slice = this.slice.toJSON());
      this.structure && (json.structure = !0);
      return json;
    }}], [{key:"fromJSON", value:function(schema, json) {
      if ("number" != typeof json.from || "number" != typeof json.to) {
        throw new RangeError("Invalid input for ReplaceStep.fromJSON");
      }
      return new ReplaceStep(json.from, json.to, prosemirrorModel.Slice.fromJSON(schema, json.slice), !!json.structure);
    }}]);
    return ReplaceStep;
  }(global);
  global.jsonID("replace", ReplaceStep);
  var ReplaceAroundStep = function(_Step6) {
    function ReplaceAroundStep(from, to, gapFrom, gapTo, slice, insert) {
      var structure = 6 < arguments.length && void 0 !== arguments[6] ? arguments[6] : !1;
      _classCallCheck(this, ReplaceAroundStep);
      var _this8 = _super6.call(this);
      _this8.from = from;
      _this8.to = to;
      _this8.gapFrom = gapFrom;
      _this8.gapTo = gapTo;
      _this8.slice = slice;
      _this8.insert = insert;
      _this8.structure = structure;
      return _this8;
    }
    _inherits(ReplaceAroundStep, _Step6);
    var _super6 = _createSuper(ReplaceAroundStep);
    _createClass(ReplaceAroundStep, [{key:"apply", value:function(doc) {
      if (this.structure && (contentBetween(doc, this.from, this.gapFrom) || contentBetween(doc, this.gapTo, this.to))) {
        return StepResult.fail("Structure gap-replace would overwrite content");
      }
      var gap = doc.slice(this.gapFrom, this.gapTo);
      return gap.openStart || gap.openEnd ? StepResult.fail("Gap is not a flat range") : (gap = this.slice.insertAt(this.insert, gap.content)) ? StepResult.fromReplace(doc, this.from, this.to, gap) : StepResult.fail("Content does not fit in gap");
    }}, {key:"getMap", value:function() {
      return new StepMap([this.from, this.gapFrom - this.from, this.insert, this.gapTo, this.to - this.gapTo, this.slice.size - this.insert]);
    }}, {key:"invert", value:function(doc) {
      var gap = this.gapTo - this.gapFrom;
      return new ReplaceAroundStep(this.from, this.from + this.slice.size + gap, this.from + this.insert, this.from + this.insert + gap, doc.slice(this.from, this.to).removeBetween(this.gapFrom - this.from, this.gapTo - this.from), this.gapFrom - this.from, this.structure);
    }}, {key:"map", value:function(mapping) {
      var from = mapping.mapResult(this.from, 1), to = mapping.mapResult(this.to, -1), gapFrom = this.from == this.gapFrom ? from.pos : mapping.map(this.gapFrom, -1);
      mapping = this.to == this.gapTo ? to.pos : mapping.map(this.gapTo, 1);
      return from.deletedAcross && to.deletedAcross || gapFrom < from.pos || mapping > to.pos ? null : new ReplaceAroundStep(from.pos, to.pos, gapFrom, mapping, this.slice, this.insert, this.structure);
    }}, {key:"toJSON", value:function() {
      var json = {stepType:"replaceAround", from:this.from, to:this.to, gapFrom:this.gapFrom, gapTo:this.gapTo, insert:this.insert};
      this.slice.size && (json.slice = this.slice.toJSON());
      this.structure && (json.structure = !0);
      return json;
    }}], [{key:"fromJSON", value:function(schema, json) {
      if ("number" != typeof json.from || "number" != typeof json.to || "number" != typeof json.gapFrom || "number" != typeof json.gapTo || "number" != typeof json.insert) {
        throw new RangeError("Invalid input for ReplaceAroundStep.fromJSON");
      }
      return new ReplaceAroundStep(json.from, json.to, json.gapFrom, json.gapTo, prosemirrorModel.Slice.fromJSON(schema, json.slice), json.insert, !!json.structure);
    }}]);
    return ReplaceAroundStep;
  }(global);
  global.jsonID("replaceAround", ReplaceAroundStep);
  var Fitter = function() {
    function Fitter($from, $to, unplaced) {
      _classCallCheck(this, Fitter);
      this.$from = $from;
      this.$to = $to;
      this.unplaced = unplaced;
      this.frontier = [];
      this.placed = prosemirrorModel.Fragment.empty;
      for ($to = 0; $to <= $from.depth; $to++) {
        unplaced = $from.node($to), this.frontier.push({type:unplaced.type, match:unplaced.contentMatchAt($from.indexAfter($to))});
      }
      for ($to = $from.depth; 0 < $to; $to--) {
        this.placed = prosemirrorModel.Fragment.from($from.node($to).copy(this.placed));
      }
    }
    _createClass(Fitter, [{key:"depth", get:function() {
      return this.frontier.length - 1;
    }}, {key:"fit", value:function() {
      for (; this.unplaced.size;) {
        var fit = this.findFittable();
        fit ? this.placeNodes(fit) : this.openMore() || this.dropNode();
      }
      fit = this.mustMoveInline();
      var placedSize = this.placed.size - this.depth - this.$from.depth, $from = this.$from, $to = this.close(0 > fit ? this.$to : $from.doc.resolve(fit));
      if (!$to) {
        return null;
      }
      for (var content = this.placed, openStart = $from.depth, openEnd = $to.depth; openStart && openEnd && 1 == content.childCount;) {
        content = content.firstChild.content, openStart--, openEnd--;
      }
      content = new prosemirrorModel.Slice(content, openStart, openEnd);
      return -1 < fit ? new ReplaceAroundStep($from.pos, fit, this.$to.pos, this.$to.end(), content, placedSize) : content.size || $from.pos != this.$to.pos ? new ReplaceStep($from.pos, $to.pos, content) : null;
    }}, {key:"findFittable", value:function() {
      for (var startDepth = this.unplaced.openStart, cur = this.unplaced.content, d = 0, openEnd = this.unplaced.openEnd; d < startDepth; d++) {
        var node = cur.firstChild;
        1 < cur.childCount && (openEnd = 0);
        if (node.type.spec.isolating && openEnd <= d) {
          startDepth = d;
          break;
        }
        cur = node.content;
      }
      for (cur = 1; 2 >= cur; cur++) {
        for (d = 1 == cur ? startDepth : this.unplaced.openStart; 0 <= d; d--) {
          openEnd = null;
          d ? (openEnd = contentAt(this.unplaced.content, d - 1).firstChild, node = openEnd.content) : node = this.unplaced.content;
          node = node.firstChild;
          for (var frontierDepth = this.depth; 0 <= frontierDepth; frontierDepth--) {
            var _this$frontier$fronti = this.frontier[frontierDepth], type = _this$frontier$fronti.type;
            _this$frontier$fronti = _this$frontier$fronti.match;
            var _wrap = void 0, inject = null;
            if (1 == cur && (node ? _this$frontier$fronti.matchType(node.type) || (inject = _this$frontier$fronti.fillBefore(prosemirrorModel.Fragment.from(node), !1)) : openEnd && type.compatibleContent(openEnd.type))) {
              return {sliceDepth:d, frontierDepth, parent:openEnd, inject};
            }
            if (2 == cur && node && (_wrap = _this$frontier$fronti.findWrapping(node.type))) {
              return {sliceDepth:d, frontierDepth, parent:openEnd, wrap:_wrap};
            }
            if (openEnd && _this$frontier$fronti.matchType(openEnd.type)) {
              break;
            }
          }
        }
      }
    }}, {key:"openMore", value:function() {
      var _this$unplaced = this.unplaced, content = _this$unplaced.content, openStart = _this$unplaced.openStart;
      _this$unplaced = _this$unplaced.openEnd;
      var inner = contentAt(content, openStart);
      if (!inner.childCount || inner.firstChild.isLeaf) {
        return !1;
      }
      this.unplaced = new prosemirrorModel.Slice(content, openStart + 1, Math.max(_this$unplaced, inner.size + openStart >= content.size - _this$unplaced ? openStart + 1 : 0));
      return !0;
    }}, {key:"dropNode", value:function() {
      var _this$unplaced2 = this.unplaced, content = _this$unplaced2.content, openStart = _this$unplaced2.openStart;
      _this$unplaced2 = _this$unplaced2.openEnd;
      var inner = contentAt(content, openStart);
      1 >= inner.childCount && 0 < openStart ? (inner = content.size - openStart <= openStart + inner.size, this.unplaced = new prosemirrorModel.Slice(dropFromFragment(content, openStart - 1, 1), openStart - 1, inner ? openStart - 1 : _this$unplaced2)) : this.unplaced = new prosemirrorModel.Slice(dropFromFragment(content, openStart, 1), openStart, _this$unplaced2);
    }}, {key:"placeNodes", value:function(_ref) {
      var sliceDepth = _ref.sliceDepth, frontierDepth = _ref.frontierDepth, parent = _ref.parent, inject = _ref.inject;
      for (_ref = _ref.wrap; this.depth > frontierDepth;) {
        this.closeFrontierNode();
      }
      if (_ref) {
        for (var i = 0; i < _ref.length; i++) {
          this.openFrontierNode(_ref[i]);
        }
      }
      _ref = this.unplaced;
      var fragment = parent ? parent.content : _ref.content, openStart = _ref.openStart - sliceDepth;
      i = 0;
      var add = [], _this$frontier$fronti2 = this.frontier[frontierDepth], match = _this$frontier$fronti2.match;
      _this$frontier$fronti2 = _this$frontier$fronti2.type;
      if (inject) {
        for (var _i3 = 0; _i3 < inject.childCount; _i3++) {
          add.push(inject.child(_i3));
        }
        match = match.matchFragment(inject);
      }
      for (inject = fragment.size + sliceDepth - (_ref.content.size - _ref.openEnd); i < fragment.childCount;) {
        _i3 = fragment.child(i);
        var matches = match.matchType(_i3.type);
        if (!matches) {
          break;
        }
        i++;
        if (1 < i || 0 == openStart || _i3.content.size) {
          match = matches, add.push(closeNodeStart(_i3.mark(_this$frontier$fronti2.allowedMarks(_i3.marks)), 1 == i ? openStart : 0, i == fragment.childCount ? inject : -1));
        }
      }
      (openStart = i == fragment.childCount) || (inject = -1);
      this.placed = addToFragment(this.placed, frontierDepth, prosemirrorModel.Fragment.from(add));
      this.frontier[frontierDepth].match = match;
      openStart && 0 > inject && parent && parent.type == this.frontier[this.depth].type && 1 < this.frontier.length && this.closeFrontierNode();
      frontierDepth = 0;
      for (parent = fragment; frontierDepth < inject; frontierDepth++) {
        parent = parent.lastChild, this.frontier.push({type:parent.type, match:parent.contentMatchAt(parent.childCount)}), parent = parent.content;
      }
      this.unplaced = openStart ? 0 == sliceDepth ? prosemirrorModel.Slice.empty : new prosemirrorModel.Slice(dropFromFragment(_ref.content, sliceDepth - 1, 1), sliceDepth - 1, 0 > inject ? _ref.openEnd : sliceDepth - 1) : new prosemirrorModel.Slice(dropFromFragment(_ref.content, sliceDepth, i), _ref.openStart, _ref.openEnd);
    }}, {key:"mustMoveInline", value:function() {
      if (!this.$to.parent.isTextblock) {
        return -1;
      }
      var top = this.frontier[this.depth], level;
      if (!top.type.isTextblock || !contentAfterFits(this.$to, this.$to.depth, top.type, top.match, !1) || this.$to.depth == this.depth && (level = this.findCloseLevel(this.$to)) && level.depth == this.depth) {
        return -1;
      }
      top = this.$to.depth;
      for (level = this.$to.after(top); 1 < top && level == this.$to.end(--top);) {
        ++level;
      }
      return level;
    }}, {key:"findCloseLevel", value:function($to) {
      var i = Math.min(this.depth, $to.depth);
      a: for (; 0 <= i; i--) {
        var _this$frontier$i = this.frontier[i], match = _this$frontier$i.match, type = _this$frontier$i.type;
        _this$frontier$i = i < $to.depth && $to.end(i + 1) == $to.pos + ($to.depth - (i + 1));
        if (match = contentAfterFits($to, i, type, match, _this$frontier$i)) {
          for (type = i - 1; 0 <= type; type--) {
            var _this$frontier$d = this.frontier[type];
            _this$frontier$d = contentAfterFits($to, type, _this$frontier$d.type, _this$frontier$d.match, !0);
            if (!_this$frontier$d || _this$frontier$d.childCount) {
              continue a;
            }
          }
          return {depth:i, fit:match, move:_this$frontier$i ? $to.doc.resolve($to.after(i + 1)) : $to};
        }
      }
    }}, {key:"close", value:function($to) {
      var close = this.findCloseLevel($to);
      if (!close) {
        return null;
      }
      for (; this.depth > close.depth;) {
        this.closeFrontierNode();
      }
      close.fit.childCount && (this.placed = addToFragment(this.placed, close.depth, close.fit));
      $to = close.move;
      for (close = close.depth + 1; close <= $to.depth; close++) {
        var node = $to.node(close), add = node.type.contentMatch.fillBefore(node.content, !0, $to.index(close));
        this.openFrontierNode(node.type, node.attrs, add);
      }
      return $to;
    }}, {key:"openFrontierNode", value:function(type) {
      var attrs = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : null, content = 2 < arguments.length ? arguments[2] : void 0, top = this.frontier[this.depth];
      top.match = top.match.matchType(type);
      this.placed = addToFragment(this.placed, this.depth, prosemirrorModel.Fragment.from(type.create(attrs, content)));
      this.frontier.push({type, match:type.contentMatch});
    }}, {key:"closeFrontierNode", value:function() {
      var add = this.frontier.pop().match.fillBefore(prosemirrorModel.Fragment.empty, !0);
      add.childCount && (this.placed = addToFragment(this.placed, this.frontier.length, add));
    }}]);
    return Fitter;
  }(), AttrStep = function(_Step7) {
    function AttrStep(pos, attr, value) {
      _classCallCheck(this, AttrStep);
      var _this9 = _super7.call(this);
      _this9.pos = pos;
      _this9.attr = attr;
      _this9.value = value;
      return _this9;
    }
    _inherits(AttrStep, _Step7);
    var _super7 = _createSuper(AttrStep);
    _createClass(AttrStep, [{key:"apply", value:function(doc) {
      var node = doc.nodeAt(this.pos);
      if (!node) {
        return StepResult.fail("No node at attribute step's position");
      }
      var attrs = Object.create(null), name;
      for (name in node.attrs) {
        attrs[name] = node.attrs[name];
      }
      attrs[this.attr] = this.value;
      attrs = node.type.create(attrs, null, node.marks);
      return StepResult.fromReplace(doc, this.pos, this.pos + 1, new prosemirrorModel.Slice(prosemirrorModel.Fragment.from(attrs), 0, node.isLeaf ? 0 : 1));
    }}, {key:"getMap", value:function() {
      return StepMap.empty;
    }}, {key:"invert", value:function(doc) {
      return new AttrStep(this.pos, this.attr, doc.nodeAt(this.pos).attrs[this.attr]);
    }}, {key:"map", value:function(mapping) {
      mapping = mapping.mapResult(this.pos, 1);
      return mapping.deletedAfter ? null : new AttrStep(mapping.pos, this.attr, this.value);
    }}, {key:"toJSON", value:function() {
      return {stepType:"attr", pos:this.pos, attr:this.attr, value:this.value};
    }}], [{key:"fromJSON", value:function(schema, json) {
      if ("number" != typeof json.pos || "string" != typeof json.attr) {
        throw new RangeError("Invalid input for AttrStep.fromJSON");
      }
      return new AttrStep(json.pos, json.attr, json.value);
    }}]);
    return AttrStep;
  }(global);
  global.jsonID("attr", AttrStep);
  var DocAttrStep = function(_Step8) {
    function DocAttrStep(attr, value) {
      _classCallCheck(this, DocAttrStep);
      var _this10 = _super8.call(this);
      _this10.attr = attr;
      _this10.value = value;
      return _this10;
    }
    _inherits(DocAttrStep, _Step8);
    var _super8 = _createSuper(DocAttrStep);
    _createClass(DocAttrStep, [{key:"apply", value:function(doc) {
      var attrs = Object.create(null), name;
      for (name in doc.attrs) {
        attrs[name] = doc.attrs[name];
      }
      attrs[this.attr] = this.value;
      doc = doc.type.create(attrs, doc.content, doc.marks);
      return StepResult.ok(doc);
    }}, {key:"getMap", value:function() {
      return StepMap.empty;
    }}, {key:"invert", value:function(doc) {
      return new DocAttrStep(this.attr, doc.attrs[this.attr]);
    }}, {key:"map", value:function(mapping) {
      return this;
    }}, {key:"toJSON", value:function() {
      return {stepType:"docAttr", attr:this.attr, value:this.value};
    }}], [{key:"fromJSON", value:function(schema, json) {
      if ("string" != typeof json.attr) {
        throw new RangeError("Invalid input for DocAttrStep.fromJSON");
      }
      return new DocAttrStep(json.attr, json.value);
    }}]);
    return DocAttrStep;
  }(global);
  global.jsonID("docAttr", DocAttrStep);
  exports.TransformError = function(_Error) {
    function TransformError() {
      _classCallCheck(this, TransformError);
      return _super9.apply(this, arguments);
    }
    _inherits(TransformError, _Error);
    var _super9 = _createSuper(TransformError);
    return _createClass(TransformError);
  }(_wrapNativeSuper(Error));
  exports.TransformError = function TransformError(message) {
    message = Error.call(this, message);
    message.__proto__ = TransformError.prototype;
    return message;
  };
  exports.TransformError.prototype = Object.create(Error.prototype);
  exports.TransformError.prototype.constructor = exports.TransformError;
  exports.TransformError.prototype.name = "TransformError";
  require = function() {
    function Transform(doc) {
      _classCallCheck(this, Transform);
      this.doc = doc;
      this.steps = [];
      this.docs = [];
      this.mapping = new Mapping();
    }
    _createClass(Transform, [{key:"before", get:function() {
      return this.docs.length ? this.docs[0] : this.doc;
    }}, {key:"step", value:function(_step) {
      _step = this.maybeStep(_step);
      if (_step.failed) {
        throw new exports.TransformError(_step.failed);
      }
      return this;
    }}, {key:"maybeStep", value:function(step) {
      var result = step.apply(this.doc);
      result.failed || this.addStep(step, result.doc);
      return result;
    }}, {key:"docChanged", get:function() {
      return 0 < this.steps.length;
    }}, {key:"addStep", value:function(step, doc) {
      this.docs.push(this.doc);
      this.steps.push(step);
      this.mapping.appendMap(step.getMap());
      this.doc = doc;
    }}, {key:"replace", value:function(from) {
      var step = replaceStep(this.doc, from, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : from, 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : prosemirrorModel.Slice.empty);
      step && this.step(step);
      return this;
    }}, {key:"replaceWith", value:function(from, to, content) {
      return this.replace(from, to, new prosemirrorModel.Slice(prosemirrorModel.Fragment.from(content), 0, 0));
    }}, {key:"delete", value:function(from, to) {
      return this.replace(from, to, prosemirrorModel.Slice.empty);
    }}, {key:"insert", value:function(pos, content) {
      return this.replaceWith(pos, pos, content);
    }}, {key:"replaceRange", value:function(from, to, slice) {
      a: {
        if (slice.size) {
          var $from = this.doc.resolve(from), $to = this.doc.resolve(to);
          if (fitsTrivially($from, $to, slice)) {
            this.step(new ReplaceStep(from, to, slice));
          } else {
            var targetDepths = coveredDepths($from, this.doc.resolve(to));
            0 == targetDepths[targetDepths.length - 1] && targetDepths.pop();
            var preferredTarget = -($from.depth + 1);
            targetDepths.unshift(preferredTarget);
            for (var d = $from.depth, pos = $from.pos - 1; 0 < d; d--, pos--) {
              var spec = $from.node(d).type.spec;
              if (spec.defining || spec.definingAsContext || spec.isolating) {
                break;
              }
              -1 < targetDepths.indexOf(d) ? preferredTarget = d : $from.before(d) == pos && targetDepths.splice(1, 0, -d);
            }
            d = targetDepths.indexOf(preferredTarget);
            pos = [];
            spec = slice.openStart;
            for (var content = slice.content, i = 0;; i++) {
              content = content.firstChild;
              pos.push(content);
              if (i == slice.openStart) {
                break;
              }
              content = content.content;
            }
            for (i = spec - 1; 0 <= i; i--) {
              content = pos[i];
              var def = content.type;
              if ((def = def.spec.defining || def.spec.definingForContent) && !content.sameMarkup($from.node(Math.abs(preferredTarget) - 1))) {
                spec = i;
              } else if (def || !content.type.isTextblock) {
                break;
              }
            }
            for (preferredTarget = slice.openStart; 0 <= preferredTarget; preferredTarget--) {
              if (i = (preferredTarget + spec + 1) % (slice.openStart + 1), content = pos[i]) {
                for (def = 0; def < targetDepths.length; def++) {
                  var targetDepth = targetDepths[(def + d) % targetDepths.length], expand = !0;
                  0 > targetDepth && (expand = !1, targetDepth = -targetDepth);
                  var parent = $from.node(targetDepth - 1), index = $from.index(targetDepth - 1);
                  if (parent.canReplaceWith(index, index, content.type, content.marks)) {
                    this.replace($from.before(targetDepth), expand ? $to.after(targetDepth) : to, new prosemirrorModel.Slice(closeFragment(slice.content, 0, slice.openStart, i), i, slice.openEnd));
                    break a;
                  }
                }
              }
            }
            d = this.steps.length;
            for (pos = targetDepths.length - 1; 0 <= pos; pos--) {
              this.replace(from, to, slice);
              if (this.steps.length > d) {
                break;
              }
              spec = targetDepths[pos];
              0 > spec || (from = $from.before(spec), to = $to.after(spec));
            }
          }
        } else {
          this.deleteRange(from, to);
        }
      }
      return this;
    }}, {key:"replaceRangeWith", value:function(from, to, node) {
      if (!node.isInline && from == to && this.doc.resolve(from).parent.content.size) {
        var point = insertPoint(this.doc, from, node.type);
        null != point && (from = to = point);
      }
      this.replaceRange(from, to, new prosemirrorModel.Slice(prosemirrorModel.Fragment.from(node), 0, 0));
      return this;
    }}, {key:"deleteRange", value:function(from, to) {
      a: {
        for (var $from = this.doc.resolve(from), $to = this.doc.resolve(to), covered = coveredDepths($from, $to), i = 0; i < covered.length; i++) {
          var depth = covered[i], last = i == covered.length - 1;
          if (last && 0 == depth || $from.node(depth).type.contentMatch.validEnd) {
            this["delete"]($from.start(depth), $to.end(depth));
            break a;
          }
          if (0 < depth && (last || $from.node(depth - 1).canReplace($from.index(depth - 1), $to.indexAfter(depth - 1)))) {
            this["delete"]($from.before(depth), $to.after(depth));
            break a;
          }
        }
        for (covered = 1; covered <= $from.depth && covered <= $to.depth; covered++) {
          if (from - $from.start(covered) == $from.depth - covered && to > $from.end(covered) && $to.end(covered) - to != $to.depth - covered && $from.start(covered - 1) == $to.start(covered - 1) && $from.node(covered - 1).canReplace($from.index(covered - 1), $to.index(covered - 1))) {
            this["delete"]($from.before(covered), to);
            break a;
          }
        }
        this["delete"](from, to);
      }
      return this;
    }}, {key:"lift", value:function(range, target) {
      var $from = range.$from, $to = range.$to, depth = range.depth;
      range = $from.before(depth + 1);
      for (var gapEnd = $to.after(depth + 1), start = range, end = gapEnd, before = prosemirrorModel.Fragment.empty, openStart = 0, d = depth, splitting = !1; d > target; d--) {
        splitting || 0 < $from.index(d) ? (splitting = !0, before = prosemirrorModel.Fragment.from($from.node(d).copy(before)), openStart++) : start--;
      }
      $from = prosemirrorModel.Fragment.empty;
      d = 0;
      for (splitting = !1; depth > target; depth--) {
        splitting || $to.after(depth + 1) < $to.end(depth) ? (splitting = !0, $from = prosemirrorModel.Fragment.from($to.node(depth).copy($from)), d++) : end++;
      }
      this.step(new ReplaceAroundStep(start, end, range, gapEnd, new prosemirrorModel.Slice(before.append($from), openStart, d), before.size - openStart, !0));
      return this;
    }}, {key:"join", value:function(pos) {
      var depth = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 1, convertNewlines = null, linebreakReplacement = this.doc.type.schema.linebreakReplacement, $before = this.doc.resolve(pos - depth), beforeType = $before.node().type;
      if (linebreakReplacement && beforeType.inlineContent) {
        var pre = "pre" == beforeType.whitespace;
        linebreakReplacement = !!beforeType.contentMatch.matchType(linebreakReplacement);
        pre && !linebreakReplacement ? convertNewlines = !1 : !pre && linebreakReplacement && (convertNewlines = !0);
      }
      pre = this.steps.length;
      !1 === convertNewlines && (linebreakReplacement = this.doc.resolve(pos + depth), replaceLinebreaks(this, linebreakReplacement.node(), linebreakReplacement.before(), pre));
      beforeType.inlineContent && _clearIncompatible(this, pos + depth - 1, beforeType, $before.node().contentMatchAt($before.index()), null == convertNewlines);
      $before = this.mapping.slice(pre);
      beforeType = $before.map(pos - depth);
      this.step(new ReplaceStep(beforeType, $before.map(pos + depth, -1), prosemirrorModel.Slice.empty, !0));
      !0 === convertNewlines && (depth = this.doc.resolve(beforeType), replaceNewlines(this, depth.node(), depth.before(), this.steps.length));
      return this;
    }}, {key:"wrap", value:function(range, wrappers) {
      for (var content = prosemirrorModel.Fragment.empty, i = wrappers.length - 1; 0 <= i; i--) {
        if (content.size) {
          var match = wrappers[i].type.contentMatch.matchFragment(content);
          if (!match || !match.validEnd) {
            throw new RangeError("Wrapper type given to Transform.wrap does not form valid content of its parent wrapper");
          }
        }
        content = prosemirrorModel.Fragment.from(wrappers[i].type.create(wrappers[i].attrs, content));
      }
      i = range.start;
      range = range.end;
      this.step(new ReplaceAroundStep(i, range, i, range, new prosemirrorModel.Slice(content, 0, 0), wrappers.length, !0));
      return this;
    }}, {key:"setBlockType", value:function(from) {
      _setBlockType(this, from, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : from, 2 < arguments.length ? arguments[2] : void 0, 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : null);
      return this;
    }}, {key:"setNodeMarkup", value:function(pos, type$jscomp$0) {
      var type = type$jscomp$0, attrs = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : null, marks = 3 < arguments.length ? arguments[3] : void 0, node = this.doc.nodeAt(pos);
      if (!node) {
        throw new RangeError("No node at given position");
      }
      type || (type = node.type);
      attrs = type.create(attrs, null, marks || node.marks);
      if (node.isLeaf) {
        this.replaceWith(pos, pos + node.nodeSize, attrs);
      } else {
        if (!type.validContent(node.content)) {
          throw new RangeError("Invalid content for node type " + type.name);
        }
        this.step(new ReplaceAroundStep(pos, pos + node.nodeSize, pos + 1, pos + node.nodeSize - 1, new prosemirrorModel.Slice(prosemirrorModel.Fragment.from(attrs), 0, 0), 1, !0));
      }
      return this;
    }}, {key:"setNodeAttribute", value:function(pos, attr, value) {
      this.step(new AttrStep(pos, attr, value));
      return this;
    }}, {key:"setDocAttribute", value:function(attr, value) {
      this.step(new DocAttrStep(attr, value));
      return this;
    }}, {key:"addNodeMark", value:function(pos, mark) {
      this.step(new AddNodeMarkStep(pos, mark));
      return this;
    }}, {key:"removeNodeMark", value:function(pos, mark) {
      if (!(mark instanceof prosemirrorModel.Mark)) {
        var node = this.doc.nodeAt(pos);
        if (!node) {
          throw new RangeError("No node at position " + pos);
        }
        mark = mark.isInSet(node.marks);
        if (!mark) {
          return this;
        }
      }
      this.step(new RemoveNodeMarkStep(pos, mark));
      return this;
    }}, {key:"split", value:function(pos) {
      _split(this, pos, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 1, 2 < arguments.length ? arguments[2] : void 0);
      return this;
    }}, {key:"addMark", value:function(from, to, mark) {
      _addMark(this, from, to, mark);
      return this;
    }}, {key:"removeMark", value:function(from, to, mark) {
      _removeMark(this, from, to, mark);
      return this;
    }}, {key:"clearIncompatible", value:function(pos, parentType, match) {
      _clearIncompatible(this, pos, parentType, match);
      return this;
    }}]);
    return Transform;
  }();
  exports.AddMarkStep = AddMarkStep;
  exports.AddNodeMarkStep = AddNodeMarkStep;
  exports.AttrStep = AttrStep;
  exports.DocAttrStep = DocAttrStep;
  exports.MapResult = MapResult;
  exports.Mapping = Mapping;
  exports.RemoveMarkStep = RemoveMarkStep;
  exports.RemoveNodeMarkStep = RemoveNodeMarkStep;
  exports.ReplaceAroundStep = ReplaceAroundStep;
  exports.ReplaceStep = ReplaceStep;
  exports.Step = global;
  exports.StepMap = StepMap;
  exports.StepResult = StepResult;
  exports.Transform = require;
  exports.canJoin = function(doc, pos) {
    doc = doc.resolve(pos);
    pos = doc.index();
    var a = doc.nodeBefore, b = doc.nodeAfter;
    return !(!a || !b || a.isLeaf || !canAppendWithSubstitutedLinebreaks(a, b)) && doc.parent.canReplace(pos, pos + 1);
  };
  exports.canSplit = function(doc, pos) {
    var depth = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : 1, typesAfter = 3 < arguments.length ? arguments[3] : void 0, $pos = doc.resolve(pos), base = $pos.depth - depth, innerType = typesAfter && typesAfter[typesAfter.length - 1] || $pos.parent;
    if (0 > base || $pos.parent.type.spec.isolating || !$pos.parent.canReplace($pos.index(), $pos.parent.childCount) || !innerType.type.validContent($pos.parent.content.cutByIndex($pos.index(), $pos.parent.childCount))) {
      return !1;
    }
    innerType = $pos.depth - 1;
    for (depth -= 2; innerType > base; innerType--, depth--) {
      var node = $pos.node(innerType), _index = $pos.index(innerType);
      if (node.type.spec.isolating) {
        return !1;
      }
      var rest = node.content.cutByIndex(_index, node.childCount), overrideChild = typesAfter && typesAfter[depth + 1];
      overrideChild && (rest = rest.replaceChild(0, overrideChild.type.create(overrideChild.attrs)));
      overrideChild = typesAfter && typesAfter[depth] || node;
      if (!node.canReplace(_index + 1, node.childCount) || !overrideChild.type.validContent(rest)) {
        return !1;
      }
    }
    innerType = $pos.indexAfter(base);
    typesAfter = typesAfter && typesAfter[0];
    return $pos.node(base).canReplaceWith(innerType, innerType, typesAfter ? typesAfter.type : $pos.node(base + 1).type);
  };
  exports.dropPoint = function(doc, pos, slice) {
    doc = doc.resolve(pos);
    if (!slice.content.size) {
      return pos;
    }
    pos = slice.content;
    for (var i = 0; i < slice.openStart; i++) {
      pos = pos.firstChild.content;
    }
    for (i = 1; i <= (0 == slice.openStart && slice.size ? 2 : 1); i++) {
      for (var d = doc.depth; 0 <= d; d--) {
        var bias = d == doc.depth ? 0 : doc.pos <= (doc.start(d + 1) + doc.end(d + 1)) / 2 ? -1 : 1, insertPos = doc.index(d) + (0 < bias ? 1 : 0), parent = doc.node(d);
        if (1 == i) {
          insertPos = parent.canReplace(insertPos, insertPos, pos);
        } else {
          var wrapping = parent.contentMatchAt(insertPos).findWrapping(pos.firstChild.type);
          insertPos = wrapping && parent.canReplaceWith(insertPos, insertPos, wrapping[0]);
        }
        if (insertPos) {
          return 0 == bias ? doc.pos : 0 > bias ? doc.before(d + 1) : doc.after(d + 1);
        }
      }
    }
    return null;
  };
  exports.findWrapping = function(range, nodeType) {
    var attrs = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : null, innerRange = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : range;
    var around = range.parent;
    var startIndex = range.startIndex, endIndex = range.endIndex, around$jscomp$0 = around.contentMatchAt(startIndex).findWrapping(nodeType);
    around = around$jscomp$0 ? around.canReplaceWith(startIndex, endIndex, around$jscomp$0.length ? around$jscomp$0[0] : nodeType) ? around$jscomp$0 : null : null;
    if (startIndex = around) {
      if (startIndex = innerRange.parent, endIndex = innerRange.startIndex, innerRange = innerRange.endIndex, around$jscomp$0 = startIndex.child(endIndex), around$jscomp$0 = nodeType.contentMatch.findWrapping(around$jscomp$0.type)) {
        for (var innerMatch = (around$jscomp$0.length ? around$jscomp$0[around$jscomp$0.length - 1] : nodeType).contentMatch; innerMatch && endIndex < innerRange; endIndex++) {
          innerMatch = innerMatch.matchType(startIndex.child(endIndex).type);
        }
        startIndex = innerMatch && innerMatch.validEnd ? around$jscomp$0 : null;
      } else {
        startIndex = null;
      }
    }
    return (innerRange = startIndex) ? around.map(withAttrs).concat({type:nodeType, attrs}).concat(innerRange.map(withAttrs)) : null;
  };
  exports.insertPoint = insertPoint;
  exports.joinPoint = function(doc, pos) {
    for (var dir = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : -1, $pos = doc.resolve(pos), d = $pos.depth;; d--) {
      var index = $pos.index(d);
      if (d == $pos.depth) {
        var before = $pos.nodeBefore;
        var after = $pos.nodeAfter;
      } else {
        0 < dir ? (before = $pos.node(d + 1), index++, after = $pos.node(d).maybeChild(index)) : (before = $pos.node(d).maybeChild(index - 1), after = $pos.node(d + 1));
      }
      if (before && !before.isTextblock && before && after && !before.isLeaf && canAppendWithSubstitutedLinebreaks(before, after) && $pos.node(d).canReplace(index, index + 1)) {
        return pos;
      }
      if (0 == d) {
        break;
      }
      pos = 0 > dir ? $pos.before(d) : $pos.after(d);
    }
  };
  exports.liftTarget = function(range) {
    for (var content = range.parent.content.cutByIndex(range.startIndex, range.endIndex), depth = range.depth;; --depth) {
      var node = range.$from.node(depth), index = range.$from.index(depth), endIndex = range.$to.indexAfter(depth);
      if (depth < range.depth && node.canReplace(index, endIndex, content)) {
        return depth;
      }
      var JSCompiler_temp;
      (JSCompiler_temp = 0 == depth || node.type.spec.isolating) || (JSCompiler_temp = !((0 == index || node.canReplace(index, node.childCount)) && (endIndex == node.childCount || node.canReplace(0, endIndex))));
      if (JSCompiler_temp) {
        break;
      }
    }
    return null;
  };
  exports.replaceStep = replaceStep;
};

//# sourceMappingURL=module$node_modules$prosemirror_transform$dist$index_cjs.js.map
