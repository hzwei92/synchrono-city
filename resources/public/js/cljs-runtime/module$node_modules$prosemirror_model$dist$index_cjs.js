shadow$provide.module$node_modules$prosemirror_model$dist$index_cjs = function(global, require, module, exports) {
  function _createForOfIteratorHelper(o, allowArrayLike) {
    var it = "undefined" !== typeof Symbol && o[Symbol.iterator] || o["@@iterator"];
    if (!it) {
      if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && "number" === typeof o.length) {
        it && (o = it);
        var i = 0;
        allowArrayLike = function() {
        };
        return {s:allowArrayLike, n:function() {
          return i >= o.length ? {done:!0} : {done:!1, value:o[i++]};
        }, e:function(_e) {
          throw _e;
        }, f:allowArrayLike};
      }
      throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    var normalCompletion = !0, didErr = !1, err;
    return {s:function() {
      it = it.call(o);
    }, n:function() {
      var step = it.next();
      normalCompletion = step.done;
      return step;
    }, e:function(_e2) {
      didErr = !0;
      err = _e2;
    }, f:function() {
      try {
        if (!normalCompletion && null != it["return"]) {
          it["return"]();
        }
      } finally {
        if (didErr) {
          throw err;
        }
      }
    }};
  }
  function _unsupportedIterableToArray(o, minLen) {
    if (o) {
      if ("string" === typeof o) {
        return _arrayLikeToArray(o, minLen);
      }
      var n = Object.prototype.toString.call(o).slice(8, -1);
      "Object" === n && o.constructor && (n = o.constructor.name);
      if ("Map" === n || "Set" === n) {
        return Array.from(o);
      }
      if ("Arguments" === n || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) {
        return _arrayLikeToArray(o, minLen);
      }
    }
  }
  function _arrayLikeToArray(arr, len) {
    if (null == len || len > arr.length) {
      len = arr.length;
    }
    for (var i = 0, arr2 = Array(len); i < len; i++) {
      arr2[i] = arr[i];
    }
    return arr2;
  }
  function _get() {
    _get = "undefined" !== typeof Reflect && Reflect.get ? Reflect.get.bind() : function(target, property, receiver) {
      var base;
      for (base = target; !Object.prototype.hasOwnProperty.call(base, property) && (base = _getPrototypeOf(base), null !== base);) {
      }
      if (base) {
        return base = Object.getOwnPropertyDescriptor(base, property), base.get ? base.get.call(3 > arguments.length ? target : receiver) : base.value;
      }
    };
    return _get.apply(this, arguments);
  }
  function _inherits(subClass, superClass) {
    if ("function" !== typeof superClass && null !== superClass) {
      throw new TypeError("Super expression must either be null or a function");
    }
    subClass.prototype = Object.create(superClass && superClass.prototype, {constructor:{value:subClass, writable:!0, configurable:!0}});
    Object.defineProperty(subClass, "prototype", {writable:!1});
    superClass && _setPrototypeOf(subClass, superClass);
  }
  function _createSuper(Derived) {
    var hasNativeReflectConstruct = _isNativeReflectConstruct();
    return function() {
      var Super = _getPrototypeOf(Derived);
      if (hasNativeReflectConstruct) {
        var NewTarget = _getPrototypeOf(this).constructor;
        Super = Reflect.construct(Super, arguments, NewTarget);
      } else {
        Super = Super.apply(this, arguments);
      }
      if (!Super || "object" !== _typeof(Super) && "function" !== typeof Super) {
        if (void 0 !== Super) {
          throw new TypeError("Derived constructors may only return object or undefined");
        }
        if (void 0 === this) {
          throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
        }
        Super = this;
      }
      return Super;
    };
  }
  function _wrapNativeSuper(Class) {
    var _cache = "function" === typeof Map ? new Map() : void 0;
    _wrapNativeSuper = function(Class) {
      function Wrapper() {
        return _construct(Class, arguments, _getPrototypeOf(this).constructor);
      }
      if (null === Class || !_isNativeFunction(Class)) {
        return Class;
      }
      if ("function" !== typeof Class) {
        throw new TypeError("Super expression must either be null or a function");
      }
      if ("undefined" !== typeof _cache) {
        if (_cache.has(Class)) {
          return _cache.get(Class);
        }
        _cache.set(Class, Wrapper);
      }
      Wrapper.prototype = Object.create(Class.prototype, {constructor:{value:Wrapper, enumerable:!1, writable:!0, configurable:!0}});
      return _setPrototypeOf(Wrapper, Class);
    };
    return _wrapNativeSuper(Class);
  }
  function _construct(Parent, args, Class) {
    _construct = _isNativeReflectConstruct() ? Reflect.construct.bind() : function(Parent, args, Class) {
      var a = [null];
      a.push.apply(a, args);
      Parent = new (Function.bind.apply(Parent, a))();
      Class && _setPrototypeOf(Parent, Class.prototype);
      return Parent;
    };
    return _construct.apply(null, arguments);
  }
  function _isNativeReflectConstruct() {
    if ("undefined" === typeof Reflect || !Reflect.construct || Reflect.construct.sham) {
      return !1;
    }
    if ("function" === typeof Proxy) {
      return !0;
    }
    try {
      return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
      })), !0;
    } catch (e) {
      return !1;
    }
  }
  function _isNativeFunction(fn) {
    try {
      return -1 !== Function.toString.call(fn).indexOf("[native code]");
    } catch (e) {
      return "function" === typeof fn;
    }
  }
  function _setPrototypeOf(o, p) {
    _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(o, p) {
      o.__proto__ = p;
      return o;
    };
    return _setPrototypeOf(o, p);
  }
  function _getPrototypeOf(o) {
    _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(o) {
      return o.__proto__ || Object.getPrototypeOf(o);
    };
    return _getPrototypeOf(o);
  }
  function _typeof(o) {
    "@babel/helpers - typeof";
    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
      return typeof o;
    } : function(o) {
      return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
    }, _typeof(o);
  }
  function _classCallCheck(instance, Constructor) {
    if (!(instance instanceof Constructor)) {
      throw new TypeError("Cannot call a class as a function");
    }
  }
  function _defineProperties(target, props) {
    for (var i = 0; i < props.length; i++) {
      var descriptor = props[i];
      descriptor.enumerable = descriptor.enumerable || !1;
      descriptor.configurable = !0;
      "value" in descriptor && (descriptor.writable = !0);
      var JSCompiler_temp_const = Object, JSCompiler_temp_const$jscomp$0 = JSCompiler_temp_const.defineProperty;
      a: {
        var JSCompiler_inline_result = descriptor.key;
        if ("object" === _typeof(JSCompiler_inline_result) && null !== JSCompiler_inline_result) {
          var prim = JSCompiler_inline_result[Symbol.toPrimitive];
          if (void 0 !== prim) {
            JSCompiler_inline_result = prim.call(JSCompiler_inline_result, "string");
            if ("object" !== _typeof(JSCompiler_inline_result)) {
              break a;
            }
            throw new TypeError("@@toPrimitive must return a primitive value.");
          }
          JSCompiler_inline_result = String(JSCompiler_inline_result);
        }
      }
      JSCompiler_inline_result = "symbol" === _typeof(JSCompiler_inline_result) ? JSCompiler_inline_result : String(JSCompiler_inline_result);
      JSCompiler_temp_const$jscomp$0.call(JSCompiler_temp_const, target, JSCompiler_inline_result, descriptor);
    }
  }
  function _createClass(Constructor, protoProps, staticProps) {
    protoProps && _defineProperties(Constructor.prototype, protoProps);
    staticProps && _defineProperties(Constructor, staticProps);
    Object.defineProperty(Constructor, "prototype", {writable:!1});
    return Constructor;
  }
  function _findDiffStart(a, b, pos) {
    for (var i = 0;; i++) {
      if (i == a.childCount || i == b.childCount) {
        return a.childCount == b.childCount ? null : pos;
      }
      var childA = a.child(i), childB = b.child(i);
      if (childA != childB) {
        if (!childA.sameMarkup(childB)) {
          return pos;
        }
        if (childA.isText && childA.text != childB.text) {
          for (a = 0; childA.text[a] == childB.text[a]; a++) {
            pos++;
          }
          return pos;
        }
        if (childA.content.size || childB.content.size) {
          if (childB = _findDiffStart(childA.content, childB.content, pos + 1), null != childB) {
            return childB;
          }
        }
      }
      pos += childA.nodeSize;
    }
  }
  function _findDiffEnd(a, b, posA, posB) {
    for (var iA = a.childCount, iB = b.childCount;;) {
      if (0 == iA || 0 == iB) {
        return iA == iB ? null : {a:posA, b:posB};
      }
      var childA = a.child(--iA), childB = b.child(--iB), size = childA.nodeSize;
      if (childA != childB) {
        if (!childA.sameMarkup(childB)) {
          return {a:posA, b:posB};
        }
        if (childA.isText && childA.text != childB.text) {
          a = 0;
          for (b = Math.min(childA.text.length, childB.text.length); a < b && childA.text[childA.text.length - a - 1] == childB.text[childB.text.length - a - 1];) {
            a++, posA--, posB--;
          }
          return {a:posA, b:posB};
        }
        if (childA.content.size || childB.content.size) {
          if (childA = _findDiffEnd(childA.content, childB.content, posA - 1, posB - 1)) {
            return childA;
          }
        }
      }
      posA -= size;
      posB -= size;
    }
  }
  function retIndex(index, offset) {
    found.index = index;
    found.offset = offset;
    return found;
  }
  function compareDeep(a, b) {
    if (a === b) {
      return !0;
    }
    if (!a || "object" != _typeof(a) || !b || "object" != _typeof(b)) {
      return !1;
    }
    var array = Array.isArray(a);
    if (Array.isArray(b) != array) {
      return !1;
    }
    if (array) {
      if (a.length != b.length) {
        return !1;
      }
      for (array = 0; array < a.length; array++) {
        if (!compareDeep(a[array], b[array])) {
          return !1;
        }
      }
    } else {
      for (var p in a) {
        if (!(p in b && compareDeep(a[p], b[p]))) {
          return !1;
        }
      }
      for (var _p in b) {
        if (!(_p in a)) {
          return !1;
        }
      }
    }
    return !0;
  }
  function removeRange(content, from, to) {
    var _content$findIndex = content.findIndex(from), index = _content$findIndex.index;
    _content$findIndex = _content$findIndex.offset;
    var child = content.maybeChild(index), _content$findIndex2 = content.findIndex(to), indexTo = _content$findIndex2.index;
    _content$findIndex2 = _content$findIndex2.offset;
    if (_content$findIndex == from || child.isText) {
      if (_content$findIndex2 != to && !content.child(indexTo).isText) {
        throw new RangeError("Removing non-flat range");
      }
      return content.cut(0, from).append(content.cut(to));
    }
    if (index != indexTo) {
      throw new RangeError("Removing non-flat range");
    }
    return content.replaceChild(index, child.copy(removeRange(child.content, from - _content$findIndex - 1, to - _content$findIndex - 1)));
  }
  function insertInto(content, dist, insert, parent) {
    var _content$findIndex3 = content.findIndex(dist), index = _content$findIndex3.index, offset = _content$findIndex3.offset;
    _content$findIndex3 = content.maybeChild(index);
    return offset == dist || _content$findIndex3.isText ? parent && !parent.canReplace(index, index, insert) ? null : content.cut(0, dist).append(insert).append(content.cut(dist)) : (dist = insertInto(_content$findIndex3.content, dist - offset - 1, insert)) && content.replaceChild(index, _content$findIndex3.copy(dist));
  }
  function replaceOuter($from, $to, slice, depth) {
    var JSCompiler_object_inline_start_585 = $from.index(depth);
    var node = $from.node(depth);
    if (JSCompiler_object_inline_start_585 == $to.index(depth) && depth < $from.depth - slice.openStart) {
      return $from = replaceOuter($from, $to, slice, depth + 1), node.copy(node.content.replaceChild(JSCompiler_object_inline_start_585, $from));
    }
    if (slice.content.size) {
      if (slice.openStart || slice.openEnd || $from.depth != depth || $to.depth != depth) {
        var extra = $from.depth - slice.openStart, node$jscomp$0 = $from.node(extra).copy(slice.content);
        for (JSCompiler_object_inline_start_585 = extra - 1; 0 <= JSCompiler_object_inline_start_585; JSCompiler_object_inline_start_585--) {
          node$jscomp$0 = $from.node(JSCompiler_object_inline_start_585).copy(Fragment.from(node$jscomp$0));
        }
        JSCompiler_object_inline_start_585 = node$jscomp$0.resolveNoCache(slice.openStart + extra);
        slice = node$jscomp$0.resolveNoCache(node$jscomp$0.content.size - slice.openEnd - extra);
        return close(node, replaceThreeWay($from, JSCompiler_object_inline_start_585, slice, $to, depth));
      }
      node = $from.parent;
      depth = node.content;
      return close(node, depth.cut(0, $from.parentOffset).append(slice.content).append(depth.cut($to.parentOffset)));
    }
    return close(node, replaceTwoWay($from, $to, depth));
  }
  function checkJoin(main, sub) {
    if (!sub.type.compatibleContent(main.type)) {
      throw new ReplaceError("Cannot join " + sub.type.name + " onto " + main.type.name);
    }
  }
  function joinable($before, $after, depth) {
    $before = $before.node(depth);
    checkJoin($before, $after.node(depth));
    return $before;
  }
  function addNode(child, target) {
    var last = target.length - 1;
    0 <= last && child.isText && child.sameMarkup(target[last]) ? target[last] = child.withText(target[last].text + child.text) : target.push(child);
  }
  function addRange($start, $end, depth, target) {
    var node = ($end || $start).node(depth), startIndex = 0, endIndex = $end ? $end.index(depth) : node.childCount;
    $start && (startIndex = $start.index(depth), $start.depth > depth ? startIndex++ : $start.textOffset && (addNode($start.nodeAfter, target), startIndex++));
    for ($start = startIndex; $start < endIndex; $start++) {
      addNode(node.child($start), target);
    }
    $end && $end.depth == depth && $end.textOffset && addNode($end.nodeBefore, target);
  }
  function close(node, content) {
    node.type.checkContent(content);
    return node.copy(content);
  }
  function replaceThreeWay($from, $start, $end, $to, depth) {
    var openStart = $from.depth > depth && joinable($from, $start, depth + 1), openEnd = $to.depth > depth && joinable($end, $to, depth + 1), content = [];
    addRange(null, $from, depth, content);
    openStart && openEnd && $start.index(depth) == $end.index(depth) ? (checkJoin(openStart, openEnd), addNode(close(openStart, replaceThreeWay($from, $start, $end, $to, depth + 1)), content)) : (openStart && addNode(close(openStart, replaceTwoWay($from, $start, depth + 1)), content), addRange($start, $end, depth, content), openEnd && addNode(close(openEnd, replaceTwoWay($end, $to, depth + 1)), content));
    addRange($to, null, depth, content);
    return new Fragment(content);
  }
  function replaceTwoWay($from, $to, depth) {
    var content = [];
    addRange(null, $from, depth, content);
    if ($from.depth > depth) {
      var type = joinable($from, $to, depth + 1);
      addNode(close(type, replaceTwoWay($from, $to, depth + 1)), content);
    }
    addRange($to, null, depth, content);
    return new Fragment(content);
  }
  function wrapMarks(marks, str) {
    for (var i = marks.length - 1; 0 <= i; i--) {
      str = marks[i].type.name + "(" + str + ")";
    }
    return str;
  }
  function parseExpr(stream$jscomp$2) {
    var exprs = [];
    do {
      var JSCompiler_temp_const = exprs, JSCompiler_temp_const$jscomp$0 = JSCompiler_temp_const.push, stream = stream$jscomp$2, exprs$jscomp$0 = [];
      do {
        for (var JSCompiler_temp_const$jscomp$1 = exprs$jscomp$0, JSCompiler_temp_const$jscomp$2 = JSCompiler_temp_const$jscomp$1.push, stream$jscomp$0 = stream, expr = parseExprAtom(stream$jscomp$0);;) {
          if (stream$jscomp$0.eat("+")) {
            expr = {type:"plus", expr};
          } else if (stream$jscomp$0.eat("*")) {
            expr = {type:"star", expr};
          } else if (stream$jscomp$0.eat("?")) {
            expr = {type:"opt", expr};
          } else if (stream$jscomp$0.eat("{")) {
            var stream$jscomp$1 = stream$jscomp$0, min = parseNum(stream$jscomp$1), max = min;
            stream$jscomp$1.eat(",") && (max = "}" != stream$jscomp$1.next ? parseNum(stream$jscomp$1) : -1);
            stream$jscomp$1.eat("}") || stream$jscomp$1.err("Unclosed braced range");
            expr = {type:"range", min, max, expr};
          } else {
            break;
          }
        }
        JSCompiler_temp_const$jscomp$2.call(JSCompiler_temp_const$jscomp$1, expr);
      } while (stream.next && ")" != stream.next && "|" != stream.next);
      JSCompiler_temp_const$jscomp$0.call(JSCompiler_temp_const, 1 == exprs$jscomp$0.length ? exprs$jscomp$0[0] : {type:"seq", exprs:exprs$jscomp$0});
    } while (stream$jscomp$2.eat("|"));
    return 1 == exprs.length ? exprs[0] : {type:"choice", exprs};
  }
  function parseNum(stream) {
    /\D/.test(stream.next) && stream.err("Expected number, got '" + stream.next + "'");
    var result = Number(stream.next);
    stream.pos++;
    return result;
  }
  function resolveName(stream, name) {
    var types = stream.nodeTypes, type = types[name];
    if (type) {
      return [type];
    }
    type = [];
    for (var typeName in types) {
      var _type = types[typeName];
      _type.isInGroup(name) && type.push(_type);
    }
    0 == type.length && stream.err("No node type or group '" + name + "' found");
    return type;
  }
  function parseExprAtom(stream) {
    if (stream.eat("(")) {
      var expr = parseExpr(stream);
      stream.eat(")") || stream.err("Missing closing paren");
      return expr;
    }
    if (/\W/.test(stream.next)) {
      stream.err("Unexpected token '" + stream.next + "'");
    } else {
      return expr = resolveName(stream, stream.next).map(function(type) {
        null == stream.inline ? stream.inline = type.isInline : stream.inline != type.isInline && stream.err("Mixing inline and block content");
        return {type:"name", value:type};
      }), stream.pos++, 1 == expr.length ? expr[0] : {type:"choice", exprs:expr};
    }
  }
  function nfa(expr) {
    function node() {
      return nfa.push([]) - 1;
    }
    function edge(from, to, term) {
      to = {term, to};
      nfa[from].push(to);
      return to;
    }
    function connect(edges, to) {
      edges.forEach(function(edge) {
        return edge.to = to;
      });
    }
    function compile(expr, from) {
      if ("choice" == expr.type) {
        return expr.exprs.reduce(function(out, expr) {
          return out.concat(compile(expr, from));
        }, []);
      }
      if ("seq" == expr.type) {
        for (var i = 0;; i++) {
          var next = compile(expr.exprs[i], from);
          if (i == expr.exprs.length - 1) {
            return next;
          }
          connect(next, from = node());
        }
      } else {
        if ("star" == expr.type) {
          return i = node(), edge(from, i), connect(compile(expr.expr, i), i), [edge(i)];
        }
        if ("plus" == expr.type) {
          return i = node(), connect(compile(expr.expr, from), i), connect(compile(expr.expr, i), i), [edge(i)];
        }
        if ("opt" == expr.type) {
          return [edge(from)].concat(compile(expr.expr, from));
        }
        if ("range" == expr.type) {
          i = from;
          for (next = 0; next < expr.min; next++) {
            var _next = node();
            connect(compile(expr.expr, i), _next);
            i = _next;
          }
          if (-1 == expr.max) {
            connect(compile(expr.expr, i), i);
          } else {
            for (next = expr.min; next < expr.max; next++) {
              _next = node(), edge(i, _next), connect(compile(expr.expr, i), _next), i = _next;
            }
          }
          return [edge(i)];
        }
        if ("name" == expr.type) {
          return [edge(from, void 0, expr.value)];
        }
        throw Error("Unknown expr type");
      }
    }
    var nfa = [[]];
    connect(compile(expr, 0), node());
    return nfa;
  }
  function cmp(a, b) {
    return b - a;
  }
  function nullFrom(nfa, node) {
    function scan(node) {
      var edges = nfa[node];
      if (1 == edges.length && !edges[0].term) {
        return scan(edges[0].to);
      }
      result.push(node);
      for (node = 0; node < edges.length; node++) {
        var _edges$i = edges[node], to = _edges$i.to;
        _edges$i.term || -1 != result.indexOf(to) || scan(to);
      }
    }
    var result = [];
    scan(node);
    return result.sort(cmp);
  }
  function dfa(nfa) {
    function explore(states) {
      var out = [];
      states.forEach(function(node) {
        nfa[node].forEach(function(_ref) {
          var term = _ref.term;
          _ref = _ref.to;
          if (term) {
            for (var set, i = 0; i < out.length; i++) {
              out[i][0] == term && (set = out[i][1]);
            }
            nullFrom(nfa, _ref).forEach(function(node) {
              set || out.push([term, set = []]);
              -1 == set.indexOf(node) && set.push(node);
            });
          }
        });
      });
      states = labeled[states.join(",")] = new ContentMatch(-1 < states.indexOf(nfa.length - 1));
      for (var i = 0; i < out.length; i++) {
        var _states = out[i][1].sort(cmp);
        states.next.push({type:out[i][0], next:labeled[_states.join(",")] || explore(_states)});
      }
      return states;
    }
    var labeled = Object.create(null);
    return explore(nullFrom(nfa, 0));
  }
  function defaultAttrs(attrs) {
    var defaults = Object.create(null), attrName;
    for (attrName in attrs) {
      var attr = attrs[attrName];
      if (!attr.hasDefault) {
        return null;
      }
      defaults[attrName] = attr["default"];
    }
    return defaults;
  }
  function _computeAttrs(attrs, value) {
    var built = Object.create(null), name;
    for (name in attrs) {
      var given = value && value[name];
      if (void 0 === given) {
        if (given = attrs[name], given.hasDefault) {
          given = given["default"];
        } else {
          throw new RangeError("No value supplied for attribute " + name);
        }
      }
      built[name] = given;
    }
    return built;
  }
  function _checkAttrs(attrs, values, type, name) {
    for (var _name in values) {
      if (!(_name in attrs)) {
        throw new RangeError("Unsupported attribute ".concat(_name, " for ").concat(type, " of type ").concat(_name));
      }
    }
    for (var _name2 in attrs) {
      type = attrs[_name2], type.validate && type.validate(values[_name2]);
    }
  }
  function initAttrs(typeName, attrs) {
    var result = Object.create(null);
    if (attrs) {
      for (var name in attrs) {
        result[name] = new Attribute(typeName, name, attrs[name]);
      }
    }
    return result;
  }
  function validateType(typeName, attrName, type) {
    var types = type.split("|");
    return function(value) {
      value = null === value ? "null" : _typeof(value);
      if (0 > types.indexOf(value)) {
        throw new RangeError("Expected value of type ".concat(types, " for attribute ").concat(attrName, " on type ").concat(typeName, ", got ").concat(value));
      }
    };
  }
  function gatherMarks(schema, marks) {
    for (var found = [], i = 0; i < marks.length; i++) {
      var name = marks[i], mark = schema.marks[name], ok = mark;
      if (mark) {
        found.push(mark);
      } else {
        for (var prop in schema.marks) {
          mark = schema.marks[prop], ("_" == name || mark.spec.group && -1 < mark.spec.group.split(" ").indexOf(name)) && found.push(ok = mark);
        }
      }
      if (!ok) {
        throw new SyntaxError("Unknown mark type: '" + marks[i] + "'");
      }
    }
    return found;
  }
  function wsOptionsFor(type, preserveWhitespace, base) {
    return null != preserveWhitespace ? (preserveWhitespace ? 1 : 0) | ("full" === preserveWhitespace ? 2 : 0) : type && "pre" == type.whitespace ? 3 : base & -5;
  }
  function copy(obj) {
    var copy = {}, prop;
    for (prop in obj) {
      copy[prop] = obj[prop];
    }
    return copy;
  }
  function markMayApply(markType, nodeType) {
    var nodes = nodeType.schema.nodes, _loop5 = function() {
      var parent = nodes[name];
      if (!parent.allowsMarkType(markType)) {
        return 0;
      }
      var seen = [];
      if (function scan(match) {
        seen.push(match);
        for (var i = 0; i < match.edgeCount; i++) {
          var _match$edge = match.edge(i), next = _match$edge.next;
          if (_match$edge.type == nodeType || 0 > seen.indexOf(next) && scan(next)) {
            return !0;
          }
        }
      }(parent.contentMatch)) {
        return {v:!0};
      }
    }, name;
    for (name in nodes) {
      var _ret2 = _loop5();
      if (0 !== _ret2 && _ret2) {
        return _ret2.v;
      }
    }
  }
  function gatherToDOM(obj) {
    var result = {}, name;
    for (name in obj) {
      var toDOM = obj[name].spec.toDOM;
      toDOM && (result[name] = toDOM);
    }
    return result;
  }
  function suspiciousAttributesInner(attrs) {
    function scan(value) {
      if (value && "object" == _typeof(value)) {
        if (Array.isArray(value)) {
          if ("string" == typeof value[0]) {
            result || (result = []), result.push(value);
          } else {
            for (var i = 0; i < value.length; i++) {
              scan(value[i]);
            }
          }
        } else {
          for (i in value) {
            scan(value[i]);
          }
        }
      }
    }
    var result = null;
    scan(attrs);
    return result;
  }
  function _renderSpec(doc, structure, xmlNS, blockArraysIn) {
    if ("string" == typeof structure) {
      return {dom:doc.createTextNode(structure)};
    }
    if (null != structure.nodeType) {
      return {dom:structure};
    }
    if (structure.dom && null != structure.dom.nodeType) {
      return structure;
    }
    var tagName = structure[0];
    if ("string" != typeof tagName) {
      throw new RangeError("Invalid array passed to renderSpec");
    }
    var JSCompiler_temp;
    if (JSCompiler_temp = blockArraysIn) {
      var suspicious = suspiciousAttributeCache.get(blockArraysIn);
      void 0 === suspicious && suspiciousAttributeCache.set(blockArraysIn, suspicious = suspiciousAttributesInner(blockArraysIn));
      JSCompiler_temp = suspicious;
    }
    if (JSCompiler_temp && -1 < suspicious.indexOf(structure)) {
      throw new RangeError("Using an array from an attribute object as a DOM spec. This may be an attempted cross site scripting attack.");
    }
    suspicious = tagName.indexOf(" ");
    0 < suspicious && (xmlNS = tagName.slice(0, suspicious), tagName = tagName.slice(suspicious + 1));
    tagName = xmlNS ? doc.createElementNS(xmlNS, tagName) : doc.createElement(tagName);
    JSCompiler_temp = structure[1];
    suspicious = 1;
    if (JSCompiler_temp && "object" == _typeof(JSCompiler_temp) && null == JSCompiler_temp.nodeType && !Array.isArray(JSCompiler_temp)) {
      suspicious = 2;
      for (var name in JSCompiler_temp) {
        if (null != JSCompiler_temp[name]) {
          var _space = name.indexOf(" ");
          0 < _space ? tagName.setAttributeNS(name.slice(0, _space), name.slice(_space + 1), JSCompiler_temp[name]) : tagName.setAttribute(name, JSCompiler_temp[name]);
        }
      }
    }
    for (name = suspicious; name < structure.length; name++) {
      JSCompiler_temp = structure[name];
      if (0 === JSCompiler_temp) {
        if (name < structure.length - 1 || name > suspicious) {
          throw new RangeError("Content hole must be the only child of its parent node");
        }
        return {dom:tagName, contentDOM:tagName};
      }
      JSCompiler_temp = _renderSpec(doc, JSCompiler_temp, xmlNS, blockArraysIn);
      _space = JSCompiler_temp.contentDOM;
      tagName.appendChild(JSCompiler_temp.dom);
      if (_space) {
        if (contentDOM) {
          throw new RangeError("Multiple content holes");
        }
        var contentDOM = _space;
      }
    }
    return {dom:tagName, contentDOM};
  }
  var OrderedMap = require("module$node_modules$orderedmap$dist$index_cjs"), Fragment = function() {
    function Fragment(content, size) {
      _classCallCheck(this, Fragment);
      this.content = content;
      this.size = size || 0;
      if (null == size) {
        for (size = 0; size < content.length; size++) {
          this.size += content[size].nodeSize;
        }
      }
    }
    _createClass(Fragment, [{key:"nodesBetween", value:function(from, to, f) {
      for (var nodeStart = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : 0, parent = 4 < arguments.length ? arguments[4] : void 0, i = 0, pos = 0; pos < to; i++) {
        var child = this.content[i], end = pos + child.nodeSize;
        end > from && !1 !== f(child, nodeStart + pos, parent || null, i) && child.content.size && (pos += 1, child.nodesBetween(Math.max(0, from - pos), Math.min(child.content.size, to - pos), f, nodeStart + pos));
        pos = end;
      }
    }}, {key:"descendants", value:function(f) {
      this.nodesBetween(0, this.size, f);
    }}, {key:"textBetween", value:function(from, to, blockSeparator, leafText) {
      var text = "", first = !0;
      this.nodesBetween(from, to, function(node, pos) {
        pos = node.isText ? node.text.slice(Math.max(from, pos) - pos, to - pos) : node.isLeaf ? leafText ? "function" === typeof leafText ? leafText(node) : leafText : node.type.spec.leafText ? node.type.spec.leafText(node) : "" : "";
        node.isBlock && (node.isLeaf && pos || node.isTextblock) && blockSeparator && (first ? first = !1 : text += blockSeparator);
        text += pos;
      }, 0);
      return text;
    }}, {key:"append", value:function(other) {
      if (!other.size) {
        return this;
      }
      if (!this.size) {
        return other;
      }
      var last = this.lastChild, first = other.firstChild, content = this.content.slice(), i = 0;
      last.isText && last.sameMarkup(first) && (content[content.length - 1] = last.withText(last.text + first.text), i = 1);
      for (; i < other.content.length; i++) {
        content.push(other.content[i]);
      }
      return new Fragment(content, this.size + other.size);
    }}, {key:"cut", value:function(from) {
      var to = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : this.size;
      if (0 == from && to == this.size) {
        return this;
      }
      var result = [], size = 0;
      if (to > from) {
        for (var i = 0, pos = 0; pos < to; i++) {
          var child = this.content[i], end = pos + child.nodeSize;
          if (end > from) {
            if (pos < from || end > to) {
              child = child.isText ? child.cut(Math.max(0, from - pos), Math.min(child.text.length, to - pos)) : child.cut(Math.max(0, from - pos - 1), Math.min(child.content.size, to - pos - 1));
            }
            result.push(child);
            size += child.nodeSize;
          }
          pos = end;
        }
      }
      return new Fragment(result, size);
    }}, {key:"cutByIndex", value:function(from, to) {
      return from == to ? Fragment.empty : 0 == from && to == this.content.length ? this : new Fragment(this.content.slice(from, to));
    }}, {key:"replaceChild", value:function(index, node) {
      var current = this.content[index];
      if (current == node) {
        return this;
      }
      var copy = this.content.slice();
      current = this.size + node.nodeSize - current.nodeSize;
      copy[index] = node;
      return new Fragment(copy, current);
    }}, {key:"addToStart", value:function(node) {
      return new Fragment([node].concat(this.content), this.size + node.nodeSize);
    }}, {key:"addToEnd", value:function(node) {
      return new Fragment(this.content.concat(node), this.size + node.nodeSize);
    }}, {key:"eq", value:function(other) {
      if (this.content.length != other.content.length) {
        return !1;
      }
      for (var i = 0; i < this.content.length; i++) {
        if (!this.content[i].eq(other.content[i])) {
          return !1;
        }
      }
      return !0;
    }}, {key:"firstChild", get:function() {
      return this.content.length ? this.content[0] : null;
    }}, {key:"lastChild", get:function() {
      return this.content.length ? this.content[this.content.length - 1] : null;
    }}, {key:"childCount", get:function() {
      return this.content.length;
    }}, {key:"child", value:function(index) {
      var found = this.content[index];
      if (!found) {
        throw new RangeError("Index " + index + " out of range for " + this);
      }
      return found;
    }}, {key:"maybeChild", value:function(index) {
      return this.content[index] || null;
    }}, {key:"forEach", value:function(f) {
      for (var i = 0, p = 0; i < this.content.length; i++) {
        var child = this.content[i];
        f(child, p, i);
        p += child.nodeSize;
      }
    }}, {key:"findDiffStart", value:function(other) {
      return _findDiffStart(this, other, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 0);
    }}, {key:"findDiffEnd", value:function(other) {
      return _findDiffEnd(this, other, 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : this.size, 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : other.size);
    }}, {key:"findIndex", value:function(pos) {
      var round = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : -1;
      if (0 == pos) {
        return retIndex(0, pos);
      }
      if (pos == this.size) {
        return retIndex(this.content.length, pos);
      }
      if (pos > this.size || 0 > pos) {
        throw new RangeError("Position ".concat(pos, " outside of fragment (").concat(this, ")"));
      }
      for (var i = 0, curPos = 0;; i++) {
        var cur = this.child(i);
        cur = curPos + cur.nodeSize;
        if (cur >= pos) {
          return cur == pos || 0 < round ? retIndex(i + 1, cur) : retIndex(i, curPos);
        }
        curPos = cur;
      }
    }}, {key:"toString", value:function() {
      return "\x3c" + this.toStringInner() + "\x3e";
    }}, {key:"toStringInner", value:function() {
      return this.content.join(", ");
    }}, {key:"toJSON", value:function() {
      return this.content.length ? this.content.map(function(n) {
        return n.toJSON();
      }) : null;
    }}], [{key:"fromJSON", value:function(schema, value) {
      if (!value) {
        return Fragment.empty;
      }
      if (!Array.isArray(value)) {
        throw new RangeError("Invalid input for Fragment.fromJSON");
      }
      return new Fragment(value.map(schema.nodeFromJSON));
    }}, {key:"fromArray", value:function(array) {
      if (!array.length) {
        return Fragment.empty;
      }
      for (var joined, size = 0, i = 0; i < array.length; i++) {
        var node = array[i];
        size += node.nodeSize;
        i && node.isText && array[i - 1].sameMarkup(node) ? (joined || (joined = array.slice(0, i)), joined[joined.length - 1] = node.withText(joined[joined.length - 1].text + node.text)) : joined && joined.push(node);
      }
      return new Fragment(joined || array, size);
    }}, {key:"from", value:function(nodes) {
      if (!nodes) {
        return Fragment.empty;
      }
      if (nodes instanceof Fragment) {
        return nodes;
      }
      if (Array.isArray(nodes)) {
        return this.fromArray(nodes);
      }
      if (nodes.attrs) {
        return new Fragment([nodes], nodes.nodeSize);
      }
      throw new RangeError("Can not convert " + nodes + " to a Fragment" + (nodes.nodesBetween ? " (looks like multiple versions of prosemirror-model were loaded)" : ""));
    }}]);
    return Fragment;
  }();
  Fragment.empty = new Fragment([], 0);
  var found = {index:0, offset:0}, Mark = function() {
    function Mark(type, attrs) {
      _classCallCheck(this, Mark);
      this.type = type;
      this.attrs = attrs;
    }
    _createClass(Mark, [{key:"addToSet", value:function(set) {
      for (var copy, placed = !1, i = 0; i < set.length; i++) {
        var other = set[i];
        if (this.eq(other)) {
          return set;
        }
        if (this.type.excludes(other.type)) {
          copy || (copy = set.slice(0, i));
        } else {
          if (other.type.excludes(this.type)) {
            return set;
          }
          !placed && other.type.rank > this.type.rank && (copy || (copy = set.slice(0, i)), copy.push(this), placed = !0);
          copy && copy.push(other);
        }
      }
      copy || (copy = set.slice());
      placed || copy.push(this);
      return copy;
    }}, {key:"removeFromSet", value:function(set) {
      for (var i = 0; i < set.length; i++) {
        if (this.eq(set[i])) {
          return set.slice(0, i).concat(set.slice(i + 1));
        }
      }
      return set;
    }}, {key:"isInSet", value:function(set) {
      for (var i = 0; i < set.length; i++) {
        if (this.eq(set[i])) {
          return !0;
        }
      }
      return !1;
    }}, {key:"eq", value:function(other) {
      return this == other || this.type == other.type && compareDeep(this.attrs, other.attrs);
    }}, {key:"toJSON", value:function() {
      var obj = {type:this.type.name}, _;
      for (_ in this.attrs) {
        obj.attrs = this.attrs;
        break;
      }
      return obj;
    }}], [{key:"fromJSON", value:function(schema, json) {
      if (!json) {
        throw new RangeError("Invalid input for Mark.fromJSON");
      }
      schema = schema.marks[json.type];
      if (!schema) {
        throw new RangeError("There is no mark type ".concat(json.type, " in this schema"));
      }
      json = schema.create(json.attrs);
      schema.checkAttrs(json.attrs);
      return json;
    }}, {key:"sameSet", value:function(a, b) {
      if (a == b) {
        return !0;
      }
      if (a.length != b.length) {
        return !1;
      }
      for (var i = 0; i < a.length; i++) {
        if (!a[i].eq(b[i])) {
          return !1;
        }
      }
      return !0;
    }}, {key:"setFrom", value:function(marks) {
      if (!marks || Array.isArray(marks) && 0 == marks.length) {
        return Mark.none;
      }
      if (marks instanceof Mark) {
        return [marks];
      }
      marks = marks.slice();
      marks.sort(function(a, b) {
        return a.type.rank - b.type.rank;
      });
      return marks;
    }}]);
    return Mark;
  }();
  Mark.none = [];
  var ReplaceError = function(_Error) {
    function ReplaceError() {
      _classCallCheck(this, ReplaceError);
      return _super.apply(this, arguments);
    }
    _inherits(ReplaceError, _Error);
    var _super = _createSuper(ReplaceError);
    return _createClass(ReplaceError);
  }(_wrapNativeSuper(Error)), Slice = function() {
    function Slice(content, openStart, openEnd) {
      _classCallCheck(this, Slice);
      this.content = content;
      this.openStart = openStart;
      this.openEnd = openEnd;
    }
    _createClass(Slice, [{key:"size", get:function() {
      return this.content.size - this.openStart - this.openEnd;
    }}, {key:"insertAt", value:function(pos, fragment) {
      return (pos = insertInto(this.content, pos + this.openStart, fragment)) && new Slice(pos, this.openStart, this.openEnd);
    }}, {key:"removeBetween", value:function(from, to) {
      return new Slice(removeRange(this.content, from + this.openStart, to + this.openStart), this.openStart, this.openEnd);
    }}, {key:"eq", value:function(other) {
      return this.content.eq(other.content) && this.openStart == other.openStart && this.openEnd == other.openEnd;
    }}, {key:"toString", value:function() {
      return this.content + "(" + this.openStart + "," + this.openEnd + ")";
    }}, {key:"toJSON", value:function() {
      if (!this.content.size) {
        return null;
      }
      var json = {content:this.content.toJSON()};
      0 < this.openStart && (json.openStart = this.openStart);
      0 < this.openEnd && (json.openEnd = this.openEnd);
      return json;
    }}], [{key:"fromJSON", value:function(schema, json) {
      if (!json) {
        return Slice.empty;
      }
      var openStart = json.openStart || 0, openEnd = json.openEnd || 0;
      if ("number" != typeof openStart || "number" != typeof openEnd) {
        throw new RangeError("Invalid input for Slice.fromJSON");
      }
      return new Slice(Fragment.fromJSON(schema, json.content), openStart, openEnd);
    }}, {key:"maxOpen", value:function(fragment) {
      for (var openIsolating = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : !0, openStart = 0, openEnd = 0, n = fragment.firstChild; n && !n.isLeaf && (openIsolating || !n.type.spec.isolating); n = n.firstChild) {
        openStart++;
      }
      for (n = fragment.lastChild; n && !n.isLeaf && (openIsolating || !n.type.spec.isolating); n = n.lastChild) {
        openEnd++;
      }
      return new Slice(fragment, openStart, openEnd);
    }}]);
    return Slice;
  }();
  Slice.empty = new Slice(Fragment.empty, 0, 0);
  var ResolvedPos = function() {
    function ResolvedPos(pos, path, parentOffset) {
      _classCallCheck(this, ResolvedPos);
      this.pos = pos;
      this.path = path;
      this.parentOffset = parentOffset;
      this.depth = path.length / 3 - 1;
    }
    _createClass(ResolvedPos, [{key:"resolveDepth", value:function(val) {
      return null == val ? this.depth : 0 > val ? this.depth + val : val;
    }}, {key:"parent", get:function() {
      return this.node(this.depth);
    }}, {key:"doc", get:function() {
      return this.node(0);
    }}, {key:"node", value:function(depth) {
      return this.path[3 * this.resolveDepth(depth)];
    }}, {key:"index", value:function(depth) {
      return this.path[3 * this.resolveDepth(depth) + 1];
    }}, {key:"indexAfter", value:function(depth) {
      depth = this.resolveDepth(depth);
      return this.index(depth) + (depth != this.depth || this.textOffset ? 1 : 0);
    }}, {key:"start", value:function(depth) {
      depth = this.resolveDepth(depth);
      return 0 == depth ? 0 : this.path[3 * depth - 1] + 1;
    }}, {key:"end", value:function(depth) {
      depth = this.resolveDepth(depth);
      return this.start(depth) + this.node(depth).content.size;
    }}, {key:"before", value:function(depth) {
      depth = this.resolveDepth(depth);
      if (!depth) {
        throw new RangeError("There is no position before the top-level node");
      }
      return depth == this.depth + 1 ? this.pos : this.path[3 * depth - 1];
    }}, {key:"after", value:function(depth) {
      depth = this.resolveDepth(depth);
      if (!depth) {
        throw new RangeError("There is no position after the top-level node");
      }
      return depth == this.depth + 1 ? this.pos : this.path[3 * depth - 1] + this.path[3 * depth].nodeSize;
    }}, {key:"textOffset", get:function() {
      return this.pos - this.path[this.path.length - 1];
    }}, {key:"nodeAfter", get:function() {
      var parent = this.parent, index = this.index(this.depth);
      if (index == parent.childCount) {
        return null;
      }
      var dOff = this.pos - this.path[this.path.length - 1], child = parent.child(index);
      return dOff ? parent.child(index).cut(dOff) : child;
    }}, {key:"nodeBefore", get:function() {
      var index = this.index(this.depth), dOff = this.pos - this.path[this.path.length - 1];
      return dOff ? this.parent.child(index).cut(0, dOff) : 0 == index ? null : this.parent.child(index - 1);
    }}, {key:"posAtIndex", value:function(index, depth) {
      depth = this.resolveDepth(depth);
      var node = this.path[3 * depth];
      depth = 0 == depth ? 0 : this.path[3 * depth - 1] + 1;
      for (var i = 0; i < index; i++) {
        depth += node.child(i).nodeSize;
      }
      return depth;
    }}, {key:"marks", value:function() {
      var parent = this.parent, index = this.index();
      if (0 == parent.content.size) {
        return Mark.none;
      }
      if (this.textOffset) {
        return parent.child(index).marks;
      }
      var main = parent.maybeChild(index - 1);
      parent = parent.maybeChild(index);
      main || (index = main, main = parent, parent = index);
      main = main.marks;
      for (index = 0; index < main.length; index++) {
        !1 !== main[index].type.spec.inclusive || parent && main[index].isInSet(parent.marks) || (main = main[index--].removeFromSet(main));
      }
      return main;
    }}, {key:"marksAcross", value:function($end) {
      var after = this.parent.maybeChild(this.index());
      if (!after || !after.isInline) {
        return null;
      }
      after = after.marks;
      $end = $end.parent.maybeChild($end.index());
      for (var i = 0; i < after.length; i++) {
        !1 !== after[i].type.spec.inclusive || $end && after[i].isInSet($end.marks) || (after = after[i--].removeFromSet(after));
      }
      return after;
    }}, {key:"sharedDepth", value:function(pos) {
      for (var depth = this.depth; 0 < depth; depth--) {
        if (this.start(depth) <= pos && this.end(depth) >= pos) {
          return depth;
        }
      }
      return 0;
    }}, {key:"blockRange", value:function() {
      var other = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : this, pred = 1 < arguments.length ? arguments[1] : void 0;
      if (other.pos < this.pos) {
        return other.blockRange(this);
      }
      for (var d = this.depth - (this.parent.inlineContent || this.pos == other.pos ? 1 : 0); 0 <= d; d--) {
        if (other.pos <= this.end(d) && (!pred || pred(this.node(d)))) {
          return new NodeRange(this, other, d);
        }
      }
      return null;
    }}, {key:"sameParent", value:function(other) {
      return this.pos - this.parentOffset == other.pos - other.parentOffset;
    }}, {key:"max", value:function(other) {
      return other.pos > this.pos ? other : this;
    }}, {key:"min", value:function(other) {
      return other.pos < this.pos ? other : this;
    }}, {key:"toString", value:function() {
      for (var str = "", i = 1; i <= this.depth; i++) {
        str += (str ? "/" : "") + this.node(i).type.name + "_" + this.index(i - 1);
      }
      return str + ":" + this.parentOffset;
    }}], [{key:"resolve", value:function(doc, pos) {
      if (!(0 <= pos && pos <= doc.content.size)) {
        throw new RangeError("Position " + pos + " out of range");
      }
      for (var path = [], start = 0, parentOffset = pos;;) {
        var _node$content$findInd = doc.content.findIndex(parentOffset), index = _node$content$findInd.index;
        _node$content$findInd = _node$content$findInd.offset;
        var rem = parentOffset - _node$content$findInd;
        path.push(doc, index, start + _node$content$findInd);
        if (!rem) {
          break;
        }
        doc = doc.child(index);
        if (doc.isText) {
          break;
        }
        parentOffset = rem - 1;
        start += _node$content$findInd + 1;
      }
      return new ResolvedPos(pos, path, parentOffset);
    }}, {key:"resolveCached", value:function(doc, pos) {
      var cache = resolveCache.get(doc);
      if (cache) {
        for (var i = 0; i < cache.elts.length; i++) {
          var elt = cache.elts[i];
          if (elt.pos == pos) {
            return elt;
          }
        }
      } else {
        resolveCache.set(doc, cache = new ResolveCache());
      }
      doc = cache.elts[cache.i] = ResolvedPos.resolve(doc, pos);
      cache.i = (cache.i + 1) % resolveCacheSize;
      return doc;
    }}]);
    return ResolvedPos;
  }(), ResolveCache = _createClass(function ResolveCache() {
    _classCallCheck(this, ResolveCache);
    this.elts = [];
    this.i = 0;
  }), resolveCacheSize = 12, resolveCache = new WeakMap(), NodeRange = function() {
    function NodeRange($from, $to, depth) {
      _classCallCheck(this, NodeRange);
      this.$from = $from;
      this.$to = $to;
      this.depth = depth;
    }
    _createClass(NodeRange, [{key:"start", get:function() {
      return this.$from.before(this.depth + 1);
    }}, {key:"end", get:function() {
      return this.$to.after(this.depth + 1);
    }}, {key:"parent", get:function() {
      return this.$from.node(this.depth);
    }}, {key:"startIndex", get:function() {
      return this.$from.index(this.depth);
    }}, {key:"endIndex", get:function() {
      return this.$to.indexAfter(this.depth);
    }}]);
    return NodeRange;
  }(), emptyAttrs = Object.create(null), Node = function() {
    function Node(type, attrs, content) {
      var marks = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : Mark.none;
      _classCallCheck(this, Node);
      this.type = type;
      this.attrs = attrs;
      this.marks = marks;
      this.content = content || Fragment.empty;
    }
    _createClass(Node, [{key:"nodeSize", get:function() {
      return this.isLeaf ? 1 : 2 + this.content.size;
    }}, {key:"childCount", get:function() {
      return this.content.childCount;
    }}, {key:"child", value:function(index) {
      return this.content.child(index);
    }}, {key:"maybeChild", value:function(index) {
      return this.content.maybeChild(index);
    }}, {key:"forEach", value:function(f) {
      this.content.forEach(f);
    }}, {key:"nodesBetween", value:function(from, to, f) {
      this.content.nodesBetween(from, to, f, 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : 0, this);
    }}, {key:"descendants", value:function(f) {
      this.nodesBetween(0, this.content.size, f);
    }}, {key:"textContent", get:function() {
      return this.isLeaf && this.type.spec.leafText ? this.type.spec.leafText(this) : this.textBetween(0, this.content.size, "");
    }}, {key:"textBetween", value:function(from, to, blockSeparator, leafText) {
      return this.content.textBetween(from, to, blockSeparator, leafText);
    }}, {key:"firstChild", get:function() {
      return this.content.firstChild;
    }}, {key:"lastChild", get:function() {
      return this.content.lastChild;
    }}, {key:"eq", value:function(other) {
      return this == other || this.sameMarkup(other) && this.content.eq(other.content);
    }}, {key:"sameMarkup", value:function(other) {
      return this.hasMarkup(other.type, other.attrs, other.marks);
    }}, {key:"hasMarkup", value:function(type, attrs, marks) {
      return this.type == type && compareDeep(this.attrs, attrs || type.defaultAttrs || emptyAttrs) && Mark.sameSet(this.marks, marks || Mark.none);
    }}, {key:"copy", value:function() {
      var content = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : null;
      return content == this.content ? this : new Node(this.type, this.attrs, content, this.marks);
    }}, {key:"mark", value:function(marks) {
      return marks == this.marks ? this : new Node(this.type, this.attrs, this.content, marks);
    }}, {key:"cut", value:function(from) {
      var to = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : this.content.size;
      return 0 == from && to == this.content.size ? this : this.copy(this.content.cut(from, to));
    }}, {key:"slice", value:function(from) {
      var to = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : this.content.size, includeParents = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : !1;
      if (from == to) {
        return Slice.empty;
      }
      var $from = this.resolve(from), $to = this.resolve(to);
      to = includeParents ? 0 : $from.sharedDepth(to);
      includeParents = $from.start(to);
      includeParents = $from.node(to).content.cut($from.pos - includeParents, $to.pos - includeParents);
      return new Slice(includeParents, $from.depth - to, $to.depth - to);
    }}, {key:"replace", value:function(from, to, slice) {
      from = this.resolve(from);
      to = this.resolve(to);
      if (slice.openStart > from.depth) {
        throw new ReplaceError("Inserted content deeper than insertion position");
      }
      if (from.depth - slice.openStart != to.depth - slice.openEnd) {
        throw new ReplaceError("Inconsistent open depths");
      }
      return replaceOuter(from, to, slice, 0);
    }}, {key:"nodeAt", value:function(pos) {
      for (var node = this;;) {
        var _node$content$findInd2 = node.content.findIndex(pos), offset = _node$content$findInd2.offset;
        node = node.maybeChild(_node$content$findInd2.index);
        if (!node) {
          return null;
        }
        if (offset == pos || node.isText) {
          return node;
        }
        pos -= offset + 1;
      }
    }}, {key:"childAfter", value:function(pos) {
      var _this$content$findInd = this.content.findIndex(pos);
      pos = _this$content$findInd.index;
      _this$content$findInd = _this$content$findInd.offset;
      return {node:this.content.maybeChild(pos), index:pos, offset:_this$content$findInd};
    }}, {key:"childBefore", value:function(pos) {
      if (0 == pos) {
        return {node:null, index:0, offset:0};
      }
      var _this$content$findInd2 = this.content.findIndex(pos), index = _this$content$findInd2.index;
      _this$content$findInd2 = _this$content$findInd2.offset;
      if (_this$content$findInd2 < pos) {
        return {node:this.content.child(index), index, offset:_this$content$findInd2};
      }
      pos = this.content.child(index - 1);
      return {node:pos, index:index - 1, offset:_this$content$findInd2 - pos.nodeSize};
    }}, {key:"resolve", value:function(pos) {
      return ResolvedPos.resolveCached(this, pos);
    }}, {key:"resolveNoCache", value:function(pos) {
      return ResolvedPos.resolve(this, pos);
    }}, {key:"rangeHasMark", value:function(from, to, type) {
      var found = !1;
      to > from && this.nodesBetween(from, to, function(node) {
        type.isInSet(node.marks) && (found = !0);
        return !found;
      });
      return found;
    }}, {key:"isBlock", get:function() {
      return this.type.isBlock;
    }}, {key:"isTextblock", get:function() {
      return this.type.isTextblock;
    }}, {key:"inlineContent", get:function() {
      return this.type.inlineContent;
    }}, {key:"isInline", get:function() {
      return this.type.isInline;
    }}, {key:"isText", get:function() {
      return this.type.isText;
    }}, {key:"isLeaf", get:function() {
      return this.type.isLeaf;
    }}, {key:"isAtom", get:function() {
      return this.type.isAtom;
    }}, {key:"toString", value:function() {
      if (this.type.spec.toDebugString) {
        return this.type.spec.toDebugString(this);
      }
      var name = this.type.name;
      this.content.size && (name += "(" + this.content.toStringInner() + ")");
      return wrapMarks(this.marks, name);
    }}, {key:"contentMatchAt", value:function(index) {
      index = this.type.contentMatch.matchFragment(this.content, 0, index);
      if (!index) {
        throw Error("Called contentMatchAt on a node with invalid content");
      }
      return index;
    }}, {key:"canReplace", value:function(from, to) {
      var replacement = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : Fragment.empty, start = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : 0, end = 4 < arguments.length && void 0 !== arguments[4] ? arguments[4] : replacement.childCount, one = this.contentMatchAt(from).matchFragment(replacement, start, end);
      one = one && one.matchFragment(this.content, to);
      if (!one || !one.validEnd) {
        return !1;
      }
      for (; start < end; start++) {
        if (!this.type.allowsMarks(replacement.child(start).marks)) {
          return !1;
        }
      }
      return !0;
    }}, {key:"canReplaceWith", value:function(from, to, type, marks) {
      return marks && !this.type.allowsMarks(marks) ? !1 : (to = (from = this.contentMatchAt(from).matchType(type)) && from.matchFragment(this.content, to)) ? to.validEnd : !1;
    }}, {key:"canAppend", value:function(other) {
      return other.content.size ? this.canReplace(this.childCount, this.childCount, other.content) : this.type.compatibleContent(other.type);
    }}, {key:"check", value:function() {
      this.type.checkContent(this.content);
      this.type.checkAttrs(this.attrs);
      for (var copy = Mark.none, i = 0; i < this.marks.length; i++) {
        var mark = this.marks[i];
        mark.type.checkAttrs(mark.attrs);
        copy = mark.addToSet(copy);
      }
      if (!Mark.sameSet(copy, this.marks)) {
        throw new RangeError("Invalid collection of marks for node ".concat(this.type.name, ": ").concat(this.marks.map(function(m) {
          return m.type.name;
        })));
      }
      this.content.forEach(function(node) {
        return node.check();
      });
    }}, {key:"toJSON", value:function() {
      var obj = {type:this.type.name}, _;
      for (_ in this.attrs) {
        obj.attrs = this.attrs;
        break;
      }
      this.content.size && (obj.content = this.content.toJSON());
      this.marks.length && (obj.marks = this.marks.map(function(n) {
        return n.toJSON();
      }));
      return obj;
    }}], [{key:"fromJSON", value:function(schema, json) {
      if (!json) {
        throw new RangeError("Invalid input for Node.fromJSON");
      }
      var marks = void 0;
      if (json.marks) {
        if (!Array.isArray(json.marks)) {
          throw new RangeError("Invalid mark data for Node.fromJSON");
        }
        marks = json.marks.map(schema.markFromJSON);
      }
      if ("text" == json.type) {
        if ("string" != typeof json.text) {
          throw new RangeError("Invalid text node in JSON");
        }
        return schema.text(json.text, marks);
      }
      var content = Fragment.fromJSON(schema, json.content);
      schema = schema.nodeType(json.type).create(json.attrs, content, marks);
      schema.type.checkAttrs(schema.attrs);
      return schema;
    }}]);
    return Node;
  }();
  Node.prototype.text = void 0;
  var TextNode = function(_Node) {
    function TextNode(type, attrs, content, marks) {
      _classCallCheck(this, TextNode);
      type = _super2.call(this, type, attrs, null, marks);
      if (!content) {
        throw new RangeError("Empty text nodes are not allowed");
      }
      type.text = content;
      return type;
    }
    _inherits(TextNode, _Node);
    var _super2 = _createSuper(TextNode);
    _createClass(TextNode, [{key:"toString", value:function() {
      return this.type.spec.toDebugString ? this.type.spec.toDebugString(this) : wrapMarks(this.marks, JSON.stringify(this.text));
    }}, {key:"textContent", get:function() {
      return this.text;
    }}, {key:"textBetween", value:function(from, to) {
      return this.text.slice(from, to);
    }}, {key:"nodeSize", get:function() {
      return this.text.length;
    }}, {key:"mark", value:function(marks) {
      return marks == this.marks ? this : new TextNode(this.type, this.attrs, this.text, marks);
    }}, {key:"withText", value:function(text) {
      return text == this.text ? this : new TextNode(this.type, this.attrs, text, this.marks);
    }}, {key:"cut", value:function() {
      var from = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : 0, to = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : this.text.length;
      return 0 == from && to == this.text.length ? this : this.withText(this.text.slice(from, to));
    }}, {key:"eq", value:function(other) {
      return this.sameMarkup(other) && this.text == other.text;
    }}, {key:"toJSON", value:function() {
      var base = _get(_getPrototypeOf(TextNode.prototype), "toJSON", this).call(this);
      base.text = this.text;
      return base;
    }}]);
    return TextNode;
  }(Node), ContentMatch = function() {
    function ContentMatch(validEnd) {
      _classCallCheck(this, ContentMatch);
      this.validEnd = validEnd;
      this.next = [];
      this.wrapCache = [];
    }
    _createClass(ContentMatch, [{key:"matchType", value:function(type) {
      for (var i = 0; i < this.next.length; i++) {
        if (this.next[i].type == type) {
          return this.next[i].next;
        }
      }
      return null;
    }}, {key:"matchFragment", value:function(frag) {
      for (var end = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : frag.childCount, cur = this, i = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : 0; cur && i < end; i++) {
        cur = cur.matchType(frag.child(i).type);
      }
      return cur;
    }}, {key:"inlineContent", get:function() {
      return 0 != this.next.length && this.next[0].type.isInline;
    }}, {key:"defaultType", get:function() {
      for (var i = 0; i < this.next.length; i++) {
        var type = this.next[i].type;
        if (!type.isText && !type.hasRequiredAttrs()) {
          return type;
        }
      }
      return null;
    }}, {key:"compatible", value:function(other) {
      for (var i = 0; i < this.next.length; i++) {
        for (var j = 0; j < other.next.length; j++) {
          if (this.next[i].type == other.next[j].type) {
            return !0;
          }
        }
      }
      return !1;
    }}, {key:"fillBefore", value:function(after) {
      function search(match, types) {
        var finished = match.matchFragment(after, startIndex);
        if (finished && (!toEnd || finished.validEnd)) {
          return Fragment.from(types.map(function(tp) {
            return tp.createAndFill();
          }));
        }
        for (finished = 0; finished < match.next.length; finished++) {
          var _match$next$i = match.next[finished], type = _match$next$i.type;
          _match$next$i = _match$next$i.next;
          if (!type.isText && !type.hasRequiredAttrs() && -1 == seen.indexOf(_match$next$i) && (seen.push(_match$next$i), type = search(_match$next$i, types.concat(type)))) {
            return type;
          }
        }
        return null;
      }
      var toEnd = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : !1, startIndex = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : 0, seen = [this];
      return search(this, []);
    }}, {key:"findWrapping", value:function(target) {
      for (var i = 0; i < this.wrapCache.length; i += 2) {
        if (this.wrapCache[i] == target) {
          return this.wrapCache[i + 1];
        }
      }
      i = this.computeWrapping(target);
      this.wrapCache.push(target, i);
      return i;
    }}, {key:"computeWrapping", value:function(target) {
      for (var seen = Object.create(null), active = [{match:this, type:null, via:null}]; active.length;) {
        var current = active.shift(), match = current.match;
        if (match.matchType(target)) {
          for (target = []; current.type; current = current.via) {
            target.push(current.type);
          }
          return target.reverse();
        }
        for (var i = 0; i < match.next.length; i++) {
          var _match$next$i2 = match.next[i], type = _match$next$i2.type;
          _match$next$i2 = _match$next$i2.next;
          type.isLeaf || type.hasRequiredAttrs() || type.name in seen || current.type && !_match$next$i2.validEnd || (active.push({match:type.contentMatch, type, via:current}), seen[type.name] = !0);
        }
      }
      return null;
    }}, {key:"edgeCount", get:function() {
      return this.next.length;
    }}, {key:"edge", value:function(n) {
      if (n >= this.next.length) {
        throw new RangeError("There's no ".concat(n, "th edge in this content match"));
      }
      return this.next[n];
    }}, {key:"toString", value:function() {
      function scan(m) {
        seen.push(m);
        for (var i = 0; i < m.next.length; i++) {
          -1 == seen.indexOf(m.next[i].next) && scan(m.next[i].next);
        }
      }
      var seen = [];
      scan(this);
      return seen.map(function(m, i) {
        i = i + (m.validEnd ? "*" : " ") + " ";
        for (var _i = 0; _i < m.next.length; _i++) {
          i += (_i ? ", " : "") + m.next[_i].type.name + "-\x3e" + seen.indexOf(m.next[_i].next);
        }
        return i;
      }).join("\n");
    }}], [{key:"parse", value:function(string, nodeTypes) {
      string = new TokenStream(string, nodeTypes);
      if (null == string.next) {
        return ContentMatch.empty;
      }
      nodeTypes = parseExpr(string);
      string.next && string.err("Unexpected trailing text");
      nodeTypes = dfa(nfa(nodeTypes));
      for (var i = 0, work = [nodeTypes]; i < work.length; i++) {
        for (var state = work[i], dead = !state.validEnd, nodes = [], j = 0; j < state.next.length; j++) {
          var _state$next$j = state.next[j], type = _state$next$j.type;
          _state$next$j = _state$next$j.next;
          nodes.push(type.name);
          !dead || type.isText || type.hasRequiredAttrs() || (dead = !1);
          -1 == work.indexOf(_state$next$j) && work.push(_state$next$j);
        }
        dead && string.err("Only non-generatable nodes (" + nodes.join(", ") + ") in a required position (see https://prosemirror.net/docs/guide/#generatable)");
      }
      return nodeTypes;
    }}]);
    return ContentMatch;
  }();
  ContentMatch.empty = new ContentMatch(!0);
  var TokenStream = function() {
    function TokenStream(string, nodeTypes) {
      _classCallCheck(this, TokenStream);
      this.string = string;
      this.nodeTypes = nodeTypes;
      this.inline = null;
      this.pos = 0;
      this.tokens = string.split(/\s*(?=\b|\W|$)/);
      "" == this.tokens[this.tokens.length - 1] && this.tokens.pop();
      "" == this.tokens[0] && this.tokens.shift();
    }
    _createClass(TokenStream, [{key:"next", get:function() {
      return this.tokens[this.pos];
    }}, {key:"eat", value:function(tok) {
      return this.next == tok && (this.pos++ || !0);
    }}, {key:"err", value:function(str) {
      throw new SyntaxError(str + " (in content expression '" + this.string + "')");
    }}]);
    return TokenStream;
  }(), NodeType = function() {
    function NodeType(name, schema, spec) {
      _classCallCheck(this, NodeType);
      this.name = name;
      this.schema = schema;
      this.spec = spec;
      this.markSet = null;
      this.groups = spec.group ? spec.group.split(" ") : [];
      this.attrs = initAttrs(name, spec.attrs);
      this.defaultAttrs = defaultAttrs(this.attrs);
      this.inlineContent = this.contentMatch = null;
      this.isBlock = !(spec.inline || "text" == name);
      this.isText = "text" == name;
    }
    _createClass(NodeType, [{key:"isInline", get:function() {
      return !this.isBlock;
    }}, {key:"isTextblock", get:function() {
      return this.isBlock && this.inlineContent;
    }}, {key:"isLeaf", get:function() {
      return this.contentMatch == ContentMatch.empty;
    }}, {key:"isAtom", get:function() {
      return this.isLeaf || !!this.spec.atom;
    }}, {key:"isInGroup", value:function(group) {
      return -1 < this.groups.indexOf(group);
    }}, {key:"whitespace", get:function() {
      return this.spec.whitespace || (this.spec.code ? "pre" : "normal");
    }}, {key:"hasRequiredAttrs", value:function() {
      for (var n in this.attrs) {
        if (this.attrs[n].isRequired) {
          return !0;
        }
      }
      return !1;
    }}, {key:"compatibleContent", value:function(other) {
      return this == other || this.contentMatch.compatible(other.contentMatch);
    }}, {key:"computeAttrs", value:function(attrs) {
      return !attrs && this.defaultAttrs ? this.defaultAttrs : _computeAttrs(this.attrs, attrs);
    }}, {key:"create", value:function() {
      var attrs = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : null, content = 1 < arguments.length ? arguments[1] : void 0, marks = 2 < arguments.length ? arguments[2] : void 0;
      if (this.isText) {
        throw Error("NodeType.create can't construct text nodes");
      }
      return new Node(this, this.computeAttrs(attrs), Fragment.from(content), Mark.setFrom(marks));
    }}, {key:"createChecked", value:function() {
      var attrs = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : null, content = 1 < arguments.length ? arguments[1] : void 0, marks = 2 < arguments.length ? arguments[2] : void 0;
      content = Fragment.from(content);
      this.checkContent(content);
      return new Node(this, this.computeAttrs(attrs), content, Mark.setFrom(marks));
    }}, {key:"createAndFill", value:function() {
      var attrs = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : null, content = 1 < arguments.length ? arguments[1] : void 0, marks = 2 < arguments.length ? arguments[2] : void 0;
      attrs = this.computeAttrs(attrs);
      content = Fragment.from(content);
      if (content.size) {
        var before = this.contentMatch.fillBefore(content);
        if (!before) {
          return null;
        }
        content = before.append(content);
      }
      return (before = (before = this.contentMatch.matchFragment(content)) && before.fillBefore(Fragment.empty, !0)) ? new Node(this, attrs, content.append(before), Mark.setFrom(marks)) : null;
    }}, {key:"validContent", value:function(content) {
      var result = this.contentMatch.matchFragment(content);
      if (!result || !result.validEnd) {
        return !1;
      }
      for (result = 0; result < content.childCount; result++) {
        if (!this.allowsMarks(content.child(result).marks)) {
          return !1;
        }
      }
      return !0;
    }}, {key:"checkContent", value:function(content) {
      if (!this.validContent(content)) {
        throw new RangeError("Invalid content for node ".concat(this.name, ": ").concat(content.toString().slice(0, 50)));
      }
    }}, {key:"checkAttrs", value:function(attrs) {
      _checkAttrs(this.attrs, attrs, "node", this.name);
    }}, {key:"allowsMarkType", value:function(markType) {
      return null == this.markSet || -1 < this.markSet.indexOf(markType);
    }}, {key:"allowsMarks", value:function(marks) {
      if (null == this.markSet) {
        return !0;
      }
      for (var i = 0; i < marks.length; i++) {
        if (!this.allowsMarkType(marks[i].type)) {
          return !1;
        }
      }
      return !0;
    }}, {key:"allowedMarks", value:function(marks) {
      if (null == this.markSet) {
        return marks;
      }
      for (var copy, i = 0; i < marks.length; i++) {
        this.allowsMarkType(marks[i].type) ? copy && copy.push(marks[i]) : copy || (copy = marks.slice(0, i));
      }
      return copy ? copy.length ? copy : Mark.none : marks;
    }}], [{key:"compile", value:function(nodes, schema) {
      var result = Object.create(null);
      nodes.forEach(function(name, spec) {
        return result[name] = new NodeType(name, schema, spec);
      });
      nodes = schema.spec.topNode || "doc";
      if (!result[nodes]) {
        throw new RangeError("Schema is missing its top node type ('" + nodes + "')");
      }
      if (!result.text) {
        throw new RangeError("Every schema needs a 'text' type");
      }
      for (var _ in result.text.attrs) {
        throw new RangeError("The text node type should not have attributes");
      }
      return result;
    }}]);
    return NodeType;
  }(), Attribute = function() {
    function Attribute(typeName, attrName, options) {
      _classCallCheck(this, Attribute);
      this.hasDefault = Object.prototype.hasOwnProperty.call(options, "default");
      this["default"] = options["default"];
      this.validate = "string" == typeof options.validate ? validateType(typeName, attrName, options.validate) : options.validate;
    }
    _createClass(Attribute, [{key:"isRequired", get:function() {
      return !this.hasDefault;
    }}]);
    return Attribute;
  }(), MarkType = function() {
    function MarkType(name, rank, schema, spec) {
      _classCallCheck(this, MarkType);
      this.name = name;
      this.rank = rank;
      this.schema = schema;
      this.spec = spec;
      this.attrs = initAttrs(name, spec.attrs);
      this.excluded = null;
      this.instance = (name = defaultAttrs(this.attrs)) ? new Mark(this, name) : null;
    }
    _createClass(MarkType, [{key:"create", value:function() {
      var attrs = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : null;
      return !attrs && this.instance ? this.instance : new Mark(this, _computeAttrs(this.attrs, attrs));
    }}, {key:"removeFromSet", value:function(set) {
      for (var i = 0; i < set.length; i++) {
        set[i].type == this && (set = set.slice(0, i).concat(set.slice(i + 1)), i--);
      }
      return set;
    }}, {key:"isInSet", value:function(set) {
      for (var i = 0; i < set.length; i++) {
        if (set[i].type == this) {
          return set[i];
        }
      }
    }}, {key:"checkAttrs", value:function(attrs) {
      _checkAttrs(this.attrs, attrs, "mark", this.name);
    }}, {key:"excludes", value:function(other) {
      return -1 < this.excluded.indexOf(other);
    }}], [{key:"compile", value:function(marks, schema) {
      var result = Object.create(null), rank = 0;
      marks.forEach(function(name, spec) {
        return result[name] = new MarkType(name, rank++, schema, spec);
      });
      return result;
    }}]);
    return MarkType;
  }();
  global = function() {
    function Schema(spec) {
      _classCallCheck(this, Schema);
      this.linebreakReplacement = null;
      this.cached = Object.create(null);
      var instanceSpec = this.spec = {};
      for (prop in spec) {
        instanceSpec[prop] = spec[prop];
      }
      instanceSpec.nodes = OrderedMap.from(spec.nodes);
      instanceSpec.marks = OrderedMap.from(spec.marks || {});
      this.nodes = NodeType.compile(this.spec.nodes, this);
      this.marks = MarkType.compile(this.spec.marks, this);
      spec = Object.create(null);
      for (var _prop in this.nodes) {
        if (_prop in this.marks) {
          throw new RangeError(_prop + " can not be both a node and a mark");
        }
        instanceSpec = this.nodes[_prop];
        var prop = instanceSpec.spec.content || "";
        var markExpr = instanceSpec.spec.marks;
        instanceSpec.contentMatch = spec[prop] || (spec[prop] = ContentMatch.parse(prop, this.nodes));
        instanceSpec.inlineContent = instanceSpec.contentMatch.inlineContent;
        if (instanceSpec.spec.linebreakReplacement) {
          if (this.linebreakReplacement) {
            throw new RangeError("Multiple linebreak nodes defined");
          }
          if (!instanceSpec.isInline || !instanceSpec.isLeaf) {
            throw new RangeError("Linebreak replacement nodes must be inline leaf nodes");
          }
          this.linebreakReplacement = instanceSpec;
        }
        instanceSpec.markSet = "_" == markExpr ? null : markExpr ? gatherMarks(this, markExpr.split(" ")) : "" != markExpr && instanceSpec.inlineContent ? null : [];
      }
      for (var _prop2 in this.marks) {
        _prop = this.marks[_prop2], spec = _prop.spec.excludes, _prop.excluded = null == spec ? [_prop] : "" == spec ? [] : gatherMarks(this, spec.split(" "));
      }
      this.nodeFromJSON = this.nodeFromJSON.bind(this);
      this.markFromJSON = this.markFromJSON.bind(this);
      this.topNodeType = this.nodes[this.spec.topNode || "doc"];
      this.cached.wrappings = Object.create(null);
    }
    _createClass(Schema, [{key:"node", value:function(type) {
      var attrs = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : null, content = 2 < arguments.length ? arguments[2] : void 0, marks = 3 < arguments.length ? arguments[3] : void 0;
      if ("string" == typeof type) {
        type = this.nodeType(type);
      } else {
        if (!(type instanceof NodeType)) {
          throw new RangeError("Invalid node type: " + type);
        }
        if (type.schema != this) {
          throw new RangeError("Node type from different schema used (" + type.name + ")");
        }
      }
      return type.createChecked(attrs, content, marks);
    }}, {key:"text", value:function(_text, marks) {
      var type = this.nodes.text;
      return new TextNode(type, type.defaultAttrs, _text, Mark.setFrom(marks));
    }}, {key:"mark", value:function(type, attrs) {
      "string" == typeof type && (type = this.marks[type]);
      return type.create(attrs);
    }}, {key:"nodeFromJSON", value:function(json) {
      return Node.fromJSON(this, json);
    }}, {key:"markFromJSON", value:function(json) {
      return Mark.fromJSON(this, json);
    }}, {key:"nodeType", value:function(name) {
      var found = this.nodes[name];
      if (!found) {
        throw new RangeError("Unknown node type: " + name);
      }
      return found;
    }}]);
    return Schema;
  }();
  require = function() {
    function DOMParser(schema, rules) {
      var _this2 = this;
      _classCallCheck(this, DOMParser);
      this.schema = schema;
      this.rules = rules;
      this.tags = [];
      this.styles = [];
      var matchedStyles = this.matchedStyles = [];
      rules.forEach(function(rule) {
        if (null != rule.tag) {
          _this2.tags.push(rule);
        } else if (null != rule.style) {
          var prop = /[^=]*/.exec(rule.style)[0];
          0 > matchedStyles.indexOf(prop) && matchedStyles.push(prop);
          _this2.styles.push(rule);
        }
      });
      this.normalizeLists = !this.tags.some(function(r) {
        if (!/^(ul|ol)\b/.test(r.tag) || !r.node) {
          return !1;
        }
        r = schema.nodes[r.node];
        return r.contentMatch.matchType(r);
      });
    }
    _createClass(DOMParser, [{key:"parse", value:function(dom) {
      var options = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}, context = new ParseContext(this, options, !1);
      context.addAll(dom, Mark.none, options.from, options.to);
      return context.finish();
    }}, {key:"parseSlice", value:function(dom) {
      var options = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}, context = new ParseContext(this, options, !0);
      context.addAll(dom, Mark.none, options.from, options.to);
      return Slice.maxOpen(context.finish());
    }}, {key:"matchTag", value:function(dom, context, after) {
      for (after = after ? this.tags.indexOf(after) + 1 : 0; after < this.tags.length; after++) {
        var rule = this.tags[after];
        if ((dom.matches || dom.msMatchesSelector || dom.webkitMatchesSelector || dom.mozMatchesSelector).call(dom, rule.tag) && (void 0 === rule.namespace || dom.namespaceURI == rule.namespace) && (!rule.context || context.matchesContext(rule.context))) {
          if (rule.getAttrs) {
            var result = rule.getAttrs(dom);
            if (!1 === result) {
              continue;
            }
            rule.attrs = result || void 0;
          }
          return rule;
        }
      }
    }}, {key:"matchStyle", value:function(prop, value, context, after) {
      for (after = after ? this.styles.indexOf(after) + 1 : 0; after < this.styles.length; after++) {
        var rule = this.styles[after], style = rule.style;
        if (!(0 != style.indexOf(prop) || rule.context && !context.matchesContext(rule.context) || style.length > prop.length && (61 != style.charCodeAt(prop.length) || style.slice(prop.length + 1) != value))) {
          if (rule.getAttrs) {
            style = rule.getAttrs(value);
            if (!1 === style) {
              continue;
            }
            rule.attrs = style || void 0;
          }
          return rule;
        }
      }
    }}], [{key:"schemaRules", value:function(schema) {
      function insert(rule) {
        for (var priority = null == rule.priority ? 50 : rule.priority, i = 0; i < result.length; i++) {
          var next = result[i];
          if ((null == next.priority ? 50 : next.priority) < priority) {
            break;
          }
        }
        result.splice(i, 0, rule);
      }
      var result = [], _loop2 = function(name) {
        var rules = schema.marks[name].spec.parseDOM;
        rules && rules.forEach(function(rule) {
          insert(rule = copy(rule));
          rule.mark || rule.ignore || rule.clearMark || (rule.mark = name);
        });
      }, name;
      for (name in schema.marks) {
        _loop2(name);
      }
      _loop2 = function(_name3) {
        var rules = schema.nodes[_name3].spec.parseDOM;
        rules && rules.forEach(function(rule) {
          insert(rule = copy(rule));
          rule.node || rule.ignore || rule.mark || (rule.node = _name3);
        });
      };
      for (var _name3 in schema.nodes) {
        _loop2(_name3);
      }
      return result;
    }}, {key:"fromSchema", value:function(schema) {
      return schema.cached.domParser || (schema.cached.domParser = new DOMParser(schema, DOMParser.schemaRules(schema)));
    }}]);
    return DOMParser;
  }();
  var blockTags = {address:!0, article:!0, aside:!0, blockquote:!0, canvas:!0, dd:!0, div:!0, dl:!0, fieldset:!0, figcaption:!0, figure:!0, footer:!0, form:!0, h1:!0, h2:!0, h3:!0, h4:!0, h5:!0, h6:!0, header:!0, hgroup:!0, hr:!0, li:!0, noscript:!0, ol:!0, output:!0, p:!0, pre:!0, section:!0, table:!0, tfoot:!0, ul:!0}, ignoreTags = {head:!0, noscript:!0, object:!0, script:!0, style:!0, title:!0}, listTags = {ol:!0, ul:!0}, NodeContext = function() {
    function NodeContext(type, attrs, marks, solid, match, options) {
      _classCallCheck(this, NodeContext);
      this.type = type;
      this.attrs = attrs;
      this.marks = marks;
      this.solid = solid;
      this.options = options;
      this.content = [];
      this.activeMarks = Mark.none;
      this.match = match || (options & 4 ? null : type.contentMatch);
    }
    _createClass(NodeContext, [{key:"findWrapping", value:function(node) {
      if (!this.match) {
        if (!this.type) {
          return [];
        }
        var fill = this.type.contentMatch.fillBefore(Fragment.from(node));
        if (fill) {
          this.match = this.type.contentMatch.matchFragment(fill);
        } else {
          return fill = this.type.contentMatch, (node = fill.findWrapping(node.type)) ? (this.match = fill, node) : null;
        }
      }
      return this.match.findWrapping(node.type);
    }}, {key:"finish", value:function(openEnd) {
      if (!(this.options & 1)) {
        var last = this.content[this.content.length - 1], m;
        last && last.isText && (m = /[ \t\r\n\u000c]+$/.exec(last.text)) && (last.text.length == m[0].length ? this.content.pop() : this.content[this.content.length - 1] = last.withText(last.text.slice(0, last.text.length - m[0].length)));
      }
      last = Fragment.from(this.content);
      !openEnd && this.match && (last = last.append(this.match.fillBefore(Fragment.empty, !0)));
      return this.type ? this.type.create(this.attrs, last, this.marks) : last;
    }}, {key:"inlineContext", value:function(node) {
      return this.type ? this.type.inlineContent : this.content.length ? this.content[0].isInline : node.parentNode && !blockTags.hasOwnProperty(node.parentNode.nodeName.toLowerCase());
    }}]);
    return NodeContext;
  }(), ParseContext = function() {
    function ParseContext(parser, options, isOpen) {
      _classCallCheck(this, ParseContext);
      this.parser = parser;
      this.options = options;
      this.isOpen = isOpen;
      this.open = 0;
      var topNode = options.topNode, topOptions = wsOptionsFor(null, options.preserveWhitespace, 0) | (isOpen ? 4 : 0);
      this.nodes = [topNode ? new NodeContext(topNode.type, topNode.attrs, Mark.none, !0, options.topMatch || topNode.type.contentMatch, topOptions) : isOpen ? new NodeContext(null, null, Mark.none, !0, null, topOptions) : new NodeContext(parser.schema.topNodeType, null, Mark.none, !0, null, topOptions)];
      this.find = options.findPositions;
      this.needsBlock = !1;
    }
    _createClass(ParseContext, [{key:"top", get:function() {
      return this.nodes[this.open];
    }}, {key:"addDOM", value:function(dom, marks) {
      3 == dom.nodeType ? this.addTextNode(dom, marks) : 1 == dom.nodeType && this.addElement(dom, marks);
    }}, {key:"addTextNode", value:function(dom, marks) {
      var value = dom.nodeValue, top = this.top;
      if (top.options & 2 || top.inlineContext(dom) || /[^ \t\r\n\u000c]/.test(value)) {
        if (top.options & 1) {
          value = top.options & 2 ? value.replace(/\r\n?/g, "\n") : value.replace(/\r?\n|\r/g, " ");
        } else {
          if (value = value.replace(/[ \t\r\n\u000c]+/g, " "), /^[ \t\r\n\u000c]/.test(value) && this.open == this.nodes.length - 1) {
            top = top.content[top.content.length - 1];
            var domNodeBefore = dom.previousSibling;
            if (!top || domNodeBefore && "BR" == domNodeBefore.nodeName || top.isText && /[ \t\r\n\u000c]$/.test(top.text)) {
              value = value.slice(1);
            }
          }
        }
        value && this.insertNode(this.parser.schema.text(value), marks);
        this.findInText(dom);
      } else {
        this.findInside(dom);
      }
    }}, {key:"addElement", value:function(dom, marks, matchAfter) {
      var name = dom.nodeName.toLowerCase(), ruleID;
      if (listTags.hasOwnProperty(name) && this.parser.normalizeLists) {
        for (var child = dom.firstChild, prevItem = null; child; child = child.nextSibling) {
          var name$jscomp$0 = 1 == child.nodeType ? child.nodeName.toLowerCase() : null;
          name$jscomp$0 && listTags.hasOwnProperty(name$jscomp$0) && prevItem ? (prevItem.appendChild(child), child = prevItem) : "li" == name$jscomp$0 ? prevItem = child : name$jscomp$0 && (prevItem = null);
        }
      }
      if ((matchAfter = this.options.ruleFromNode && this.options.ruleFromNode(dom) || (ruleID = this.parser.matchTag(dom, this, matchAfter))) ? matchAfter.ignore : ignoreTags.hasOwnProperty(name)) {
        this.findInside(dom), this.ignoreFallback(dom, marks);
      } else if (!matchAfter || matchAfter.skip || matchAfter.closeParent) {
        matchAfter && matchAfter.closeParent ? this.open = Math.max(0, this.open - 1) : matchAfter && matchAfter.skip.nodeType && (dom = matchAfter.skip);
        ruleID = this.top;
        child = this.needsBlock;
        if (blockTags.hasOwnProperty(name)) {
          ruleID.content.length && ruleID.content[0].isInline && this.open && (this.open--, ruleID = this.top);
          var sync = !0;
          ruleID.type || (this.needsBlock = !0);
        } else if (!dom.firstChild) {
          this.leafFallback(dom, marks);
          return;
        }
        (marks = matchAfter && matchAfter.skip ? marks : this.readStyles(dom, marks)) && this.addAll(dom, marks);
        sync && this.sync(ruleID);
        this.needsBlock = child;
      } else {
        (sync = this.readStyles(dom, marks)) && this.addElementByRule(dom, matchAfter, sync, !1 === matchAfter.consuming ? ruleID : void 0);
      }
    }}, {key:"leafFallback", value:function(dom, marks) {
      "BR" == dom.nodeName && this.top.type && this.top.type.inlineContent && this.addTextNode(dom.ownerDocument.createTextNode("\n"), marks);
    }}, {key:"ignoreFallback", value:function(dom, marks) {
      "BR" != dom.nodeName || this.top.type && this.top.type.inlineContent || this.findPlace(this.parser.schema.text("-"), marks);
    }}, {key:"readStyles", value:function(dom, marks) {
      var _this3 = this;
      if ((dom = dom.style) && dom.length) {
        for (var i = 0; i < this.parser.matchedStyles.length; i++) {
          var name = this.parser.matchedStyles[i], value = dom.getPropertyValue(name);
          if (value) {
            for (var _loop4 = function(_after) {
              var rule = _this3.parser.matchStyle(name, value, _this3, _after);
              if (!rule) {
                return after = _after, 0;
              }
              if (rule.ignore) {
                return {v:null};
              }
              marks = rule.clearMark ? marks.filter(function(m) {
                return !rule.clearMark(m);
              }) : marks.concat(_this3.parser.schema.marks[rule.mark].create(rule.attrs));
              if (!1 === rule.consuming) {
                _after = rule;
              } else {
                return after = _after, 0;
              }
              after = _after;
            }, _ret, after = void 0;;) {
              _ret = _loop4(after);
              if (0 === _ret) {
                break;
              }
              if (_ret) {
                return _ret.v;
              }
            }
          }
        }
      }
      return marks;
    }}, {key:"addElementByRule", value:function(dom, rule, marks, continueAfter) {
      var _this4 = this;
      if (rule.node) {
        var nodeType = this.parser.schema.nodes[rule.node];
        if (nodeType.isLeaf) {
          this.insertNode(nodeType.create(rule.attrs), marks) || this.leafFallback(dom, marks);
        } else {
          var inner = this.enter(nodeType, rule.attrs || null, marks, rule.preserveWhitespace);
          if (inner) {
            var sync = !0;
            marks = inner;
          }
        }
      } else {
        marks = marks.concat(this.parser.schema.marks[rule.mark].create(rule.attrs));
      }
      inner = this.top;
      nodeType && nodeType.isLeaf ? this.findInside(dom) : continueAfter ? this.addElement(dom, marks, continueAfter) : rule.getContent ? (this.findInside(dom), rule.getContent(dom, this.parser.schema).forEach(function(node) {
        return _this4.insertNode(node, marks);
      })) : (continueAfter = dom, "string" == typeof rule.contentElement ? continueAfter = dom.querySelector(rule.contentElement) : "function" == typeof rule.contentElement ? continueAfter = rule.contentElement(dom) : rule.contentElement && (continueAfter = rule.contentElement), this.findAround(dom, continueAfter, !0), this.addAll(continueAfter, marks), this.findAround(dom, continueAfter, !1));
      sync && this.sync(inner) && this.open--;
    }}, {key:"addAll", value:function(parent, marks, startIndex, endIndex) {
      var index = startIndex || 0;
      startIndex = startIndex ? parent.childNodes[startIndex] : parent.firstChild;
      for (endIndex = null == endIndex ? null : parent.childNodes[endIndex]; startIndex != endIndex; startIndex = startIndex.nextSibling, ++index) {
        this.findAtPoint(parent, index), this.addDOM(startIndex, marks);
      }
      this.findAtPoint(parent, index);
    }}, {key:"findPlace", value:function(node, marks) {
      for (var route, sync, depth = this.open; 0 <= depth; depth--) {
        var cx = this.nodes[depth], _found2 = cx.findWrapping(node);
        if (_found2 && (!route || route.length > _found2.length) && (route = _found2, sync = cx, !_found2.length)) {
          break;
        }
        if (cx.solid) {
          break;
        }
      }
      if (!route) {
        return null;
      }
      this.sync(sync);
      for (node = 0; node < route.length; node++) {
        marks = this.enterInner(route[node], null, marks, !1);
      }
      return marks;
    }}, {key:"insertNode", value:function(node, marks) {
      if (node.isInline && this.needsBlock && !this.top.type) {
        var block = this.textblockFromContext();
        block && (marks = this.enterInner(block, null, marks));
      }
      var innerMarks = this.findPlace(node, marks);
      if (innerMarks) {
        this.closeExtra();
        marks = this.top;
        marks.match && (marks.match = marks.match.matchType(node.type));
        block = Mark.none;
        innerMarks = _createForOfIteratorHelper(innerMarks.concat(node.marks));
        var _step;
        try {
          for (innerMarks.s(); !(_step = innerMarks.n()).done;) {
            var m = _step.value;
            if (marks.type ? marks.type.allowsMarkType(m.type) : markMayApply(m.type, node.type)) {
              block = m.addToSet(block);
            }
          }
        } catch (err) {
          innerMarks.e(err);
        } finally {
          innerMarks.f();
        }
        marks.content.push(node.mark(block));
        return !0;
      }
      return !1;
    }}, {key:"enter", value:function(type, attrs, marks, preserveWS) {
      var innerMarks = this.findPlace(type.create(attrs), marks);
      innerMarks && (innerMarks = this.enterInner(type, attrs, marks, !0, preserveWS));
      return innerMarks;
    }}, {key:"enterInner", value:function(type, attrs, marks) {
      var solid = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : !1, preserveWS = 4 < arguments.length ? arguments[4] : void 0;
      this.closeExtra();
      var top = this.top;
      top.match = top.match && top.match.matchType(type);
      preserveWS = wsOptionsFor(type, preserveWS, top.options);
      top.options & 4 && 0 == top.content.length && (preserveWS |= 4);
      var applyMarks = Mark.none;
      marks = marks.filter(function(m) {
        return (top.type ? top.type.allowsMarkType(m.type) : markMayApply(m.type, type)) ? (applyMarks = m.addToSet(applyMarks), !1) : !0;
      });
      this.nodes.push(new NodeContext(type, attrs, applyMarks, solid, null, preserveWS));
      this.open++;
      return marks;
    }}, {key:"closeExtra", value:function() {
      var openEnd = 0 < arguments.length && void 0 !== arguments[0] ? arguments[0] : !1, i = this.nodes.length - 1;
      if (i > this.open) {
        for (; i > this.open; i--) {
          this.nodes[i - 1].content.push(this.nodes[i].finish(openEnd));
        }
        this.nodes.length = this.open + 1;
      }
    }}, {key:"finish", value:function() {
      this.open = 0;
      this.closeExtra(this.isOpen);
      return this.nodes[0].finish(this.isOpen || this.options.topOpen);
    }}, {key:"sync", value:function(to) {
      for (var i = this.open; 0 <= i; i--) {
        if (this.nodes[i] == to) {
          return this.open = i, !0;
        }
      }
      return !1;
    }}, {key:"currentPos", get:function() {
      this.closeExtra();
      for (var pos = 0, i = this.open; 0 <= i; i--) {
        for (var content = this.nodes[i].content, j = content.length - 1; 0 <= j; j--) {
          pos += content[j].nodeSize;
        }
        i && pos++;
      }
      return pos;
    }}, {key:"findAtPoint", value:function(parent, offset) {
      if (this.find) {
        for (var i = 0; i < this.find.length; i++) {
          this.find[i].node == parent && this.find[i].offset == offset && (this.find[i].pos = this.currentPos);
        }
      }
    }}, {key:"findInside", value:function(parent) {
      if (this.find) {
        for (var i = 0; i < this.find.length; i++) {
          null == this.find[i].pos && 1 == parent.nodeType && parent.contains(this.find[i].node) && (this.find[i].pos = this.currentPos);
        }
      }
    }}, {key:"findAround", value:function(parent, content, before) {
      if (parent != content && this.find) {
        for (var i = 0; i < this.find.length; i++) {
          null == this.find[i].pos && 1 == parent.nodeType && parent.contains(this.find[i].node) && content.compareDocumentPosition(this.find[i].node) & (before ? 2 : 4) && (this.find[i].pos = this.currentPos);
        }
      }
    }}, {key:"findInText", value:function(textNode) {
      if (this.find) {
        for (var i = 0; i < this.find.length; i++) {
          this.find[i].node == textNode && (this.find[i].pos = this.currentPos - (textNode.nodeValue.length - this.find[i].offset));
        }
      }
    }}, {key:"matchesContext", value:function(context) {
      var _this5 = this;
      if (-1 < context.indexOf("|")) {
        return context.split(/\s*\|\s*/).some(this.matchesContext, this);
      }
      var parts = context.split("/"), option = this.options.context, useRoot = !this.isOpen && (!option || option.parent.type == this.nodes[0].type), minDepth = -(option ? option.depth + 1 : 0) + (useRoot ? 0 : 1);
      return function match(i, depth) {
        for (; 0 <= i; i--) {
          var part = parts[i];
          if ("" == part) {
            if (i == parts.length - 1 || 0 == i) {
              continue;
            }
            for (; depth >= minDepth; depth--) {
              if (match(i - 1, depth)) {
                return !0;
              }
            }
            return !1;
          }
          var next = 0 < depth || 0 == depth && useRoot ? _this5.nodes[depth].type : option && depth >= minDepth ? option.node(depth - minDepth).type : null;
          if (!next || next.name != part && !next.isInGroup(part)) {
            return !1;
          }
          depth--;
        }
        return !0;
      }(parts.length - 1, this.open);
    }}, {key:"textblockFromContext", value:function() {
      var $context = this.options.context;
      if ($context) {
        for (var d = $context.depth; 0 <= d; d--) {
          var deflt = $context.node(d).contentMatchAt($context.indexAfter(d)).defaultType;
          if (deflt && deflt.isTextblock && deflt.defaultAttrs) {
            return deflt;
          }
        }
      }
      for (var name in this.parser.schema.nodes) {
        if ($context = this.parser.schema.nodes[name], $context.isTextblock && $context.defaultAttrs) {
          return $context;
        }
      }
    }}]);
    return ParseContext;
  }();
  module = function() {
    function DOMSerializer(nodes, marks) {
      _classCallCheck(this, DOMSerializer);
      this.nodes = nodes;
      this.marks = marks;
    }
    _createClass(DOMSerializer, [{key:"serializeFragment", value:function(fragment) {
      var _this6 = this, options = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}, target = 2 < arguments.length ? arguments[2] : void 0;
      target || (target = (options.document || window.document).createDocumentFragment());
      var top = target, active = [];
      fragment.forEach(function(node) {
        if (active.length || node.marks.length) {
          for (var keep = 0, rendered = 0; keep < active.length && rendered < node.marks.length;) {
            var next = node.marks[rendered];
            if (_this6.marks[next.type.name]) {
              if (!next.eq(active[keep][0]) || !1 === next.type.spec.spanning) {
                break;
              }
              keep++;
            }
            rendered++;
          }
          for (; keep < active.length;) {
            top = active.pop()[1];
          }
          for (; rendered < node.marks.length;) {
            if (keep = node.marks[rendered++], next = _this6.serializeMark(keep, node.isInline, options)) {
              active.push([keep, top]), top.appendChild(next.dom), top = next.contentDOM || next.dom;
            }
          }
        }
        top.appendChild(_this6.serializeNodeInner(node, options));
      });
      return target;
    }}, {key:"serializeNodeInner", value:function(node, options) {
      var _renderSpec2 = _renderSpec(options.document || window.document, this.nodes[node.type.name](node), null, node.attrs), dom = _renderSpec2.dom;
      if (_renderSpec2 = _renderSpec2.contentDOM) {
        if (node.isLeaf) {
          throw new RangeError("Content hole not allowed in a leaf node spec");
        }
        this.serializeFragment(node.content, options, _renderSpec2);
      }
      return dom;
    }}, {key:"serializeNode", value:function(node) {
      for (var options = 1 < arguments.length && void 0 !== arguments[1] ? arguments[1] : {}, dom = this.serializeNodeInner(node, options), i = node.marks.length - 1; 0 <= i; i--) {
        var wrap = this.serializeMark(node.marks[i], node.isInline, options);
        wrap && ((wrap.contentDOM || wrap.dom).appendChild(dom), dom = wrap.dom);
      }
      return dom;
    }}, {key:"serializeMark", value:function(mark, inline) {
      var options = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : {}, toDOM = this.marks[mark.type.name];
      return toDOM && _renderSpec(options.document || window.document, toDOM(mark, inline), null, mark.attrs);
    }}], [{key:"renderSpec", value:function(doc, structure) {
      return _renderSpec(doc, structure, 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : null, 3 < arguments.length ? arguments[3] : void 0);
    }}, {key:"fromSchema", value:function(schema) {
      return schema.cached.domSerializer || (schema.cached.domSerializer = new DOMSerializer(this.nodesFromSchema(schema), this.marksFromSchema(schema)));
    }}, {key:"nodesFromSchema", value:function(schema) {
      schema = gatherToDOM(schema.nodes);
      schema.text || (schema.text = function(node) {
        return node.text;
      });
      return schema;
    }}, {key:"marksFromSchema", value:function(schema) {
      return gatherToDOM(schema.marks);
    }}]);
    return DOMSerializer;
  }();
  var suspiciousAttributeCache = new WeakMap();
  exports.ContentMatch = ContentMatch;
  exports.DOMParser = require;
  exports.DOMSerializer = module;
  exports.Fragment = Fragment;
  exports.Mark = Mark;
  exports.MarkType = MarkType;
  exports.Node = Node;
  exports.NodeRange = NodeRange;
  exports.NodeType = NodeType;
  exports.ReplaceError = ReplaceError;
  exports.ResolvedPos = ResolvedPos;
  exports.Schema = global;
  exports.Slice = Slice;
};

//# sourceMappingURL=module$node_modules$prosemirror_model$dist$index_cjs.js.map
